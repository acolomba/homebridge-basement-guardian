---
phase: quick-260904-od5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements:
  [
    CONF-06,
    SAFE-01,
    SAFE-02,
    SAFE-03,
    SAFE-04,
    SAFE-05,
    SAFE-06,
    SAFE-07,
    SAFE-08,
    RES-01,
    RES-02,
    RES-03,
    RES-04,
    CTRL-01,
    CTRL-02,
    CTRL-03,
    CTRL-04,
    CTRL-05,
  ]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "The phase-summary checkbox list near the top of ROADMAP.md marks Phase 3, Phase 4, Phase 5, and Phase 5.1 as checked ([x]), matching STATE.md's recorded implementation-complete status for all four; Phase 6 stays unchecked ([ ])."
    - "The '## Progress' table's Plans Complete column reads 8/8 for Phase 3, 6/6 for Phase 4, 19/19 for Phase 5, and 7/7 for a new Phase 5.1 row — none read 0/TBD or omit a phase that has shipped plans."
    - "Phase 3 and Phase 4's Status cells use identical wording that distinguishes implementation-complete-with-deferred-human-verification from both 'in progress' and a fully closed 'Complete', because both phases have open human-verification items per STATE.md's Deferred Verification table."
    - "Phase 5.1's Status cell reads 'Complete' with a completion date of 2026-09-04, because STATE.md records it as fully done — all seven waves executed, both runtimes green, a live check performed — with no deferred verification item against it."
    - "Phase 1, Phase 2, and Phase 6 rows and checkboxes, and the 'Execution Order' line, are byte-identical before and after the edit."
    - "No file other than .planning/ROADMAP.md changes."
  artifacts:
    - ".planning/ROADMAP.md — phase-summary checkbox list (Phase 3/4/5/5.1 lines) and '## Progress' table (Phase 3/4/5 rows corrected, Phase 5.1 row added) updated to match STATE.md."
  key_links:
    - "Checkbox list values <-> Progress table Status column — both must agree that Phase 3, 4, 5, and 5.1 are past 'not started'/blank and are implementation-complete; a reader checking either list gets the same answer."
    - "Progress table Plans Complete counts <-> each phase's own '**Plans**:' line in the '### Phase N' detail section (Phase 3: '8/8 plans executed' at line 151, Phase 4: '6/6 plans executed' at line 201, Phase 5: '19/19 plans executed...' at line 237, Phase 5.1: '7/7 plans executed' at line 332) — the fix must not contradict these already-accurate per-phase headers, which is why the same counts are reused rather than recomputed."
    - "Progress table Status wording <-> STATE.md's 'Deferred Verification' table, which lists only Phase 3 and Phase 4 as verification_deferred_human — Phase 5 is not in that table and gets plain 'Implementation complete' with no deferred-verification qualifier, and Phase 5.1's STATE.md entry (status: complete, stopped_at: Completed 05.1-07-PLAN.md) carries no deferred item at all."
---

<objective>
Repair ROADMAP.md's phase-summary checkbox list and '## Progress' table so both match the true,
measured phase status recorded in STATE.md: Phase 3, Phase 4, and Phase 5 are implementation-complete
(not 'In Progress' or '0/TBD, Not started'), and Phase 5.1 — currently missing a Progress table row
entirely — is fully complete (7/7 plans).

Purpose: a reader of ROADMAP.md today sees Phase 3 reading 'In Progress' after all 8 of its plans
shipped and its verification passed 6/6 must-haves; sees Phase 4 reading '0/TBD, Not started' after
all 6 of its plans shipped and verification passed 17/17 must-haves; sees Phase 5 reading 'In
Progress' after all 19 of its plans shipped; and sees no row at all for Phase 5.1, which finished
seven waves ago. The roadmap is the document a new reader (or a future planning agent) trusts first;
right now it actively contradicts STATE.md, the authoritative source, on every one of these four
phases.

Output: `.planning/ROADMAP.md` with a corrected phase-summary checkbox list and a corrected,
complete '## Progress' table. No other file changes; no code changes.
</objective>

<execution_context>
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
</context>

<investigation_findings>

Read in full during planning, so the task below verifies rather than re-derives.

**Checkbox list** (`.planning/ROADMAP.md` lines 18-24). Phase 1 and Phase 2 already read `- [x]`.
Phase 3, Phase 4, Phase 5, and Phase 5.1 all read `- [ ]`. Phase 6 reads `- [ ]` and is correct as-is
(genuinely unplanned — `**Plans**: TBD` at line 383).

**Progress table** (`.planning/ROADMAP.md` lines 389-396):

    | Phase | Plans Complete | Status | Completed |
    |-------|----------------|--------|-----------|
    | 1. Secure Cloud Foundation | 17/17 | Complete | 2026-08-29 |
    | 2. Safe Gemini Discovery and Identity | 6/6 | Complete    | 2026-08-29 |
    | 3. Safety Monitoring in HomeKit | 8/8 | In Progress|  |
    | 4. Pump Records and Official Controls | 0/TBD | Not started | - |
    | 5. Degraded Operation and Recovery | 19/19 | In Progress|  |
    | 6. Validated Release Candidate | 0/TBD | Not started | - |

Rows 1, 2, and 6 are correct. Row 3's Plans Complete (8/8) is already correct; only Status is wrong
— `03-VERIFICATION.md` verified all six success criteria and 6/6 must-haves (STATE.md line 527-531),
with one human-verification check (flood automation) still open and gating `1.0.0`, not phase
completion (STATE.md line 68-69). Row 4's Plans Complete is stale (`0/TBD` — Phase 4's own detail
section at line 201 already says `6/6 plans executed`); STATE.md line 65 and 505 confirm Phase 4 is
implementation-complete, verified 17/17 must-haves, with six human items deferred to
`/gsd-verify-work 4` (none blocking phase completion). Row 5's Plans Complete (19/19) is already
correct; STATE.md gives no deferred-verification qualifier for Phase 5 — it is absent from the
'Deferred Verification' table (STATE.md lines 502-505), which lists only Phase 3 and Phase 4 — so
Phase 5's Status is plain 'Implementation complete', not the same deferred-verification wording used
for 3 and 4. Phase 5.1 has no row at all; STATE.md's frontmatter reads `current_phase: 05.1`,
`status: complete`, `stopped_at: Completed 05.1-07-PLAN.md`, `last_activity_desc: Phase 05.1
complete — seven waves, both runtimes green, live check performed`, dated `2026-09-04`, and the
phase's own '### Phase 05.1' detail section (line 332) already reads `**Plans**: 7/7 plans executed`.

**No existing status-wording convention exists in ROADMAP.md** for 'implementation complete, human
verification deferred' — every `### Phase N` detail heading uses plain `**Plans**: N/N plans
executed` with no deferred-verification qualifier; that nuance lives only in STATE.md's 'Deferred
Verification' table. This task introduces the wording 'Implementation complete (human verification
pending)' for the Progress table's Status column only, used identically for Phase 3 and Phase 4 so
the two rows read the same way for the same underlying situation.

**Confirmed plan/summary count for Phase 5.1**: `ls .planning/phases/05.1-per-pump-trust-and-monotonic-silence/`
shows seven `05.1-0N-PLAN.md` files (01 through 07) each paired with a matching `05.1-0N-SUMMARY.md`,
plus `05.1-CONTEXT.md`, `05.1-DISCUSSION-LOG.md`, `05.1-PATTERNS.md`, `05.1-RESEARCH.md`, and
`05.1-VALIDATION.md`. No eighth plan or partial plan exists — 7/7 is exact, not rounded.

</investigation_findings>

<tasks>

<task type="auto">
  <name>Task 1: Correct the checkbox list and the Progress table against STATE.md</name>
  <precondition>`grep -c '^- \[ \] \*\*Phase 3: Safety Monitoring in HomeKit\*\*' .planning/ROADMAP.md` returns `1` and `grep -c '| 5\. Degraded Operation and Recovery | 19/19 | In Progress' .planning/ROADMAP.md` returns `1`. If either returns `0`, ROADMAP.md has already been edited since this plan was written — stop and report rather than editing against a stale baseline.</precondition>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 18-24 — the phase-summary checkbox list.
    - `.planning/ROADMAP.md` lines 385-396 — the '## Progress' table and the 'Execution Order' line above it.
    - `.planning/STATE.md` lines 1-17 (frontmatter), 64-77 (Phase 3/4 implementation-complete notes), 500-535 (Deferred Verification table and its narrative) — the authoritative source this edit must match.
  </read_first>
  <action>
Edit `.planning/ROADMAP.md` in two places. Use the Edit tool with a scoped `old_string`/`new_string`
for each — never rewrite the whole file.

**Edit 1 — checkbox list.** Replace the four consecutive lines for Phase 3, Phase 4, Phase 5, and
Phase 5.1 (currently each starting `- [ ]`) with the same four lines unchanged except each leading
`- [ ]` becomes `- [x]`. Phase 1, Phase 2 (already `- [x]`, untouched) and Phase 6 (stays `- [ ]`,
untouched) are outside this edit's span.

**Edit 2 — Progress table.** Replace the four table rows for Phase 3 through Phase 6 (the block
starting `| 3. Safety Monitoring in HomeKit ...` and ending `| 6. Validated Release Candidate ...`)
with five rows: the corrected Phase 3, 4, and 5 rows, a new Phase 5.1 row inserted between Phase 5
and Phase 6, and Phase 6 unchanged. The header row and the Phase 1/Phase 2 rows above this block, and
the 'Execution Order' line above the table, are outside this edit's span and must not change.

New Phase 3 row: Plans Complete stays `8/8`. Status becomes `Implementation complete (human
verification pending)`. Completed cell stays blank (STATE.md gives no single implementation-complete
date for Phase 3 — its own detail section and Deferred Verification table only date the individual
verification checks).

New Phase 4 row: Plans Complete becomes `6/6` (was the stale `0/TBD`). Status becomes
`Implementation complete (human verification pending)` — identical wording to Phase 3's, because
both share the same underlying situation (STATE.md's Deferred Verification table lists both, neither
other phase). Completed cell stays blank, same reasoning as Phase 3.

New Phase 5 row: Plans Complete stays `19/19`. Status becomes `Implementation complete` — no
deferred-verification qualifier, because Phase 5 is absent from STATE.md's Deferred Verification
table (only Phase 3 and Phase 4 appear there). Completed cell stays blank, same reasoning as Phase 3.

New Phase 5.1 row (inserted, did not exist before): `5.1. Per-Pump Trust and Monotonic Silence` in
the Phase column, Plans Complete `7/7`, Status `Complete` (no deferred-verification qualifier —
STATE.md's frontmatter reads `status: complete` with no open item against this phase), Completed cell
`2026-09-04` (STATE.md `last_activity: 2026-09-04`, `last_activity_desc: Phase 05.1 complete — seven
waves, both runtimes green, live check performed`).

Phase 6 row: unchanged — `0/TBD`, `Not started`, `-`.

Do not touch the 'Execution Order' line, the table header row, or the Phase 1/Phase 2 rows. Do not
touch any `### Phase N` detail section — those headings already carry the correct plan counts this
edit reuses (verify they still match after editing; do not recompute them independently).

Commit discipline: `pre-commit run --files .planning/ROADMAP.md`, fix anything it flags, restage,
re-run until clean. Then `git add .planning/ROADMAP.md` and a plain
`git commit -m "docs(roadmap): reconcile progress table and checklist with STATE.md"` with no
pathspec — never `gsd-tools query commit`, which can report success while committing nothing
(STATE.md Blockers). Do not stage `.planning/milestone.lock` (untracked; a separate concern). Then
`git show --name-only --format="" HEAD` and confirm the file list names only `.planning/ROADMAP.md`.
Stay on the current branch; never `--no-verify`; never rebase.
  </action>
  <verify>
    <automated>set -e; grep -c '^- \[x\] \*\*Phase 3: Safety Monitoring in HomeKit\*\*' .planning/ROADMAP.md | grep -qx 1; grep -c '^- \[x\] \*\*Phase 4: Pump Records and Official Controls\*\*' .planning/ROADMAP.md | grep -qx 1; grep -c '^- \[x\] \*\*Phase 5: Degraded Operation and Recovery\*\*' .planning/ROADMAP.md | grep -qx 1; grep -c '^- \[x\] \*\*Phase 5.1: Per-Pump Trust and Monotonic Silence\*\*' .planning/ROADMAP.md | grep -qx 1; grep -c '^- \[ \] \*\*Phase 6: Validated Release Candidate\*\*' .planning/ROADMAP.md | grep -qx 1; grep -cF '| 3. Safety Monitoring in HomeKit | 8/8 | Implementation complete (human verification pending) |' .planning/ROADMAP.md | grep -qx 1; grep -cF '| 4. Pump Records and Official Controls | 6/6 | Implementation complete (human verification pending) |' .planning/ROADMAP.md | grep -qx 1; grep -cF '| 5. Degraded Operation and Recovery | 19/19 | Implementation complete |' .planning/ROADMAP.md | grep -qx 1; grep -cF '| 5.1. Per-Pump Trust and Monotonic Silence | 7/7 | Complete | 2026-09-04 |' .planning/ROADMAP.md | grep -qx 1; grep -cF '| 6. Validated Release Candidate | 0/TBD | Not started | - |' .planning/ROADMAP.md | grep -qx 1; grep -cF '| 1. Secure Cloud Foundation | 17/17 | Complete | 2026-08-29 |' .planning/ROADMAP.md | grep -qx 1; grep -cF '**Execution Order:** Phase 1 → Phase 2 → Phase 3 → Phase 4 → Phase 5 → Phase 6' .planning/ROADMAP.md | grep -qx 1; git show --name-only --format="" HEAD | grep -qxF '.planning/ROADMAP.md'; test "$(git show --name-only --format="" HEAD | wc -l)" -eq 1; echo ALL_OK</automated>
    <fails_when>Any checkbox grep returns other than 1 — a leading `- [x]`/`- [ ]` did not flip as
    specified, or flipped a line it should not have. Any table-row grep returns other than 1 — a
    Status cell, Plans Complete count, or the new Phase 5.1 row does not read exactly as specified,
    or the Phase 1 row / Execution Order line (checked as untouched controls) no longer matches its
    original text, meaning the edit's span leaked into content it was told not to touch. The final
    two checks fail if the commit is empty (the `gsd-tools query commit` hazard STATE.md's Blockers
    section records) or if it swept in a file other than `.planning/ROADMAP.md`.</fails_when>
  </verify>
  <done>All five checkbox greps and all seven table/control-line greps return exactly 1. The commit
  named by `git show --name-only --format="" HEAD` lists only `.planning/ROADMAP.md`. The
  checkbox list and the Progress table agree with each other and with STATE.md: Phase 3 and Phase 4
  read implementation-complete with deferred human verification, Phase 5 reads implementation-complete
  with no deferral qualifier, Phase 5.1 reads fully Complete with a 2026-09-04 date, and Phase 1,
  Phase 2, and Phase 6 are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning record -> a future reader | ROADMAP.md's checkbox list and Progress table are read as a trustworthy summary of phase status; a reader does not cross-check STATE.md unless something looks wrong. |
| Planning record -> the next planning agent | `/gsd-plan-phase` and `/gsd-progress` read ROADMAP.md to decide what is already done; a stale row could cause a future agent to re-plan a finished phase or skip a phase that is not actually done. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-od5-01 | Tampering | Scoped edit accidentally changes Phase 1, Phase 2, Phase 6, or the Execution Order line | medium | mitigate | Edits target only the four checkbox lines and the four-row table block by exact `old_string`; verify greps check the Phase 1 row and Execution Order line as untouched controls. |
| T-od5-02 | Repudiation | No record of what the roadmap said before this edit | low | mitigate | Git history preserves the prior version; the commit is a single, named, non-empty change verified by `git show --name-only`. |
| T-od5-03 | Tampering | Source tree or other planning files touched by a documentation-only task | high | mitigate | `files_modified` names only `.planning/ROADMAP.md`; the commit is verified by content to touch exactly that one path. |
| T-od5-04 | Tampering | An empty commit reported as successful | high | mitigate | `gsd-tools query commit` is forbidden per STATE.md Blockers; the task verifies its own commit with `git show --name-only --format="" HEAD`. |
| T-od5-SC | Tampering | Package installs | n/a | accept | No package is installed, removed, or upgraded by this task. |
</threat_model>

<verification>

After the task:

1. The five checkbox-list greps and the seven table/control-line greps in `<verify>` all pass.
2. `git show --name-only --format="" HEAD` names only `.planning/ROADMAP.md`.
3. `git diff HEAD~1..HEAD -- .planning/ROADMAP.md` shows only the checkbox-list and Progress-table
   hunks — no changes inside any `### Phase N` detail section, no changes to the Execution Order
   line, and no changes to the Phase 1 or Phase 2 rows.
4. Manually re-read the finished table and checkbox list once against STATE.md's frontmatter
   (`current_phase: 05.1`, `status: complete`) and Deferred Verification table to confirm no fifth
   discrepancy was missed.

</verification>

<success_criteria>

- The phase-summary checkbox list and the `## Progress` table agree with each other and with
  STATE.md on Phase 3, Phase 4, Phase 5, and Phase 5.1's status.
- Phase 5.1 has a Progress table row for the first time: `7/7`, `Complete`, `2026-09-04`.
- Phase 3 and Phase 4 share one consistent status wording that distinguishes
  implementation-complete-with-deferred-verification from both "in progress" and a fully closed
  "Complete".
- Phase 1, Phase 2, Phase 6, and the Execution Order line are unchanged.
- Exactly one commit exists, touching only `.planning/ROADMAP.md`.

</success_criteria>

<output>
Create `.planning/quick/260904-od5-repair-the-stale-roadmap-md-progress-tab/260904-od5-SUMMARY.md`
when done.

Record: the before/after text of both edited regions, the result of every verify grep, the commit
hash, and confirmation that `git diff` for this commit touches only the two intended regions of
`.planning/ROADMAP.md`.
</output>
