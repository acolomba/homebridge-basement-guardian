---
phase: 260828-bq0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements:
  - CONF-05
  - CONF-06
  - AUTH-02
  - DEV-07
  - DEV-08
  - SAFE-01
  - SAFE-05
  - CTRL-02
  - CTRL-04
  - RES-01
  - RES-02
  - RES-03
  - REL-07
  - REL-08

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "REQUIREMENTS.md defines 46 v1 requirements and its traceability table maps all 46 with zero unmapped."
    - "No requirement text and no roadmap success criterion makes phase completion conditional on a hardware or real-home validation gate."
    - "RES-01 and RES-02 sit in Phase 3 in both REQUIREMENTS.md and ROADMAP.md; Phase 5 keeps only RES-03 and RES-04."
    - "The union of the ROADMAP per-phase Requirements lines equals the REQUIREMENTS.md traceability ID set, with identical phase assignment."
    - "PROJECT.md records gate G-004 and the backup-battery fault-adapter open proposal, and every locked decision block is byte-identical to its prior state."
    - "STATE.md presents G-001 through G-004 as 1.0.0 release gates, not phase blockers, and carries the four new pending todos."
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/PROJECT.md
    - .planning/STATE.md
  key_links:
    - "REQUIREMENTS.md traceability table <-> ROADMAP.md per-phase Requirements lines (same ID set, same phase)."
    - "REL-07 gate list <-> PROJECT.md Context gate bullets <-> STATE.md Blockers/Concerns (all four gates present in all three)."
    - "CONF-06 rationale <-> D-017 seven-slug enumeration (no eighth slug is introduced)."
---

<objective>
Apply the ten locked resolutions from `260828-bq0-CONTEXT.md` to the four planning
artifacts: `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/PROJECT.md`,
and `.planning/STATE.md`.

Purpose: the roadmap currently stalls Phases 3 through 5 behind hardware and real-home
validation gates that cannot close on demand, three v1 requirements are missing, and
`RES-01`/`RES-02` sit in the wrong phase. This makes the phase sequence executable and
the traceability record complete.

Output: four edited planning documents, three commits, and a mechanically verified
match between the REQUIREMENTS.md traceability table and the ROADMAP.md phase
requirement lines.
</objective>

<execution_context>
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260828-bq0-refine-planning-artifacts-against-ingest/260828-bq0-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.agents/skills/simple-english/SKILL.md
</context>

<constraints>
- `260828-bq0-CONTEXT.md` is the authoritative and complete change list. Do not add
  scope. Do not drop items. Do not re-derive the audit.
- Documentation only. Do not touch `src/**`, `package.json`, `config.schema.json`,
  `docs/research/**`, or `.planning/intel/**`.
- `.planning/config.json` is already correct. Verify it. Do not edit it.
- Read `.planning/intel/**` only to copy exact facts and canonical names. It is the
  ingested record; its counts must stay accurate.
- Keep the existing document structure, heading order, bullet style, and requirement ID
  format in every file. Add no new top-level sections beyond the one named in Task 3.
- New and reworded requirement prose follows the Simplified Technical English register
  already used in these files: short sentences, active voice, one meaning per word,
  condition before command. Load `.agents/skills/simple-english/SKILL.md` before writing
  new requirement text.
- Stay on the current branch `features/planning-refinement`. Never commit to `main`.
  Never pass `--no-verify`.
- Before each `git commit`, run `pre-commit run --files <changed files>`. Fix failures,
  restage, and rerun until the run is clean. A failed hook means the commit did not
  happen, so do not try to recover with `--amend`.
- This repository checkout is a normal checkout, not a linked worktree, so the standard
  pre-commit invocation works and no `SKIP=` prefix is needed.
- Scratch files go under
  `/tmp/claude-1000/-home-acolomba-homebridge-basement-guardian/e36e0b97-a9ef-41c9-a0be-bb757d28a7eb/scratchpad`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite REQUIREMENTS.md against the locked change list</name>
  <files>.planning/REQUIREMENTS.md</files>
  <precondition>The checked-out branch is `features/planning-refinement`
  (`git rev-parse --abbrev-ref HEAD`), and no tracked file under `src/`, `docs/`,
  `.planning/intel/`, `package.json`, or `config.schema.json` has uncommitted changes
  (`git status --porcelain -- src docs .planning/intel package.json config.schema.json`
  is empty). Halt if either check fails. Note that `.planning/config.json` is expected
  to show as modified: its seven values were applied before planning and are not yet
  committed. Leave it exactly as it is.</precondition>
  <read_first>
    - `260828-bq0-CONTEXT.md` sections A1 through A5. These are the exact edits.
    - `.planning/intel/constraints.md` around lines 360-372 for the canonical functional
      service names, and around line 494 for the `offlineConfirmationPollCount` bound.
    - `.planning/intel/constraints.md` around line 512 for the source sentence behind
      gate G-004.
    - `.planning/intel/context.md` around lines 12-22 and 430-436 for the heartbeat
      period, the normal shadow-silence window, and the two-missed-heartbeat rule.
    - `.planning/intel/context.md` around lines 918-930 for the poll-interval bound and
      default.
    - `.planning/PROJECT.md` lines 95-97 for `D-015`, `D-016`, and `D-017`.
  </read_first>
  <action>
Apply change-list section A to `.planning/REQUIREMENTS.md`.

Section A1 — strip the gate-precondition prefix that currently opens `SAFE-01` and
`CTRL-04`, and remove the gate-conditional clause that currently opens `CTRL-02`.
Restate each of the three per the A1 text. In `SAFE-01`, state the explicit
water-level lookup over the legal codes `0`, `1`, `3`, `7`, `15`, `31`, the fault
response to an unknown value, flood activation only at the threshold, and that the
flood-threshold constant and every level mapping other than `1` stay provisional until
G-002 closes and that G-002 gates only the `1.0.0` release. In `CTRL-04`, state that
the control follows reported `alarm_audio_muted`, sends only the official
`{"alarm_audio_muted": true}` boolean, offers no duration, timer, simulated unmute, or
off write while mute is active, and that G-001 gates only the `1.0.0` release. In
`CTRL-02`, keep the documentation obligation and leave G-003 listed in `REL-07` alone.

Section A2 — reword `RES-01`, `RES-03`, `CONF-05`, `CONF-06`, `AUTH-02`, `SAFE-01`, and
`SAFE-05` per the A2 bullets. Use the canonical functional service names **Sump Pit
Level** and **Sump Mains Power** from the intel service table. In `RES-03`, name the
field `connectivity.connected === false` explicitly and demote `data.offline === true`
to corroboration and diagnostics that never activate the adapter alone, citing `D-016`.
In `CONF-05`, add the 300-to-3600-second poll bound with the approximately 900-second
default, and note that the `D-015` 105-to-120-minute worst-case offline confirmation
assumes the default interval while a 3600-second interval with a count of 8 stretches
confirmation to roughly eight hours. In `CONF-06`, record why the enumeration holds
seven slugs and not eight: **Primary Pump Running** is an activity adapter, not a
notification adapter, and it is deliberately not removable.

Section A3 — add `DEV-07` and `DEV-08` to the "Device Families and Accessory Lifecycle"
section and `REL-08` to the "Release Quality, Privacy, and Distribution" section, using
the full text given in A3. Match the existing `- [ ] **ID**: ...` bullet form.

Section A4 — add G-004 to the `REL-07` gate list beside G-001, G-002, and G-003, and
state inside `REL-07` what G-004 covers: Leak Sensor notification delivery for **Sump
Pit Flood** validated in a real eligible Apple home with a current home hub and the
current Home architecture, with no Critical Alerts guarantee claimed anywhere in the
documentation. Do not create a new gate-definition section here; the four gate
definitions live as Context bullets in `PROJECT.md` and Task 3 adds the G-004 bullet
there.

Section A5 — in the traceability table, move `RES-01` and `RES-02` to Phase 3, and add
rows `DEV-07` to Phase 2, `DEV-08` to Phase 2, and `REL-08` to Phase 6. Place each new
row in the existing ID-group order rather than appending at the end. Update the coverage
block to 46 total, 46 mapped, 0 unmapped. Update the "Last updated" footer line to
2026-08-28 with a short reason.

Load `.agents/skills/simple-english/SKILL.md` before writing the new and reworded
prose. Keep sentences short and active. Do not restructure or renumber any requirement
that the change list does not name.
  </action>
  <verify>
    <automated>
cd /home/acolomba/homebridge-basement-guardian
SP=/tmp/claude-1000/-home-acolomba-homebridge-basement-guardian/e36e0b97-a9ef-41c9-a0be-bb757d28a7eb/scratchpad
mkdir -p "$SP"

# 1. Traceability table holds exactly 46 requirement rows.
ROWS=$(grep -cE '^\| [A-Z]+-[0-9]+ \| Phase [0-9]+ \| ' .planning/REQUIREMENTS.md)
test "$ROWS" -eq 46 || { echo "FAIL traceability rows=$ROWS want 46"; exit 1; }

# 2. Every v1 ID defined in the body appears exactly once in the table, and vice versa.
#    sort without -u so a duplicate on either side shows up as a diff.
grep -oE '^- \[ \] \*\*[A-Z]+-[0-9]+\*\*' .planning/REQUIREMENTS.md \
  | grep -oE '[A-Z]+-[0-9]+' | sort > "$SP/body-ids.txt"
grep -oE '^\| [A-Z]+-[0-9]+ \| Phase [0-9]+ \| ' .planning/REQUIREMENTS.md \
  | grep -oE '[A-Z]+-[0-9]+' | sort > "$SP/table-ids.txt"
diff "$SP/body-ids.txt" "$SP/table-ids.txt" || { echo "FAIL body/table ID mismatch"; exit 1; }
test "$(wc -l < "$SP/body-ids.txt")" -eq 46 || { echo "FAIL body v1 ID count"; exit 1; }

# 3. Coverage block agrees.
grep -qE 'v1 requirements: 46 total' .planning/REQUIREMENTS.md || { echo "FAIL coverage total"; exit 1; }
grep -qE 'Mapped to phases: 46' .planning/REQUIREMENTS.md || { echo "FAIL coverage mapped"; exit 1; }
grep -qE 'Unmapped: 0' .planning/REQUIREMENTS.md || { echo "FAIL coverage unmapped"; exit 1; }

# 4. New requirements and the fourth gate exist.
for ID in DEV-07 DEV-08 REL-08; do
  grep -qE "^- \[ \] \*\*$ID\*\*:" .planning/REQUIREMENTS.md || { echo "FAIL missing $ID"; exit 1; }
done
grep -q 'G-004' .planning/REQUIREMENTS.md || { echo "FAIL G-004 absent"; exit 1; }

# 5. Phase moves landed.
grep -qE '^\| RES-01 \| Phase 3 \|' .planning/REQUIREMENTS.md || { echo "FAIL RES-01 phase"; exit 1; }
grep -qE '^\| RES-02 \| Phase 3 \|' .planning/REQUIREMENTS.md || { echo "FAIL RES-02 phase"; exit 1; }
grep -qE '^\| DEV-07 \| Phase 2 \|' .planning/REQUIREMENTS.md || { echo "FAIL DEV-07 phase"; exit 1; }
grep -qE '^\| DEV-08 \| Phase 2 \|' .planning/REQUIREMENTS.md || { echo "FAIL DEV-08 phase"; exit 1; }
grep -qE '^\| REL-08 \| Phase 6 \|' .planning/REQUIREMENTS.md || { echo "FAIL REL-08 phase"; exit 1; }

# 6. No requirement text still opens with a gate precondition.
#    HTML-comment lines are excluded so a note can never satisfy the gate by accident.
LEFT=$(grep -v '^[[:space:]]*<!--' .planning/REQUIREMENTS.md | grep -cE 'After G-0[0-9][0-9]')
test "$LEFT" -eq 0 || { echo "FAIL gate precondition still present x$LEFT"; exit 1; }

# 7. Canonical functional service names are used.
grep -q 'Sump Pit Level' .planning/REQUIREMENTS.md || { echo "FAIL Sump Pit Level"; exit 1; }
grep -q 'Sump Mains Power' .planning/REQUIREMENTS.md || { echo "FAIL Sump Mains Power"; exit 1; }

echo "REQUIREMENTS.md OK"
    </automated>
    <human-check>Read the new `DEV-07`, `DEV-08`, `REL-08` text and the reworded
    `SAFE-01`, `CTRL-02`, `CTRL-04`, `RES-01`, `RES-03`, `CONF-05`, `CONF-06`, and
    `AUTH-02`. Confirm the register matches the surrounding requirements: short active
    sentences, no marketing tone, no hedging.</human-check>
  </verify>
  <done>
`.planning/REQUIREMENTS.md` defines 46 v1 requirements including `DEV-07`, `DEV-08`, and
`REL-08`; the traceability table has 46 rows whose ID set exactly equals the body ID set;
`RES-01` and `RES-02` map to Phase 3; the coverage block reads 46/46/0; `REL-07` lists
four gates; no requirement text gates phase completion on a validation gate; the footer
date reads 2026-08-28. `pre-commit run --files .planning/REQUIREMENTS.md` is clean and the
change is committed on `features/planning-refinement` with a Conventional Commits message
such as `docs(planning): restate requirements against ingested intel`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Realign ROADMAP.md phases with the corrected requirement set</name>
  <files>.planning/ROADMAP.md</files>
  <precondition>Task 1 is committed. `.planning/REQUIREMENTS.md` passes the Task 1
  verify block, because Task 2 cross-checks against its traceability table.</precondition>
  <read_first>
    - `260828-bq0-CONTEXT.md` section B. These are the exact edits.
    - The committed `.planning/REQUIREMENTS.md` traceability table, which is now the
      source of truth for phase assignment.
    - `.planning/PROJECT.md` line 106 for `D-014`, the preserve-and-mark invariant.
  </read_first>
  <action>
Apply change-list section B to `.planning/ROADMAP.md`.

Overview — add one sentence stating that the hardware and real-home validation gates
block only the `1.0.0` release and never block phase completion.

Phase 2 — extend the Requirements line with `DEV-07` and `DEV-08`. Add two success
criteria: truthful identity and firmware metadata appear on the accessory, and a profile
or payload that stops validating degrades the accessory in place instead of
unregistering it.

Phase 3 — extend the Requirements line with `RES-01` and `RES-02`. Rewrite success
criterion 1 so it drops the gate precondition and becomes achievable without hardware
validation: an explicit lookup, a fault on an unknown code, flood activation only at the
threshold, and the threshold constant marked provisional. Add a success criterion for
the `D-014` invariant: an invalid or omitted field preserves the last valid value and
faults only the narrowest owning scope, and `serial_communications === false` activates
**Pump Controller Link Lost** at once.

Phase 4 — rewrite success criterion 3 so it drops the gate precondition. Rewrite success
criterion 5 so G-003 is no longer a Phase 4 completion condition; keep the Activity
History documentation obligation and leave the G-003 real-home validation to Phase 6.

Phase 5 — remove `RES-01` and `RES-02` from the Requirements line, leaving `RES-03` and
`RES-04`. Rewrite the phase goal and the success criteria so the phase covers restart
behavior, confirmed-offline detection, monitoring-path degradation, and recovery. Keep
the criterion that separates controller-link loss, vendor-confirmed device offline, and
a degraded REST or MQTT monitoring path. Update the matching one-line phase entry in the
`## Phases` list and the `## Progress` table row so the phase title and goal stay
consistent across all three places.

Phase 6 — extend the Requirements line with `REL-08`. Update success criterion 4 to list
G-001, G-002, G-003, and G-004. Add a success criterion covering the `REL-08`
documentation obligations.

Keep the existing heading order, the `**Goal**` / `**Depends on**` / `**Requirements**` /
`**Success Criteria**` / `**Plans**` field order, and the numbered criterion style. Keep
`**Plans**: TBD` on every phase. Do not renumber phases.
  </action>
  <verify>
    <automated>
cd /home/acolomba/homebridge-basement-guardian
SP=/tmp/claude-1000/-home-acolomba-homebridge-basement-guardian/e36e0b97-a9ef-41c9-a0be-bb757d28a7eb/scratchpad
mkdir -p "$SP"

# 1. Build (ID, phase) pairs from the ROADMAP per-phase Requirements lines.
awk '
  /^### Phase /   { ph=$3; sub(/:.*/,"",ph) }
  /^\*\*Requirements\*\*:/ {
      line=$0; sub(/^\*\*Requirements\*\*:[[:space:]]*/,"",line)
      n=split(line, a, /,[[:space:]]*/)
      for (i=1;i<=n;i++) { gsub(/[[:space:]]/,"",a[i]); if (a[i]!="") print a[i] " Phase " ph }
  }' .planning/ROADMAP.md | sort > "$SP/roadmap-pairs.txt"

# 2. Build the same pairs from the REQUIREMENTS.md traceability table.
grep -oE '^\| [A-Z]+-[0-9]+ \| Phase [0-9]+ \| ' .planning/REQUIREMENTS.md \
  | sed -E 's/^\| ([A-Z]+-[0-9]+) \| (Phase [0-9]+) \| /\1 \2/' | sort > "$SP/req-pairs.txt"

# 3. The two sets must be identical: same IDs, same phase, no duplicates.
diff "$SP/roadmap-pairs.txt" "$SP/req-pairs.txt" || { echo "FAIL roadmap/requirements phase mismatch"; exit 1; }
test "$(wc -l < "$SP/roadmap-pairs.txt")" -eq 46 || { echo "FAIL roadmap pair count"; exit 1; }

# 4. No success criterion still gates phase completion on a validation gate.
LEFT=$(grep -v '^[[:space:]]*<!--' .planning/ROADMAP.md | grep -cE 'After G-0[0-9][0-9]')
test "$LEFT" -eq 0 || { echo "FAIL gate precondition still present x$LEFT"; exit 1; }

# 5. Phase 6 criterion 4 lists all four gates.
grep -qE 'G-001.*G-002.*G-003.*G-004' .planning/ROADMAP.md || { echo "FAIL four-gate list"; exit 1; }

# 6. Overview states the release-only gating rule.
grep -qiE 'block(s)? only the .1\.0\.0. release|only the .1\.0\.0. release, not phase completion' .planning/ROADMAP.md \
  || { echo "FAIL overview gating sentence"; exit 1; }

# 7. Structure preserved: six phase sections, six Plans lines, six Progress rows.
test "$(grep -c '^### Phase ' .planning/ROADMAP.md)" -eq 6 || { echo "FAIL phase section count"; exit 1; }
test "$(grep -c '^\*\*Plans\*\*: TBD' .planning/ROADMAP.md)" -eq 6 || { echo "FAIL Plans lines"; exit 1; }

echo "ROADMAP.md OK"
    </automated>
    <human-check>Read the rewritten Phase 5 goal and criteria. Confirm the phase now
    reads as restart behavior, confirmed-offline detection, monitoring-path degradation,
    and recovery, and that nothing there duplicates the Phase 3 `D-014` invariant.</human-check>
  </verify>
  <done>
`.planning/ROADMAP.md` Overview states the release-only gating rule; Phase 2 lists
`DEV-07` and `DEV-08`; Phase 3 lists `RES-01` and `RES-02` and carries the `D-014`
criterion; Phase 4 no longer treats G-001 or G-003 as completion conditions; Phase 5
lists only `RES-03` and `RES-04` with a rewritten goal; Phase 6 lists `REL-08` and all
four gates. The ROADMAP-to-REQUIREMENTS pair diff is empty at 46 pairs.
`pre-commit run --files .planning/ROADMAP.md` is clean and the change is committed with a
message such as `docs(planning): realign roadmap phases with requirement set`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the open proposal in PROJECT.md, refresh STATE.md, verify config.json</name>
  <files>.planning/PROJECT.md, .planning/STATE.md</files>
  <precondition>Task 2 is committed, so `git show HEAD:.planning/PROJECT.md` still holds
  the pre-edit locked decision blocks that this task's tamper check compares
  against.</precondition>
  <read_first>
    - `260828-bq0-CONTEXT.md` sections C and D, plus Decision 4 for the proposal
      rationale.
    - `.planning/PROJECT.md` lines 41-49 (Context) and 131-139 (Evolution and footer).
    - `.planning/intel/constraints.md` around lines 260-262 and 285-292 for
      `battery_health == 32` and vendor rule `WW-GEM-ALERT-3`.
  </read_first>
  <action>
Apply change-list sections C and D, then verify the already-applied config.

`.planning/PROJECT.md` Context — add a bullet defining G-004: validate **Sump Pit
Flood** Leak Sensor notification delivery in a real eligible Apple home with a current
home hub and the current Home architecture, and confirm that no documentation claims a
Critical Alerts guarantee. Match the wording style of the existing G-001, G-002, and
G-003 bullets. Add a second bullet stating that G-001 through G-004 block the `1.0.0`
release only, never phase completion, and that phases deliver the implementation with
unvalidated constants marked provisional.

`.planning/PROJECT.md` Evolution — add an "Open Proposals" subsection holding the
backup-battery fault-adapter proposal: `intel/constraints.md` lists `battery_health == 32`
(NotDetected) as a fault-bearing signal with vendor rule `WW-GEM-ALERT-3`, `D-008`
enumerates only five Apple Home fault adapters, and Apple Home does not render
`StatusFault`, so an undetected backup battery is a total loss of backup protection that
stays invisible in Apple Home. State that the proposal resolves during Phase 3 discussion
and that resolving it in favor of a sixth adapter requires a revision to `D-008` in
`docs/research/DECISIONS.md` first, per the existing Evolution rule. Update the "Last
updated" footer line to 2026-08-28.

Change nothing inside any `<decisions status="locked">` block. Add no decision, remove
none, reword none.

`.planning/STATE.md` — under Blockers/Concerns, reframe G-001, G-002, and G-003 as
`1.0.0` release gates rather than phase blockers, and add G-004 with the Leak Sensor
notification validation and the no-Critical-Alerts-claim obligation. Under Pending Todos,
replace the placeholder with four items: the backup-battery fault-adapter proposal for
Phase 3 discussion; `package.json` `version: "1.0.0"` with `private: true`, which
contradicts the `D-026` staged `0.x` prerelease plan; `package.json`
`license: "Apache-2.0"`, which contradicts `D-035`'s `SEE LICENSE IN LICENSE`; and
`homebridge-lib` still present as a runtime dependency together with
`config.schema.json` still carrying `strictValidation: false`. Mark each repository-drift
item with the phase that resolves it. Under Accumulated Context / Decisions, extend the
Phase 3 anchor with the `D-014` preserve-and-mark invariant and extend the Phase 4 anchor
to record that the gates no longer block phase completion. Keep
`progress.total_phases` at 6 and leave every other frontmatter value untouched. Update
the "Last activity" line to 2026-08-28 with a one-line description.

Do not open `.planning/config.json` for editing. The verify block below confirms its
seven already-applied values.
  </action>
  <verify>
    <automated>
cd /home/acolomba/homebridge-basement-guardian

# 1. Tamper check: every locked decision block is byte-identical to the committed state.
#    Run this BEFORE staging, while HEAD still points at the Task 2 commit.
diff <(git show HEAD:.planning/PROJECT.md | sed -n '/^<decisions/,/^<\/decisions>/p') \
     <(sed -n '/^<decisions/,/^<\/decisions>/p' .planning/PROJECT.md) \
  || { echo "FAIL locked decision block changed"; exit 1; }
test "$(grep -c '^<decisions status="locked"' .planning/PROJECT.md)" -eq 5 \
  || { echo "FAIL decision block count"; exit 1; }

# 2. PROJECT.md gained G-004, the release-only gating rule, and the open proposal.
grep -q 'G-004' .planning/PROJECT.md || { echo "FAIL PROJECT G-004"; exit 1; }
grep -qi 'Open Proposals' .planning/PROJECT.md || { echo "FAIL Open Proposals section"; exit 1; }
grep -q 'WW-GEM-ALERT-3' .planning/PROJECT.md || { echo "FAIL proposal rationale"; exit 1; }
grep -q 'D-008' .planning/PROJECT.md || { echo "FAIL D-008 reference"; exit 1; }
grep -q 'Last updated: 2026-08-28' .planning/PROJECT.md || { echo "FAIL PROJECT footer date"; exit 1; }

# 3. STATE.md carries all four gates and the four pending todos.
for G in G-001 G-002 G-003 G-004; do
  grep -q "$G" .planning/STATE.md || { echo "FAIL STATE missing $G"; exit 1; }
done
grep -q 'homebridge-lib' .planning/STATE.md || { echo "FAIL todo homebridge-lib"; exit 1; }
grep -q 'strictValidation' .planning/STATE.md || { echo "FAIL todo strictValidation"; exit 1; }
grep -q 'D-026' .planning/STATE.md || { echo "FAIL todo D-026"; exit 1; }
grep -q 'D-035' .planning/STATE.md || { echo "FAIL todo D-035"; exit 1; }
grep -q 'D-014' .planning/STATE.md || { echo "FAIL Phase 3 anchor D-014"; exit 1; }
grep -qE '^\s*total_phases: 6$' .planning/STATE.md || { echo "FAIL total_phases"; exit 1; }
grep -q 'Last activity: 2026-08-28' .planning/STATE.md || { echo "FAIL STATE activity date"; exit 1; }
grep -qi 'None yet' .planning/STATE.md && { echo "FAIL pending-todo placeholder remains"; exit 1; }

# 4. config.json already carries the seven applied values. Verify only.
for KV in '"runtime": "claude"' \
          '"claude_md_path": "CLAUDE.md"' \
          '"context_window": 1000000' \
          '"branching_strategy": "phase"' \
          '"phase_branch_template": "features/phase-{phase}-{slug}"' \
          '"milestone_branch_template": "features/{milestone}-{slug}"' \
          '"quick_branch_template": "features/quick-{slug}"'; do
  grep -qF "$KV" .planning/config.json || { echo "FAIL config value: $KV"; exit 1; }
done
# config.json must be byte-identical to its pre-planning applied state. The hash below
# was taken after the seven values were set and before this plan ran. Those changes are
# staged-tree-only, so a HEAD comparison would not work here.
echo "7ba0d0db40b0f390058fe802192595d720a200653d1c0677508d1dc0aa2ddfa4  .planning/config.json" \
  | sha256sum -c - || { echo "FAIL config.json was modified"; exit 1; }

# 5. Nothing outside the four planning documents changed on this branch.
git diff --name-only e2208f0..HEAD -- . \
  | grep -vE '^\.planning/(REQUIREMENTS|ROADMAP)\.md$' \
  | grep -vE '^\.planning/quick/260828-bq0-' \
  | grep . && { echo "FAIL unexpected file touched in earlier commits"; exit 1; }
git status --porcelain -- src package.json config.schema.json docs .planning/intel \
  | grep . && { echo "FAIL forbidden path modified"; exit 1; }

echo "PROJECT.md, STATE.md, config.json OK"
    </automated>
    <human-check>Read the Open Proposals entry. Confirm it reads as an unresolved
    proposal with a stated resolution path, not as a decision, and that it does not
    contradict `D-008`.</human-check>
  </verify>
  <done>
`.planning/PROJECT.md` Context defines G-004 and states the release-only gating rule;
Evolution holds an Open Proposals subsection with the backup-battery adapter proposal, its
rationale, and the `D-008` revision path; all five locked decision blocks are unchanged.
`.planning/STATE.md` lists four release gates, four pending todos, updated Phase 3 and
Phase 4 anchors, `total_phases: 6`, and a 2026-08-28 activity line. `.planning/config.json`
is unmodified and holds the seven expected values.
`pre-commit run --files .planning/PROJECT.md .planning/STATE.md` is clean and the change is
committed with a message such as `docs(planning): record release gates and open proposal`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/intel/**` and `docs/research/**` -> tracked planning documents | Sanitized-but-detailed research crosses into files that ship in the public repository. Facts copied across this boundary become public. |
| Working tree -> git history | A committed planning document is permanent. Pre-commit hooks are the only gate. |
| Locked ADR baseline -> editable prose | `<decisions status="locked">` blocks sit in the same file as freely edited Context and Evolution prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-bq0-01 | Information disclosure | Requirement prose copied from `.planning/intel/**` into `REQUIREMENTS.md`, `PROJECT.md`, `STATE.md` | high | mitigate | Copy only public field names, enum values, vendor rule IDs, and documented numeric bounds. Copy no account identifier, `deviceId`, token, endpoint credential, or local-network value. `pre-commit run --files <changed files>` runs trufflehog before every commit in this plan; the checkout is a normal checkout so the git-mode hook works. |
| T-bq0-02 | Tampering | `<decisions status="locked">` blocks in `.planning/PROJECT.md` | medium | mitigate | Task 3 diffs the locked block region against `git show HEAD:.planning/PROJECT.md` and requires an empty diff plus exactly five blocks. Decision 4 is recorded as an open proposal instead of a `D-008` edit. |
| T-bq0-03 | Repudiation | Traceability link between `REQUIREMENTS.md` and `ROADMAP.md` | medium | mitigate | Task 1 asserts a 46-row table whose ID set equals the body ID set. Task 2 asserts set equality of `(ID, phase)` pairs across both files. Silent requirement loss cannot pass. |
| T-bq0-04 | Elevation of privilege | Git branch protection rule from `CLAUDE.md` | medium | mitigate | Task 1 precondition asserts the branch is `features/planning-refinement`. No task creates a branch, and no task passes `--no-verify`. |
| T-bq0-05 | Tampering | Out-of-scope source files (`src/**`, `package.json`, `config.schema.json`, `.planning/intel/**`, `docs/research/**`) | low | mitigate | Task 3 verify asserts a clean `git status` for every forbidden path and asserts `.planning/config.json` is unmodified against HEAD. |

No package-manager install occurs in this plan, so no supply-chain legitimacy checkpoint applies.
</threat_model>

<verification>
Run after all three tasks are committed.

```bash
cd /home/acolomba/homebridge-basement-guardian
SP=/tmp/claude-1000/-home-acolomba-homebridge-basement-guardian/e36e0b97-a9ef-41c9-a0be-bb757d28a7eb/scratchpad
mkdir -p "$SP"

# Full traceability chain: body IDs = table IDs = roadmap pairs, at 46 entries.
grep -oE '^- \[ \] \*\*[A-Z]+-[0-9]+\*\*' .planning/REQUIREMENTS.md \
  | grep -oE '[A-Z]+-[0-9]+' | sort > "$SP/final-body.txt"
grep -oE '^\| [A-Z]+-[0-9]+ \| Phase [0-9]+ \| ' .planning/REQUIREMENTS.md \
  | sed -E 's/^\| ([A-Z]+-[0-9]+) \| (Phase [0-9]+) \| /\1 \2/' | sort > "$SP/final-table.txt"
awk '
  /^### Phase /   { ph=$3; sub(/:.*/,"",ph) }
  /^\*\*Requirements\*\*:/ {
      line=$0; sub(/^\*\*Requirements\*\*:[[:space:]]*/,"",line)
      n=split(line, a, /,[[:space:]]*/)
      for (i=1;i<=n;i++) { gsub(/[[:space:]]/,"",a[i]); if (a[i]!="") print a[i] " Phase " ph }
  }' .planning/ROADMAP.md | sort > "$SP/final-roadmap.txt"

cut -d' ' -f1 "$SP/final-table.txt" | diff - "$SP/final-body.txt"
diff "$SP/final-table.txt" "$SP/final-roadmap.txt"
test "$(wc -l < "$SP/final-table.txt")" -eq 46

# All four gates appear in all three gate-bearing documents.
for F in .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/STATE.md; do
  for G in G-001 G-002 G-003 G-004; do grep -q "$G" "$F" || echo "MISSING $G in $F"; done
done

# No document gates phase completion on a validation gate.
for F in .planning/REQUIREMENTS.md .planning/ROADMAP.md; do
  test "$(grep -v '^[[:space:]]*<!--' "$F" | grep -cE 'After G-0[0-9][0-9]')" -eq 0 || echo "GATE PREFIX in $F"
done

# Only the four intended files changed on this branch, and each has its own commit.
git diff --name-only e2208f0..HEAD -- .
git log --oneline e2208f0..HEAD
```

Expected: three empty diffs, no `MISSING` line, no `GATE PREFIX` line, exactly four
changed files, three commits.
</verification>

<success_criteria>
- `.planning/REQUIREMENTS.md` holds 46 v1 requirements including `DEV-07`, `DEV-08`, and
  `REL-08`, with a 46-row traceability table and a 46/46/0 coverage block.
- `RES-01` and `RES-02` are Phase 3 in both `REQUIREMENTS.md` and `ROADMAP.md`; Phase 5
  keeps `RES-03` and `RES-04` only.
- The `(ID, phase)` pair set from the `ROADMAP.md` phase Requirements lines equals the
  pair set from the `REQUIREMENTS.md` traceability table.
- No requirement text and no roadmap success criterion makes phase completion depend on
  G-001, G-002, G-003, or G-004. `REL-07` and Phase 6 carry all four gates.
- `SAFE-01` and `SAFE-05` use the canonical names **Sump Pit Level** and **Sump Mains
  Power**. `CONF-06` records why `ignoredFaults` enumerates seven slugs.
- `PROJECT.md` defines G-004, states the release-only gating rule, and records the
  backup-battery adapter as an open proposal. Every locked decision block is unchanged.
- `STATE.md` presents four release gates, four pending todos, updated Phase 3 and Phase 4
  anchors, and `total_phases: 6`.
- `.planning/config.json` is verified and unmodified. No file outside the four planning
  documents changed.
- Three commits exist on `features/planning-refinement`, each preceded by a clean
  `pre-commit run --files` invocation. Nothing was committed to `main` and `--no-verify`
  was never used.
</success_criteria>

<output>
Create `.planning/quick/260828-bq0-refine-planning-artifacts-against-ingest/260828-bq0-SUMMARY.md`
when done. Record: the final requirement count, the three added requirement IDs, the added
gate, the `RES-01`/`RES-02` phase move, the open proposal recorded in `PROJECT.md`, and
the three commit SHAs.
</output>
