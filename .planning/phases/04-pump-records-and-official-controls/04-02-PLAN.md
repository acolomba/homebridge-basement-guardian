---
phase: 04-pump-records-and-official-controls
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/accessories/controls.ts
  - src/accessories/alarmMute.ts
  - src/accessories/serviceCatalogue.ts
  - src/accessories/basementGuardian.ts
  - src/cloud/api.ts
  - test/accessories/controls.test.ts
  - test/accessories/alarmMute.test.ts
  - test/accessories/serviceCatalogue.test.ts
  - test/accessories/basementGuardian.test.ts
  - test/cloud/api.test.ts
  - features/support/fakeTimers.ts
  - features/support/world.ts
autonomous: true
requirements: [CTRL-03, CTRL-04, CTRL-05]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every refusal answers the status that describes it: a value other than `true` and a confirmed-offline device answer `NOT_ALLOWED_IN_CURRENT_STATE` (-70412), a duplicate request while the capability already reads active answers `RESOURCE_BUSY` (-70403), a vendor error answers `SERVICE_COMMUNICATION_FAILURE` (-70402), and an exceeded 2.5-second deadline answers `OPERATION_TIMED_OUT` (-70408) (CTRL-05, D-04)."
    - "A locally refused write sends no request at all: the fake vendor records no `PUT` for an off write, a duplicate, a confirmed-offline device, or a capability whose reported field has not decoded (CTRL-03, CTRL-04, D-07, D-031)."
    - "A self-test is permitted while every equipment fault the device can report is active; the binder reads confirmed-offline state and the capability's own reported value and nothing else (CTRL-03, D-018)."
    - "An HTTP 200 carrying `{\"success\": false}` is a refusal, not an acceptance, and answers `SERVICE_COMMUNICATION_FAILURE` exactly as a non-2xx does (CTRL-05, D-04)."
    - "A command is attempted exactly once. A refused, timed-out, or expired request produces no second request (CTRL-05, D-038)."
    - "The 30-second pending window is scoped per capability per accessory: one accessory's pending self-test withholds neither the other accessory's self-test nor mute on the same accessory (CTRL-05, D-05, D-06)."
    - "When the window closes with no confirming report, the row resumes projecting reported state, exactly one warning naming the capability and that the device never confirmed is logged, and nothing is retried (CTRL-05, D-06, D-038)."
    - "`StatusActive` is never used to mark an expired request: it means the reported field did not decode, and one signal never carries two meanings (D-06)."
    - "`Alarm Mute` follows reported `alarm_audio_muted`, publishes from the first update with `StatusActive = false` until that field decodes, and the only body ever sent for it is `{\"alarm_audio_muted\": true}` (CTRL-04, D-03, D-019)."
    - "Every mute constant is named with a `PROVISIONAL_` prefix and lives in one module, so closing `G-001` is a single reviewable edit (CTRL-04, D-16)."
    - "The command request declares a `User-Agent` naming the plugin and nothing else, declares `Accept: application/json`, and carries no credential, account identifier, device identifier, hostname, operating-system detail, or bridge name; read requests are unchanged."
    - "[probe-edge: CTRL-05/adjacency — ASSUMPTION] Expiry and confirmation are both idempotent deletes of the same pending entry. Whichever runs first wins and the second is a no-op, so the row projects reported state in either order and no second warning is logged."
    - "[probe-edge: CTRL-05/ordering — ASSUMPTION] With both capabilities pending on one accessory, the order rows project in is the catalogue's declared order and does not depend on which pending entry was created first."
    - "[probe-edge: CTRL-05/concurrency — ASSUMPTION] The window is per capability per accessory because each accessory owns one binder and each binder holds one map entry per capability; no process-wide or cross-accessory state exists to serialise against."
    - "[probe-edge: CTRL-04/unclassified — ASSUMPTION] Nobody has observed a real Gemini's acknowledgement, reported-state timing, actual duration, or failure behaviour for alarm mute. The plugin therefore ships the mute contract as named provisional constants in one module and makes no claim about any of the four."
  artifacts:
    - path: src/accessories/alarmMute.ts
      provides: "The one place the provisional alarm-mute contract lives, so G-001 closes as a single edit"
      contains: "PROVISIONAL_ALARM_MUTE_REQUESTED_VALUE"
    - path: features/support/fakeTimers.ts
      provides: "A controllable Timers implementation driven from the scenario clock"
      contains: "createFakeTimers"
    - path: src/cloud/api.ts
      provides: "The command request header policy, applied to the body-carrying branch alone"
      contains: "COMMAND_USER_AGENT"
  key_links:
    - from: src/accessories/controls.ts
      to: src/runtime/timers.ts
      via: "the 30-second window is armed and cancelled through the injected port, never a process global"
      pattern: "timers.setTimeout|timers.clearTimeout"
    - from: features/support/world.ts
      to: features/support/fakeTimers.ts
      via: "discoveryContext supplies the controllable timers in place of systemTimers, and advanceClock fires any deadline that has passed"
      pattern: "createFakeTimers"
    - from: src/cloud/api.ts
      to: src/settings.ts
      via: "the User-Agent is built from the one plugin-name constant, so no second product string exists to drift"
      pattern: "PLUGIN_NAME"
  prohibitions:
    - statement: "MUST NOT retry a command for any reason. A command that timed out may already have reached the device, so a retry can operate a real sump pump twice."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT use `StatusActive` to mark an expired or unconfirmed request. It already means the reported field did not decode, and one signal carrying two meanings leaves a user unable to tell which happened."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT send an unmute write, a duration, a timer, or a simulated unmute for alarm mute. The vendor exposes no such command and inventing one would claim a capability the device does not have."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT treat an HTTP 200 carrying `success: false` as acceptance. The body narrows successfully because `success` is a boolean, so the resolved-but-false branch needs its own refusal."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT invent a physical eligibility rule the official client does not have. The plugin refuses on the three rules the official client applies and on undecoded state, and permits a test during equipment faults."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT add the command header policy to the read branch of the shared request builder. Doing so would silently change every read request's wire shape as a side effect."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT put a credential, an account identifier, a device identifier, a hostname, an operating-system detail, or a bridge name into any request header, and MUST NOT present the plugin as the official vendor application."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT extend the header policy to the Auth0 exchange or the AWS IoT SigV4 WebSocket handshake. Both stay out of scope and stay on the open todo."
      status: flagged-unverified
      verification: test
---

<objective>
Complete the command lifecycle the tracer opened: every refusal cause with the status that describes
it, the 30-second pending window and its expiry, the second control Switch, and the request headers
this plugin's first body-carrying request declares.

Purpose: the tracer proved one accepted path. Everything here is the error surface around it — the
part a user actually meets when a pump is unreachable, a test is already running, or the vendor is
slow. Each refusal is a branch, the coverage gate is 100% on branches, and the gate does not run in
CI, so each branch names the case that covers it.
Output: a control binder whose every outcome is decided and asserted, an Alarm Mute Switch whose
unverified contract is named as provisional in one place, and a command request that identifies the
plugin honestly and reveals nothing else.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pump-records-and-official-controls/04-CONTEXT.md
@.planning/phases/04-pump-records-and-official-controls/04-RESEARCH.md
@.planning/phases/04-pump-records-and-official-controls/04-PATTERNS.md
@.planning/phases/04-pump-records-and-official-controls/04-VALIDATION.md
@.planning/phases/04-pump-records-and-official-controls/04-01-SUMMARY.md
@.planning/intel/constraints.md
@src/accessories/controls.ts
@src/accessories/serviceCatalogue.ts
@src/accessories/basementGuardian.ts
@src/device/waterLevel.ts
@src/cloud/api.ts
@src/cloud/types.ts
@src/cloud/errors.ts
@src/settings.ts
@src/runtime/timers.ts
@features/support/world.ts
@features/support/fakeRestApi.ts
@.claude/rules/typescript-style-guide.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md

<interfaces>
<!-- Contracts this plan extends. Established by 04-01; re-read them on disk before extending. -->

```typescript
// src/accessories/controls.ts (from 04-01)
export interface ControlBinderOptions {
  hap: API['hap']; log: Logging; timers: Timers; commands: CommandPort;
  deviceId: string; republish: () => void;
}
export interface ControlBinder {
  readonly pending: ReadonlySet<DeviceCapability>;
  bind(service: Service, capability: DeviceCapability, reported: () => boolean | undefined): void;
  reconcile(capability: DeviceCapability, reported: boolean | undefined): void;
}

// src/runtime/commandPort.ts (from 04-01)
export type CommandFailure = 'vendor-error' | 'timed-out';
export type CommandOutcome = { accepted: true } | { accepted: false; failure: CommandFailure };

// src/runtime/timers.ts (unchanged)
export interface Timers {
  setTimeout(handler: () => void, delayMs: number): unknown;
  setInterval(handler: () => void, delayMs: number): unknown;
  clearTimeout(handle: unknown): void;
  clearInterval(handle: unknown): void;
}

// src/cloud/types.ts (unchanged)
// isCommandResult(value) is `isRecord(value) && typeof value.success === 'boolean'`,
// so a body of { success: false } RESOLVES rather than rejecting.

// src/cloud/api.ts (unchanged shape; the second branch is the command branch)
function requestInit(method: string, body: string | undefined, authorization: string, deadline: AbortSignal): RequestInit;
// body === undefined -> read branch: Authorization only
// otherwise -> command branch: Authorization + Content-Type

// src/settings.ts (unchanged)
export const PLUGIN_NAME = 'homebridge-basement-guardian';

// features/support/world.ts (unchanged shape)
now(): number { return this.scenarioTime; }
advanceClock(milliseconds: number): void { this.scenarioTime += milliseconds; }
private discoveryContext(...): DiscoveryContext   // currently supplies `timers: systemTimers`
```

The per-cause status map this plan implements (`04-CONTEXT.md` D-04), with the values probed off the
pinned package:

```
value other than true (no cancel, no unmute)   NOT_ALLOWED_IN_CURRENT_STATE   -70412
device confirmed offline                        NOT_ALLOWED_IN_CURRENT_STATE   -70412
capability's reported field has not decoded     NOT_ALLOWED_IN_CURRENT_STATE   -70412
duplicate request while already active          RESOURCE_BUSY                  -70403
vendor answered an error, or success: false     SERVICE_COMMUNICATION_FAILURE  -70402
2.5-second deadline exceeded                    OPERATION_TIMED_OUT            -70408
```
</interfaces>
</context>

<assumption_delta_decision>
**Decision: `no-change`.**

The assumption-delta detector's two signals are lexical false positives: `pluralization` matched the
word "second" inside the durations "2.5-second API deadline" and "30-second pending window", and
`chosen` matched "configurable" inside `CTRL-02`'s prohibition against claiming configurable
retention. Neither is an identity-model change, and this phase contains none.
</assumption_delta_decision>

## Artifacts this phase produces

Every symbol Phase 4 creates, across all six plans. Nothing outside this list is a phase artifact.

**New source modules**

| Path | What it holds |
|---|---|
| `src/accessories/controls.ts` | `ControlBinder`, `ControlBinderOptions`, `createControlBinder` |
| `src/accessories/alarmMute.ts` | `PROVISIONAL_ALARM_MUTE_REQUESTED_VALUE` (D-16) |
| `src/accessories/pumpRecords.ts` | `PumpRecords`, `PumpRecordsOptions`, `PumpRecordsObservation`, `PumpRecordValues`, `createPumpRecords` |
| `src/runtime/commandPort.ts` | `CommandPort`, `CommandOutcome`, `CommandFailure` |
| `src/runtime/accessoryStore.ts` | `AccessoryStore` |

**New test and harness files**

`test/accessories/controls.test.ts`, `test/accessories/pumpRecords.test.ts`,
`test/accessories/alarmMute.test.ts`, `test/accessories/hapWriteFidelity.test.ts`,
`test/accessories/hapImportScope.test.ts`, `test/runtime/accessoryStore.test.ts`,
`features/support/fakeTimers.ts`, `features/support/steps/controls.ts`,
`features/officialControls.feature`, `features/pumpRecords.feature`.

**New exported symbols on existing modules**

| Module | Added |
|---|---|
| `src/device/health.ts` | `TrustScope` gains `'self-test'` and `'alarm-mute'` |
| `src/device/family.ts` | `SelfTestState`, `AlarmMuteState`; `ScopedDomainState` gains `'self-test'` and `'alarm-mute'` |
| `src/runtime/accountRuntime.ts` | `AccountRuntime.commands: CommandPort` |
| `src/accessories/serviceCatalogue.ts` | `ProjectionInput.pendingControls`, `.primaryPumpRecord`, `.backupPumpRecord`; `ServiceRow.alwaysPublish` |
| `src/accessories/customCharacteristics.ts` | format union gains `'uint32'`; `ObservationStartedAt`, `ObservedActivationCount`, `LastObservedActivationAt`, `LastActivationWasTestActivity` |
| `src/accessories/basementGuardian.ts` | `BasementGuardianAccessoryOptions.commands`, `.store` |
| `src/platform.ts` | `DiscoveryContext.commands`; `BasementGuardianAccessoryContext` gains `primaryPump`, `backupPump`, `watermarks` |
| `src/persistence/accessoryContext.ts` | `PumpObservation.lastActivationWasTestActivity`; `primaryPump`, `backupPump`, `watermarks` become optional |
| `src/cloud/api.ts` | `COMMAND_USER_AGENT` |
| `features/support/fakeHap.ts` | `FakeHapStatus`, `FakeHapStatusError`, `FakeHap.HAPStatus`, `FakeHap.HapStatusError`, `FakeServiceNamespace.Switch`, `FakeCharacteristicNamespace.On`, `FakeHapCharacteristic.statusCode`/`.onSet`/`.handleSetRequest`/`.handleGetRequest`, `FakeFormats.UINT32` |
| `features/support/fakeRestApi.ts` | `holdNextCommand`, `rejectNextCommand`, `answerNextCommandWith`, `onCommandAccepted` |

**New published HomeKit identities** — two subtypes, `system-self-test` and `alarm-mute`, and four
hard-coded random v4 characteristic UUIDs outside Apple's base namespace.

**New configuration keys** — none. **New packages installed** — none.

<tasks>

<task type="auto" tdd="true">
  <name>Every remaining refusal cause and the status that describes it</name>
  <read_first>
    `src/accessories/controls.ts` exactly as 04-01 left it — the handler, the named rule helpers, the
    clearing push, and the module docblock.
    `test/accessories/controls.test.ts` as 04-01 left it, for the stand-in idiom to extend.
    `src/cloud/types.ts` lines 170-185 (`isCommandResult` accepts `success: false` as a well-formed
    body), `src/cloud/errors.ts` in full (`CloudRequestError` carries a fixed message, a status, and a
    route label and never the body), `src/cloud/api.ts` lines 90-145 (`narrow` and `send`, and where the
    2.5-second deadline is applied), `src/runtime/commandPort.ts` as 04-01 left it.
    `src/accessories/basementGuardian.ts` lines 96-160 and 498-506 — where `offlineConfirmed` is already
    computed for `ProjectionInput` and can be handed to the binder without a second source.
    `04-CONTEXT.md` decisions D-04, D-07, D-18 and D-19 as quoted under "Locked product decisions",
    D-031 and D-038. `04-RESEARCH.md` "Pattern 1", "Pattern 2", "Pitfall 1", "Pitfall 7", "Security
    Domain". `.planning/intel/constraints.md` lines 107-113 (the official client's three eligibility
    rules) and 535-541.
    `.fallowrc.json` — the health limits every new function must stay under.
  </read_first>
  <files>src/accessories/controls.ts, src/accessories/basementGuardian.ts, test/accessories/controls.test.ts, test/accessories/basementGuardian.test.ts</files>
  <behavior>
    - `ControlBinderOptions` gains `offlineConfirmed: () => boolean`, supplied by the accessory from the same count it already uses for `ProjectionInput.offlineConfirmed`, so no second source of that fact exists.
    - `bind`'s `reported` accessor keeps its `boolean | undefined` return; `undefined` means the capability's scope did not decode.
    - The handler evaluates its refusals in a fixed order and stops at the first that applies: value not exactly `true`; reported value `undefined`; confirmed offline; reported value already `true`.
    - A value other than `true` answers `NOT_ALLOWED_IN_CURRENT_STATE`.
    - A reported value of `undefined` answers `NOT_ALLOWED_IN_CURRENT_STATE` and logs one line naming the capability and that the plugin has no fresh state for it.
    - A confirmed-offline device answers `NOT_ALLOWED_IN_CURRENT_STATE`.
    - A reported value already `true` answers `RESOURCE_BUSY`.
    - Every one of those four sends nothing: the injected command port records zero calls.
    - A command port answering `{ accepted: false, failure: 'vendor-error' }` answers `SERVICE_COMMUNICATION_FAILURE`; one answering `{ accepted: false, failure: 'timed-out' }` answers `OPERATION_TIMED_OUT`.
    - The command port answers `'vendor-error'` for a rejected promise and for a resolved body whose `success` is `false`; it answers `'timed-out'` only when the deadline aborted the attempt.
    - Every refusal, local or vendor, arms the clearing push before throwing, and after the recorded deferral runs the characteristic's stored status is `0`.
    - Every vendor refusal removes the capability from `pending` before throwing.
    - No refusal produces a second request: the command port records at most one call per write.
    - Faults are not eligibility: with every reported equipment fault true and `test_running` false, an on write is sent.
    - Every refusal logs one line naming the capability and a cause, and no log line contains a URL, a header value, a token, an account identifier, or a response body.
  </behavior>
  <action>
    Extend `ControlBinderOptions` with `offlineConfirmed` and hand it in from `basementGuardian.ts`,
    reading the same `offlineCount >= offlineThreshold` expression `projectionInputOf` already uses so the
    two cannot disagree; extract that expression into one named local if it would otherwise be written
    twice. Add the four local refusals and the two vendor refusals to the handler, each as its own small
    named function carrying the deciding identifier in the comment above it, keeping every function under
    `.fallowrc.json`'s `maxUnitSize` of 60 and `maxCognitive` of 15 — a single handler body with six
    branches will not pass `npm run fallow`. Order the checks so the cheapest and most local runs first
    and nothing reaches the network that the official Gemini client would have refused.

    State in a comment above the undecoded-state refusal why it exists and why it maps to the same status
    as a confirmed-offline device: without a decoded value the plugin cannot tell a running test from an
    idle one, and issuing a command on that basis would operate a real sump pump on a guess. Cite `D-031`
    and `D-014`.

    State in a comment above the equipment-fault absence that the official Gemini client permits a
    self-test while equipment faults are present and disables the command only when the device is offline,
    so the binder reads confirmed-offline state and the capability's own reported value and nothing else.
    Cite `D-018` and the measured client behaviour.

    In the command port implementation, make the two failure causes distinguishable at the source: an
    abort whose reason is the deadline answers the timed-out cause, and every other rejection plus every
    resolved body whose `success` is not `true` answers the vendor-error cause. `isCommandResult` accepts
    a `success` of `false` as a well-formed body, so the resolved-but-not-accepted case resolves the
    promise and needs its own branch rather than falling out of the rejection path. Give that branch its
    own test.

    Keep every log line to a capability name and a cause. `CloudRequestError` already carries a fixed
    message, a route label, and a status and never the response body; do not add one, do not log the
    constructed URL, and do not log the request headers.
  </action>
  <verify>
    <automated>npm run test:coverage:direct -- dist-test/src/accessories/controls.js dist-test/test/accessories/controls.test.js</automated>
    <fails_when>non-zero exit, or any of lines, branches, or functions below 100% for controls.js in the coverage summary</fails_when>
    <automated>npm run test:coverage:direct -- dist-test/src/accessories/basementGuardian.js dist-test/test/accessories/basementGuardian.test.js</automated>
    <fails_when>non-zero exit, or any of lines, branches, or functions below 100% for basementGuardian.js in the coverage summary</fails_when>
    <automated>npm run fallow</automated>
    <fails_when>non-zero exit, or any dead-code, health, or duplicate finding is printed</fails_when>
  </verify>
  <done>Every way a control write can be refused answers the status that names it, sends nothing it
  should not, and leaves the characteristic readable.</done>
  <acceptance_criteria>
    - A table-driven test enumerates the six causes and asserts the rejection value for each equals the inline literals -70412, -70412, -70412, -70403, -70402, and -70408 in that order.
    - For each of the four local causes, a test asserts the recording command port's call count is `0`.
    - For each of the six causes, a test asserts exactly one deferral was recorded on the injected `Timers` port and that after running it the characteristic's stored status is `0`.
    - For each of the two vendor causes, a test asserts the binder's `pending` set is empty after the rejection.
    - A test builds a snapshot in which every reported equipment fault field is `true` and `test_running` is `false`, writes `true`, and asserts the command port recorded exactly one call.
    - A test resolves the underlying cloud call with a body whose `success` is `false` and asserts the write rejected with -70402 and that exactly one request was attempted.
    - A test aborts the underlying cloud call through the deadline and asserts the write rejected with -70408 and that exactly one request was attempted.
    - A test collects every string passed to the injected log across all six causes and asserts each contains the capability name and that none contains a substring matching the recorded bearer token, the base URL, or the device identifier used in the fixture.
    - `npm run fallow` reports no health finding for `src/accessories/controls.ts`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>The 30-second pending window, its expiry, and the controllable timers a scenario drives it with</name>
  <read_first>
    `src/accessories/controls.ts` as this plan's first task left it.
    `src/runtime/timers.ts` in full — the handle is `unknown` precisely so a stand-in need not produce a
    process timer.
    `features/support/world.ts` lines 200-225 (`now` and `advanceClock`), 490-560 (`discoveryContext` and
    the comment claiming the harness is the one other place the concrete process timers are wired).
    `src/platform.ts` lines 395-460, for the wiring the harness mirrors.
    `04-CONTEXT.md` decisions D-05, D-06, D-10 and the ruling that the window is per capability per
    accessory. `04-RESEARCH.md` "Pattern 5", "Pitfall 5", "Open Question 2". `04-VALIDATION.md`
    "Rulings that change existing rows".
    `.planning/intel/constraints.md` lines 535-541 (the ~16-second observed test duration, which is why
    30 seconds is a window rather than a guess at the test length).
  </read_first>
  <files>src/accessories/controls.ts, test/accessories/controls.test.ts, features/support/fakeTimers.ts, features/support/world.ts</files>
  <behavior>
    - A pending entry is created only after every local refusal has passed and immediately before the command is sent, and it carries the handle of a `timers.setTimeout` armed at 30000 milliseconds.
    - A confirming `reconcile` cancels that handle through `timers.clearTimeout` and deletes the entry.
    - The expiry handler deletes the entry, calls `republish`, and logs exactly one warning naming the capability and that the device never confirmed the request. It issues no request.
    - Reconciling a capability whose entry was already deleted by expiry changes nothing, logs nothing further, and raises nothing. Firing an expiry whose entry was already deleted by reconciliation does the same.
    - Two capabilities pending at once on one binder hold two independent entries with two independent handles; clearing one leaves the other pending.
    - Two binders, standing for two accessories, hold entirely separate pending sets: a capability pending on one is absent from the other's set.
    - After expiry the control row projects reported state again, so a Switch whose request was never confirmed returns to what the device says.
    - `features/support/fakeTimers.ts` exports a `Timers` implementation whose `setTimeout` records a deadline computed from an injected clock reading, whose `clearTimeout` removes it, and which exposes a method that fires every handler whose deadline is at or before the current clock reading, in deadline order.
    - `World.advanceClock` moves the scenario clock and then fires every due handler, so a scenario advancing past 30000 milliseconds observes the expiry without sleeping.
    - `World.discoveryContext` supplies the controllable timers in place of `systemTimers`.
  </behavior>
  <action>
    Hold the pending entries in a `Map` keyed by capability inside the binder's closure, storing the timer
    handle beside each entry, and keep `pending` a live read-only view over that map's keys so the
    accessory's `ProjectionInput` read stays a single expression. State in the comment above the map that
    one binder exists per accessory and one entry exists per capability, so the window is per capability
    per accessory by construction rather than by a rule someone has to remember; cite `D-05` and `D-06`.

    Arm the window with `timers.setTimeout` at a module-level `PENDING_WINDOW_MS` constant of 30000 whose
    docblock names `D-037`. Cancel it with `timers.clearTimeout` on a confirming reconcile. Make both the
    expiry handler and `reconcile` idempotent deletes of the same entry, so whichever runs first wins and
    the other is a no-op — the adjacency case where a confirming report lands at the instant the window
    closes needs no ordering rule beyond that.

    In the expiry handler, log exactly one warning naming the capability and stating that the device never
    confirmed the request, and state in the comment above it that nothing is retried because a command
    that timed out may already have reached the device, and that `StatusActive` is deliberately not used
    to mark this because it already means the reported field did not decode. Cite `D-038` and `D-06`.

    Narrow the expiry's republish to the two control rows rather than a full republish of every published
    row: nothing else changed, and a full republish driven from a timer would make the accessory's
    "two updates cannot interleave" claim harder to hold. Document that narrowing in the binder.

    Write `features/support/fakeTimers.ts` following `src/runtime/timers.ts`'s port shape and
    `features/support/fakeHap.ts`'s convention of justifying every stand-in-only member in a docblock.
    Take the current time from the injected clock rather than from `Date.now`, so the World's own clock is
    the single source. Fire due handlers in deadline order and remove each before invoking it, so a
    handler that arms another timer does not fire it in the same pass. Wire it into
    `World.discoveryContext` in place of `systemTimers` and drive it from `advanceClock`. Update the
    comment above `discoveryContext` that currently claims the harness is the one other place the concrete
    process timers are wired — it is no longer true and must say what the harness now supplies and why.
  </action>
  <verify>
    <automated>npm run test:coverage:direct -- dist-test/src/accessories/controls.js dist-test/test/accessories/controls.test.js</automated>
    <fails_when>non-zero exit, or any of lines, branches, or functions below 100% for controls.js in the coverage summary</fails_when>
    <automated>npm run test:cucumber</automated>
    <fails_when>non-zero exit, or the summary reports any scenario other than passed</fails_when>
  </verify>
  <done>An unconfirmed request withholds the toggle for exactly 30 seconds, then returns the Switch to
  what the device says, once, without retrying anything.</done>
  <acceptance_criteria>
    - A test asserts the deferral armed when a command is sent has `delayMs` equal to the inline literal `30000`.
    - A test confirms a pending request through `reconcile` and asserts the recorded `clearTimeout` call count is `1` and `pending` is empty.
    - A test fires the expiry handler and asserts `pending` is empty, `republish` was called once, the injected log recorded exactly one warning containing the capability name, and the command port's call count is still `1`.
    - A test fires the expiry and then calls `reconcile` for the same capability, and asserts the log recorded no second warning and nothing was raised; a companion test does the same in the opposite order.
    - A test marks both capabilities pending on one binder, reconciles one, and asserts `pending` contains exactly the other.
    - A test builds two binders, marks `'self-test'` pending on the first, and asserts the second's `pending` set is empty and that a write to either capability on the second binder is sent rather than withheld.
    - A test asserts the control row projects the reported boolean for `On` after the expiry handler has run, using the same `ProjectionInput` shape but a pending set that no longer holds the capability.
    - A Cucumber scenario advances the scenario clock past 30000 milliseconds and asserts the Switch reads the reported value and that the fake recorded exactly one `PUT`; the scenario contains no sleep and completes inside the existing 15000 millisecond step timeout.
    - `features/support/world.ts` no longer references `systemTimers` in `discoveryContext`, and its comment states what is wired instead.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>The Alarm Mute Switch, its provisional constants, and the command request header policy</name>
  <read_first>
    `src/device/waterLevel.ts` in full — the exact shape of a one-module home for constants a release
    gate must close in a single reviewable edit, including how the file overview states which gate it
    belongs to and what evidence is missing.
    `src/accessories/serviceCatalogue.ts` as 04-01 left it, especially the `system-self-test` row and its
    `controlValues` helper, which the mute row copies literally.
    `src/cloud/api.ts` lines 60-180, particularly `requestInit`'s two branches and `send`.
    `test/cloud/api.test.ts` around the route-coverage assertion, so the header assertions land beside it.
    `src/settings.ts` in full. `src/accessories/services.ts` lines 20-35 (`'alarm-mute'` is already a
    core kind and cannot be removed by `ignoredFaults`).
    `04-CONTEXT.md` decisions D-03, D-14, D-16, D-19, and the folded todo
    `2026-08-31-define-cloud-request-header-policy.md` as summarised there.
    `.planning/todos/pending/2026-08-31-define-cloud-request-header-policy.md` if it is still on disk.
    `04-RESEARCH.md` "Pitfall 8", "Security Domain", "Assumptions Log" entry A8.
    `.planning/intel/constraints.md` lines 83-113 and 546-554.
  </read_first>
  <files>src/accessories/alarmMute.ts, src/accessories/serviceCatalogue.ts, src/accessories/controls.ts, src/cloud/api.ts, test/accessories/alarmMute.test.ts, test/accessories/serviceCatalogue.test.ts, test/cloud/api.test.ts</files>
  <behavior>
    - `src/accessories/alarmMute.ts` exports exactly one runtime value, `PROVISIONAL_ALARM_MUTE_REQUESTED_VALUE`, equal to `true`, and its file overview names `G-001` and the four behaviours nobody has observed on real hardware: acknowledgement, reported-state timing, actual duration, and failure behaviour.
    - Every named export of that module begins with `PROVISIONAL_`, and no other module in `src/` declares a constant governing alarm-mute behaviour.
    - The binder uses that constant as the only value it will accept for the `'alarm-mute'` capability and as the value it passes to the command port, so a single edit changes both when `G-001` closes.
    - One new catalogue row, declared immediately after the `system-self-test` row: `kind` `'alarm-mute'`, `displayName` `Alarm Mute`, `scope` `'alarm-mute'`, `toleratedDistrust` `[]`, `serviceClass` `hap.Service.Switch`, `alwaysPublish` `true`, projecting `On` from the trusted `'alarm-mute'` group's `muted` field through the same helper the self-test row uses.
    - The `Alarm Mute` service publishes on the first update even when `alarm_audio_muted` has never decoded, and carries `StatusActive = false` until it does.
    - A write of `false` to `Alarm Mute` sends nothing and answers `NOT_ALLOWED_IN_CURRENT_STATE`; a write of `true` while `alarm_audio_muted` already reads `true` sends nothing and answers `RESOURCE_BUSY`.
    - No duration, timer, schedule, or simulated unmute exists anywhere in the mute path.
    - `src/cloud/api.ts` exports `COMMAND_USER_AGENT`, built from `PLUGIN_NAME` so exactly one product string exists in the codebase.
    - `requestInit`'s body-carrying branch declares `User-Agent` equal to `COMMAND_USER_AGENT` and `Accept: application/json` beside the existing `Authorization` and `Content-Type`. The read branch is byte-for-byte unchanged.
    - `COMMAND_USER_AGENT` contains no version, no hostname, no operating-system detail, no bridge name, no account identifier, no device identifier, and no vendor product name.
    - The Auth0 exchange and the AWS IoT SigV4 WebSocket handshake gain no header of any kind.
  </behavior>
  <action>
    Create `src/accessories/alarmMute.ts` modelled on `src/device/waterLevel.ts`: a file overview stating
    that self-test has real hardware evidence behind its wire shape and alarm mute has none, naming the
    four behaviours `G-001` must confirm, and stating that closing that gate is one reviewable edit to
    this module. Export one constant, `PROVISIONAL_ALARM_MUTE_REQUESTED_VALUE`, equal to `true`, with a
    docblock naming it as the only value the plugin will ever send for this capability and citing `D-019`
    and `CTRL-04`. Consume it in `src/accessories/controls.ts` at both the value check and the send, so
    one edit moves both.

    Add the `alarm-mute` catalogue row using the literal shape of the `system-self-test` row and the same
    `controlValues` helper, parameterised by scope, field, and capability, so the two rows share one
    projection function rather than two near-identical ones — `fallow dupes` runs with
    `--fail-on-issues` at a zero baseline and a second copy would fail `npm run check`.

    In `src/cloud/api.ts`, add `COMMAND_USER_AGENT` built from the `PLUGIN_NAME` constant in
    `src/settings.ts` and nothing else. Record the choice and its reason in the constant's docblock: no
    runtime version source exists in `src/`, `package.json` is imported nowhere at runtime, and adding an
    import of it would create a packaging dependency and a drift risk in exchange for a fact the vendor
    cannot use. A bare, honest product identifier reveals nothing about the installation and cannot drift.
    Declare it and `Accept: application/json` on the body-carrying branch of `requestInit` alone. State in
    the comment above that branch that adding either header to the read branch would silently change every
    read request's wire shape, which is a decision rather than a side effect. Cite the header policy's
    prohibition on credentials, account and device identifiers, hostnames, operating-system details, and
    bridge names, and its prohibition on presenting the plugin as the official vendor application. Change
    nothing about the Auth0 exchange or the WebSocket handshake.
  </action>
  <verify>
    <automated>npm run test:coverage:direct -- dist-test/src/accessories/alarmMute.js dist-test/test/accessories/alarmMute.test.js</automated>
    <fails_when>non-zero exit, or any of lines, branches, or functions below 100% for alarmMute.js in the coverage summary</fails_when>
    <automated>npm run test:coverage:direct -- dist-test/src/accessories/serviceCatalogue.js dist-test/test/accessories/serviceCatalogue.test.js</automated>
    <fails_when>non-zero exit, or any of lines, branches, or functions below 100% for serviceCatalogue.js in the coverage summary</fails_when>
    <automated>npm run test:coverage:direct -- dist-test/src/cloud/api.js dist-test/test/cloud/api.test.js</automated>
    <fails_when>non-zero exit, or any of lines, branches, or functions below 100% for api.js in the coverage summary</fails_when>
  </verify>
  <done>Both official controls are published and writable on the vendor's terms, the mute contract is
  named as unverified in one place, and the first request this plugin sends with a body identifies it
  honestly and reveals nothing else.</done>
  <acceptance_criteria>
    - A test imports the compiled `alarmMute` module, enumerates `Object.keys` of the module namespace, and asserts every key starts with the inline literal `PROVISIONAL_`.
    - A test asserts `PROVISIONAL_ALARM_MUTE_REQUESTED_VALUE` is strictly `true`.
    - A test enumerates every `.ts` file under `src/` other than `src/accessories/alarmMute.ts` and asserts none of them declares an identifier whose name contains both `ALARM` and `MUTE` in upper case.
    - A test constructs the catalogue and asserts the `alarm-mute` row has `subtype` equal to `'alarm-mute'`, `scope` equal to `'alarm-mute'`, `alwaysPublish` `true`, and `serviceClass.UUID` equal to `hap.Service.Switch.UUID`.
    - A test drives an update whose `alarm_audio_muted` is not a boolean and asserts an `Alarm Mute` service was published with `On` never pushed and `StatusActive` pushed as `false`.
    - A test writes `false` to `Alarm Mute` and asserts the rejection value is -70412 and the command port call count is `0`; a companion test writes `true` while the reported value is `true` and asserts -70403 and a call count of `0`.
    - A test asserts the catalogue exposes no characteristic, row, or display name whose text contains `Duration`, `Timer`, `Schedule`, or `Unmute`.
    - A test captures the `RequestInit` the command path builds and asserts its `headers` deep-equals an inline object with exactly the four keys `Authorization`, `Content-Type`, `User-Agent`, and `Accept`.
    - A test captures the `RequestInit` a read path builds and asserts its `headers` deep-equals an inline object with exactly the one key `Authorization`.
    - A test asserts `COMMAND_USER_AGENT` strictly equals `PLUGIN_NAME` and contains no `/` character.
    - A test asserts the value of `User-Agent` on the captured command request contains none of the fixture's bearer token, base URL host, device identifier, or account identifier.
  </acceptance_criteria>
  <reversibility rating="costly">
    The `Alarm Mute` Switch is published under a fixed subtype from the first release, so un-publishing it
    later would orphan anything a user attached to it. The `User-Agent` string is reversible; the decision
    to declare one at all is what the folded todo asked for.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Paired HomeKit controller -> `onSet` handler | Every refusal cause crosses it, including the ones that stop a request reaching a real pump. |
| Plugin -> vendor REST command route | The first request carrying a body, and the first carrying an identifying header. |
| Scenario clock -> the accessory's deferred work | A harness that drove real process timers would make the window untestable and the assertion meaningless. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Denial of Service (against a real sump pump, through a repeated or retried command) | `src/accessories/controls.ts` | high | mitigate | Local refusals reject a duplicate and an unreachable device before any request leaves; the pending window means at most one in-flight request per capability per accessory; nothing retries on any path. Pinned by the call-count assertion on all six causes. |
| T-04-08 | Spoofing (of device state, by acting on undecoded telemetry) | the binder's eligibility rules | high | mitigate | A capability whose reported field did not decode refuses rather than guessing. Pinned by the undecoded-state refusal case. |
| T-04-09 | Information Disclosure (identifying the installation to the vendor) | `requestInit`'s command branch | medium | mitigate | The `User-Agent` is the plugin name alone, built from the single `PLUGIN_NAME` constant; the header assertion pins the exact key set and a companion assertion pins that no fixture identifier appears in the value. |
| T-04-10 | Information Disclosure (a refusal log quoting a vendor response or a URL) | the binder's log lines | medium | mitigate | Log lines carry a capability name and a cause; `CloudRequestError` already carries a fixed message plus a route label and a status. Pinned by the log-content assertion across all six causes. |
| T-04-11 | Tampering (a mute contract presented as verified) | `src/accessories/alarmMute.ts` | medium | mitigate | Every export is `PROVISIONAL_`-named, the module overview names `G-001` and the four unobserved behaviours, and a test asserts no other module in `src/` declares a mute constant. |
| T-04-12 | Denial of Service (a pending window that never closes) | the pending map | medium | mitigate | Every entry is created with an armed 30000 millisecond deadline and both the expiry and the confirmation delete it idempotently, so no path leaves an entry without a way out. |
| T-04-SC | Tampering (supply chain) | npm installs | high | mitigate | This plan installs no package. `04-RESEARCH.md` § "Package Legitimacy Audit" records zero packages added across the phase. |
</threat_model>

<verification>
All four focused source-test pairs reach 100% direct line, branch, and function coverage; the Cucumber
run is green; `npm run check` passes. For the three safety-bearing claims in this plan — that a local
refusal sends nothing, that nothing is retried, and that the pending window isolates per capability
per accessory in both directions — reintroduce the defect and watch the specific test fail before
accepting the green run.
</verification>

<success_criteria>
Every way a control write can end is decided, named, and asserted; both official controls exist and
refuse exactly what the official client refuses; an unconfirmed request returns the Switch to what the
device says on its own clock without retrying; and the plugin's first body-carrying request identifies
itself honestly and says nothing else about the installation.
</success_criteria>

<output>
Create `.planning/phases/04-pump-records-and-official-controls/04-02-SUMMARY.md` when done.
Record the header policy chosen and the reason, and record for each safety-bearing claim the defect
that was reintroduced and the exact test that was watched to fail.
When committing, run `pre-commit run --files <changed files>` until clean first; then `git add` the
paths and run a plain `git commit -m <message>` with no pathspec, and confirm with
`git show --name-only --format="" HEAD` that the commit is not empty.
</output>
</content>
</invoke>
