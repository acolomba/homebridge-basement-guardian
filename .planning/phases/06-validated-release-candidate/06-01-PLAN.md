---
phase: 06-validated-release-candidate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - LICENSE
  - NOTICE
  - test/packaging/licenseHeaders.test.ts
  - test/packedArtifact.test.ts
  - .github/workflows/build.yml
  - .github/workflows/package-audit.yml
  - .github/workflows/publish.yml
autonomous: true
requirements: [REL-01, REL-02, REL-04, REL-05, REL-07]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "package.json declares private: false and license: \"SEE LICENSE IN LICENSE\", and `npm pack --dry-run --json` lists NOTICE among the root files (REL-05, D-01, D-035)"
    - "The CI compatibility matrix in .github/workflows/build.yml crosses node-version [22.x, 24.x] with homebridge-version ['1.8.0', '1.11.4', '2.4.0'], with fail-fast: false, so the min-supported, latest-1.x, and current-2.x boundary points D-02 names are each exercised (REL-01) — resolved boundary edge"
    - { statement: "A CI matrix-cell interruption or failure cannot corrupt another cell's result, because fail-fast: false already keeps every cell an independent GitHub Actions job with no shared state (REL-02)", verification: backstop }
    - "package-audit.yml runs a real (non-dry-run) npm pack, extracts the tarball, and scans it with trufflehog filesystem mode at --results=verified,unknown --fail, as its own isolated CI job separate from the compat matrix, so an interruption in one cannot affect the other (REL-04) — resolved concurrency edge"
    - "publish.yml triggers only on workflow_dispatch and publishes with npm publish --tag next; no push or pull_request trigger exists, so nothing in this phase's own execution or a normal merge can run npm publish (REL-07, D-01, D-026) — resolved adjacency edge"
    - { statement: "If secrets.NPM_TOKEN is unset when publish.yml is manually dispatched, npm publish fails closed on an authentication error rather than silently publishing (REL-07)", verification: backstop }
    - "test/packaging/licenseHeaders.test.ts's read-only file assertions carry no shared mutable state, so they are safe to run alongside any other node:test file in the same process (REL-05) — resolved concurrency edge"
    - { statement: "REL-01's precision edge ('where can precision loss, overflow, or rounding/tie-breaking occur') does not meaningfully apply — REL-01 is a version-range compatibility check with no floating-point arithmetic; the only numeric-like comparisons are SemVer range checks on integer version components — left unresolved rather than force-fit", status: unresolved, flagged: true }
  artifacts:
    - NOTICE
    - test/packaging/licenseHeaders.test.ts
    - .github/workflows/package-audit.yml
    - .github/workflows/publish.yml
  prohibitions:
    - { statement: "MUST NOT trigger .github/workflows/publish.yml automatically on push or pull_request — it must stay workflow_dispatch-only (D-01)", status: unverified, flagged: true }
    - { statement: "MUST NOT mark a CI matrix cell continue-on-error: true or otherwise suppress a genuine compat-matrix failure to make an unsupported Homebridge/Node combination appear supported (REL-01)", status: unverified, flagged: true }
  key_links:
    - "package.json files[] -> npm pack output -> test/packedArtifact.test.ts ALLOWED_ROOT_FILES"
    - "build.yml matrix.homebridge-version -> npm install --no-save homebridge@<version> -> npm run check"
    - "package-audit.yml real npm pack -> extracted tarball -> trufflehog filesystem scan"
---

<objective>
Wire the license boundary, the CI compatibility matrix, the packed-artifact secret scan, and the
publish workflow scaffold end to end: a manifest that declares the correct license and privacy
metadata, an artifact (LICENSE/NOTICE) that backs it up, a test gate that enforces it, a CI matrix
that runs the whole suite against the declared compatibility range, a separate CI job that scans
the real packed tarball for secrets, and a publish workflow that exists but never fires on its own.

Purpose: Phase 6 stops at "ready to publish" (D-01) — this plan builds the mechanical spine every
other plan in this phase (SPDX headers, dependency audits, docs, real-pump suite) sits on top of.
Output: package.json/LICENSE/NOTICE updated, a new test/packaging/licenseHeaders.test.ts gate, the
Homebridge-version CI matrix dimension, and two new workflows (package-audit.yml, publish.yml).
</objective>

<execution_context>
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-validated-release-candidate/06-CONTEXT.md
@.planning/phases/06-validated-release-candidate/06-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: License boundary end to end — manifest, artifact, test gate, CI matrix</name>
  <files>package.json, LICENSE, NOTICE, test/packaging/licenseHeaders.test.ts, test/packedArtifact.test.ts, .github/workflows/build.yml</files>
  <read_first>package.json, LICENSE, test/packedArtifact.test.ts, test/packageManifest.test.ts, .github/workflows/build.yml</read_first>
  <action>
    In package.json: set "private" to false (was true, D-01); set "license" to "SEE LICENSE IN LICENSE" (was "Apache-2.0", D-035); add "NOTICE" to the "files" array, which currently reads ["dist", "config.schema.json", "CHANGELOG.md"].

    Create NOTICE at the repository root. State that this project is based on the Homebridge
    plugin template (https://github.com/homebridge/homebridge-plugin-template), which is licensed
    under the Apache License, Version 2.0, and identify the copyright/attribution notices carried
    forward from it (D-035's "identify the template and its Apache license in the public
    third-party notices" clause). Name the three files that remain Apache-2.0 licensed as modified
    template material: src/index.ts, src/settings.ts, src/platform.ts. State that every other file
    under src/ is original work licensed under the MIT License. Close with a pointer to LICENSE for
    the complete text of both licenses.

    Rewrite LICENSE: keep the existing full Apache License 2.0 text (lines 1-176) byte-identical.
    Prepend, before that text, a short prose section (3-5 sentences) explaining the file-level
    licensing boundary in the same terms as NOTICE — which three files stay Apache-2.0 (modified
    template material, per Apache section 4's "mark changed files" requirement) and that every
    other file is original MIT-licensed work. Append, after the Apache text, the complete standard
    MIT License text (the "MIT License" heading, a "Copyright (c) 2026 Alessandro Colomba" line,
    and the standard permission/warranty paragraphs verbatim from the canonical MIT template).

    Create test/packaging/licenseHeaders.test.ts. Copy the imports and REPOSITORY_ROOT resolution
    pattern from test/packageManifest.test.ts (two ".." segments — this file sits at the same
    depth under dist-test/test/packaging/). Add two node:test cases: one asserting LICENSE's raw
    text matches both /Apache License/ and /MIT License/ (REL-05); one asserting NOTICE exists and
    its raw text matches /homebridge-plugin-template/ and /Apache/ (REL-05, D-035).

    In test/packedArtifact.test.ts, update the ALLOWED_ROOT_FILES constant to insert 'NOTICE' in
    correct ASCII sort order: ['CHANGELOG.md', 'LICENSE', 'NOTICE', 'README.md',
    'config.schema.json', 'package.json'] (uppercase letters sort before lowercase in the existing
    deepStrictEqual comparison, so 'NOTICE' belongs between 'LICENSE' and 'README.md').

    In .github/workflows/build.yml, add a second matrix dimension alongside the existing
    node-version array: homebridge-version: ['1.8.0', '1.11.4', '2.4.0'] (D-02's three pinned
    points — minimum-supported 1.8.x, latest 1.x, current 2.x). Add a one-line comment above it in
    the same voice as the existing node-version comment, citing D-02/D-033 and noting that the
    1.8.0 cell prints a benign EBADENGINE warning during install because Homebridge 1.8.0's own
    engines.node is ^18.15.0 || ^20.7.0 and this repo sets no engine-strict — the warning is
    expected and does not fail the job. Add a new step, "Pin matrix-selected Homebridge version",
    running `npm install --no-save homebridge@${{ matrix.homebridge-version }}`, placed
    immediately after the existing "Install dependencies" step and before "Lint the project"; the
    --no-save flag is load-bearing, it keeps package-lock.json's own ^2.4.0 pin untouched by CI.
  </action>
  <verify>
    <automated>O=$(npm run build:test 2>&1 && node --test dist-test/test/packaging/licenseHeaders.test.js dist-test/test/packedArtifact.test.js dist-test/test/packageManifest.test.js 2>&1); R=$?; test $R -eq 0 && printf '%s\n' "$O" | grep -qE '^ℹ fail 0$'</automated>
    <fails_when>non-zero exit, or the output has no line reading exactly "ℹ fail 0" (any of the three test files failing, or the build itself failing)</fails_when>
    <automated>O=$(pre-commit run check-yaml --files .github/workflows/build.yml 2>&1 && pre-commit run check-json --files package.json 2>&1); R=$?; test $R -eq 0</automated>
    <fails_when>non-zero exit, or a hook line prints "Failed"</fails_when>
  </verify>
  <done>package.json's private/license/files fields match the action; LICENSE contains both full license texts; NOTICE exists and names the template; test/packedArtifact.test.ts's ALLOWED_ROOT_FILES includes NOTICE in the stated order; build.yml's matrix carries the three pinned Homebridge versions with the pinning step wired before lint.</done>
</task>

<task type="auto">
  <name>Task 2: Packed-artifact content secret scan (REL-04)</name>
  <files>.github/workflows/package-audit.yml</files>
  <read_first>.github/workflows/build.yml, .pre-commit-config.yaml, CLAUDE.md</read_first>
  <action>
    Create .github/workflows/package-audit.yml. Trigger it the same way build.yml triggers (push
    to main, pull_request to main, workflow_dispatch) — copy that `on:` block's structure. Define
    one job, package-audit, running on ubuntu-latest with no matrix (packaging content does not
    vary by Homebridge version). Steps, in order: actions/checkout@v5; actions/setup-node@v6 with
    node-version 24.x; `npm install`; `npm run build`; a "Pack the plugin" step running `npm pack`
    (the real, non-dry-run form, which writes a .tgz file to the working directory); an "Extract
    the packed artifact" step that makes a scratch directory and extracts the produced tarball into
    it (e.g. `mkdir -p /tmp/pack && tar -xzf homebridge-basement-guardian-*.tgz -C /tmp/pack`); an
    "Install trufflehog" step running the pinned install script, matching the same v3.92.4 revision
    .pre-commit-config.yaml already pins for the trufflehog hook: `curl -sSfL
    https://raw.githubusercontent.com/trufflesecurity/trufflehog/main/scripts/install.sh | sh -s --
    -b /usr/local/bin v3.92.4`; a final "Scan the packed artifact for secrets" step running
    `trufflehog filesystem /tmp/pack --results=verified,unknown --fail` (REL-04) — this is the same
    filesystem-scan technique CLAUDE.md documents for a worktree commit, applied here to the real
    packed tarball's contents rather than the git tree, because npm pack --dry-run --json (which
    test/packedArtifact.test.ts already uses) lists paths only and never writes real tarball bytes
    to scan.
  </action>
  <verify>
    <automated>pre-commit run check-yaml --files .github/workflows/package-audit.yml</automated>
    <fails_when>non-zero exit, or a hook line prints "Failed"</fails_when>
    <automated>O=$(npm run build 2>&1 && npm pack --dry-run --json 2>&1); R=$?; test $R -eq 0</automated>
    <fails_when>non-zero exit (proves the pack step this workflow performs for real is buildable locally; the trufflehog install/scan steps only run in CI and are not exercised by this local check)</fails_when>
  </verify>
  <done>.github/workflows/package-audit.yml exists, is valid YAML, defines one non-matrixed job that performs a real npm pack, extracts it, and runs a pinned trufflehog filesystem scan with --results=verified,unknown --fail against the extraction directory.</done>
</task>

<task type="auto">
  <name>Task 3: Publish workflow scaffold, never auto-triggered (REL-07, D-01)</name>
  <files>.github/workflows/publish.yml</files>
  <read_first>.github/workflows/build.yml</read_first>
  <action>
    Create .github/workflows/publish.yml, reusing build.yml's checkout/setup-node steps and inline
    comment style. The `on:` block contains only `workflow_dispatch:` — no push and no
    pull_request trigger, under any name, anywhere in the file. Add a comment above the trigger
    block stating that this is the only publish mechanism this phase wires and that D-01 requires
    it never fire automatically; the maintainer runs it deliberately after this phase closes. One
    job, publish, on ubuntu-latest: actions/checkout@v5; actions/setup-node@v6 with node-version
    24.x and registry-url: 'https://registry.npmjs.org'; `npm install`; `npm run build`; a final
    step running `npm publish --tag next` (D-026's staged-prerelease dist-tag) with `env:
    NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}` — a classic token, not OIDC Trusted Publishing,
    because a package with no prior registry publish cannot yet be configured for Trusted
    Publishing (RESEARCH.md Assumption A1); document that as a follow-up hardening step in a
    comment rather than implementing it now.
  </action>
  <verify>
    <automated>pre-commit run check-yaml --files .github/workflows/publish.yml</automated>
    <fails_when>non-zero exit, or a hook line prints "Failed"</fails_when>
    <automated>O=$(grep -cE '^\s*(push|pull_request):' .github/workflows/publish.yml || true); test "$O" -eq 0</automated>
    <fails_when>the count is not exactly 0, meaning a push or pull_request trigger key exists in the file</fails_when>
  </verify>
  <done>publish.yml exists, its "on:" block contains only workflow_dispatch, and its one job publishes with npm publish --tag next gated behind secrets.NPM_TOKEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> npm registry | publish.yml's authenticated `npm publish` call |
| CI runner -> packed tarball | package-audit.yml extracts and scans the real npm pack output |
| Any committer -> CI trigger surface | anyone who can push to main or open a PR can cause build.yml/package-audit.yml to run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | .github/workflows/publish.yml | high | mitigate | workflow_dispatch-only trigger with no push/pull_request key; verified structurally by Task 3's grep check |
| T-06-02 | Tampering | .github/workflows/build.yml matrix (EBADENGINE warning) | low | accept | No engine-strict is set; the warning is a metadata fact about the pinned homebridge@1.8.0 package, not a defect in this project's own engines declaration; documented in a workflow comment rather than suppressed |
| T-06-03 | Information Disclosure | .github/workflows/package-audit.yml packed tarball | high | mitigate | Real (non-dry-run) npm pack extracted and scanned with trufflehog filesystem --results=verified,unknown --fail, on top of the existing path allowlist in test/packedArtifact.test.ts |
| T-06-04 | Repudiation | .github/workflows/publish.yml manual dispatch | low | accept | GitHub Actions workflow_dispatch runs are attributed to the authenticated actor who triggered them and logged in the Actions run history; no additional audit trail is added by this phase |

</threat_model>

<verification>
- `npm run build:test && node --test dist-test/test/packaging/licenseHeaders.test.js dist-test/test/packedArtifact.test.js dist-test/test/packageManifest.test.js` passes.
- `npm run build && npm pack --dry-run --json` succeeds and its file list includes NOTICE.
- `pre-commit run check-yaml --files .github/workflows/build.yml .github/workflows/package-audit.yml .github/workflows/publish.yml` passes.
- No `push:` or `pull_request:` key exists in publish.yml.
</verification>

<success_criteria>
package.json is publish-ready per D-01/D-035; the CI matrix exercises all three pinned Homebridge
versions across both Node versions; a real packed-tarball secret scan runs as its own CI job; a
publish workflow exists and cannot fire on its own.
</success_criteria>

<output>
Create `.planning/phases/06-validated-release-candidate/06-01-SUMMARY.md` when done
</output>
