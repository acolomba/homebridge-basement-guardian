---
phase: 06-validated-release-candidate
plan: 08
type: execute
wave: 2
depends_on: ["06-07"]
files_modified:
  - features/real-pump/heartbeats.feature
  - features/real-pump/lifecycle.feature
autonomous: true
requirements: [REL-09]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No heartbeats.feature or lifecycle.feature scenario requires a natural status change (a live pump run, a fault, a power event) to pass — REL-09/D-04's explicit constraint (REL-09)"
    - "The restart scenario reuses the same scratch storagePath directory across a stop/start pair within one scenario, proving the real Auth0 token cache is reused rather than granting a fresh token on every restart (AUTH-01, carried forward)"
    - "The shutdown scenario asserts no unhandled rejection was recorded across the run, reusing the same process.on('unhandledRejection', ...) recording pattern features/support/world.ts already establishes (SYNC-05, carried forward)"
    - "The real-pump suite targets exactly one real Gemini system per D-04/CONTEXT.md, so no multi-device ordering question arises for this phase; a future multi-device real-pump extension would need to revisit heartbeat/update ordering across devices — flagged rather than resolved, since the account could in principle gain a second device later"
  artifacts:
    - features/real-pump/heartbeats.feature
    - features/real-pump/lifecycle.feature
  prohibitions:
    - { statement: "MUST NOT make any assertion in heartbeats.feature or lifecycle.feature fail solely because the real device did not change state during the run (REL-09, D-04)", status: unverified, flagged: true }
  key_links:
    - "features/real-pump/support/realWorld.ts start()/stop() -> lifecycle.feature restart/shutdown scenarios"
---

<objective>
Complete the REL-09 real-pump suite: heartbeat and natural-update observation, restart with real
token-cache reuse, and clean shutdown with no unhandled rejection.

Purpose: this plan builds the second half of D-04's named scope on top of 06-07's harness and
discovery scenarios. Every scenario here is observational and time-bounded rather than
change-dependent, per REL-09's explicit "never requires a natural status change to pass."
Output: features/real-pump/heartbeats.feature and lifecycle.feature, both tagged @real @read-only,
both dry-run clean against the harness 06-07 built.
</objective>

<execution_context>
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-validated-release-candidate/06-RESEARCH.md
@features/real-pump/support/realWorld.ts
@features/support/world.ts
@features/CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Heartbeat and natural-update observation</name>
  <files>features/real-pump/heartbeats.feature</files>
  <read_first>features/real-pump/support/realWorld.ts, README.md (the "~898 seconds" heartbeat cadence, the "When the plugin cannot vouch for a value" section)</read_first>
  <action>
    Create features/real-pump/heartbeats.feature, tagged @real @read-only at the Feature level.
    Write a scenario that starts the harness and waits, up to a bounded timeout long enough to
    plausibly span one vendor heartbeat interval (README.md documents the device sends a message
    about every 898 seconds — choose and state a timeout that can realistically observe at least
    one heartbeat without making the suite impractically slow to run; document the tradeoff in a
    comment above the step), for either the connection to stay open with no error/close event, or
    for at least one additional store notification to arrive. Phrase the assertion so both
    outcomes ("the connection stayed open and healthy" and "at least one update arrived") count as
    pass, and only an actual connection error, close, or unhandled rejection during the wait
    counts as fail — a quiet-but-healthy connection is success, not an unobserved case. Add a
    second scenario documenting, in its own words, that a natural value change (if one occurs
    during the run) is reflected in the store without any step forcing it — assert only that a
    later snapshot() read differs from an earlier one when a change was recorded, guarded by an
    observations()-based conditional step, never by a hard requirement that a change occurs.
  </action>
  <verify>
    <automated>O=$(npm run build:test 2>&1 && npx cucumber-js --profile real --dry-run 2>&1); R=$?; test $R -eq 0</automated>
    <fails_when>non-zero exit, or the dry run reports an undefined/ambiguous step across the whole real profile (this file plus discovery.feature)</fails_when>
  </verify>
  <done>features/real-pump/heartbeats.feature exists, tagged @real @read-only, and every scenario passes on a quiet-but-healthy connection as well as on an observed natural change.</done>
</task>

<task type="auto">
  <name>Task 2: Restart with token-cache reuse, and clean shutdown</name>
  <files>features/real-pump/lifecycle.feature</files>
  <read_first>features/real-pump/support/realWorld.ts, src/cloud/auth.ts (TOKEN_CACHE_FILENAME, the cache-reuse condition in readCachedToken), features/degradedOperation.feature (restart scenario phrasing precedent)</read_first>
  <action>
    Create features/real-pump/lifecycle.feature, tagged @real @read-only at the Feature level.
    Write a restart scenario: start the harness, stop it, then start a second runtime against the
    SAME scratch storagePath directory (the World must expose a restart-style method that stops
    the current runtime and builds a fresh one over the same directory, rather than tearing the
    directory down between the two) — assert the second start does not require a fresh Auth0 grant
    when the first grant is still within its renewal margin, by asserting the cached-token file
    (TOKEN_CACHE_FILENAME under the scratch directory) has the same modification time or content
    hash before and after the second start's completion (a fresh grant would rewrite it). Write a
    shutdown scenario: start the harness, let it run briefly, stop it, and assert no unhandled
    rejection was recorded — reuse the same process.on('unhandledRejection', ...) recording
    pattern features/support/world.ts's constructor already establishes, adapted into
    realWorld.ts if 06-07 did not already add it (check realWorld.ts's read_first result before
    duplicating the hook).
  </action>
  <verify>
    <automated>O=$(npm run build:test 2>&1 && npx cucumber-js --profile real --dry-run 2>&1); R=$?; test $R -eq 0</automated>
    <fails_when>non-zero exit, or the dry run reports an undefined/ambiguous step across the whole real profile</fails_when>
  </verify>
  <done>features/real-pump/lifecycle.feature exists, tagged @real @read-only, with a restart scenario proving token-cache reuse and a shutdown scenario proving no unhandled rejection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| features/real-pump/ restart scenario <-> the real token cache | The one place this phase writes a real bearer token to disk, even briefly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-19 | Information Disclosure | real token-cache file under the scratch directory | high | mitigate | Written only under a per-scenario mkdtemp directory 06-07's After hook removes; never a committed or fixed path |
| T-06-20 | Denial of Service | heartbeats.feature's bounded wait | low | accept | A generous but finite timeout is chosen and documented; a hung wait fails the scenario rather than the CI job, and this suite never runs in CI regardless |

</threat_model>

<verification>
- `npx cucumber-js --profile real --dry-run` resolves every step across all of features/real-pump/.
</verification>

<success_criteria>
The full REL-09 scope (discovery, initial state, heartbeats, natural updates, restart, shutdown)
is built and dry-run clean; no scenario requires a natural status change to pass.
</success_criteria>

<output>
Create `.planning/phases/06-validated-release-candidate/06-08-SUMMARY.md` when done
</output>
