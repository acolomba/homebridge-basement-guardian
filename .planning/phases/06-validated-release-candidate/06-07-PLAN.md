---
phase: 06-validated-release-candidate
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - cucumber.json
  - features/real-pump/support/realWorld.ts
  - features/real-pump/discovery.feature
  - test/realPumpCommandBlock.test.ts
autonomous: true
requirements: [REL-09]
user_setup:
  - service: basement-guardian-vendor-account
    why: "Running the opt-in real-pump suite against a live account (never required for npm test or CI)"
    env_vars:
      - name: BG_EMAIL
        source: "The maintainer's own Basement Guardian account email, same value used in dev/hb's .env"
      - name: BG_PASSWORD
        source: "The maintainer's own Basement Guardian account password, same value used in dev/hb's .env"

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "cucumber.json's \"default\" profile carries \"tags\": \"not @real\", so creating features/real-pump/**/*.feature cannot make npm test or CI silently pick them up (REL-09)"
    - "A scenario tagged @real without @read-only, or @read-only without @real, matches neither the default profile's \"not @real\" filter's intended exclusion boundary nor the real profile's \"@real and @read-only\" filter — every features/real-pump/ scenario carries both tags together at the Feature level, so no scenario can run unblocked under either profile (REL-09) — resolved adjacency edge"
    - "No file under features/real-pump/ contains the substring \".commands\", \"CommandPort\", or \"sendCommand\" — proven by a source-text gate mirroring test/accessories/hapImportScope.test.ts's static-gate pattern, so command-blocking is a structural property of the transport itself rather than a convention a scenario author could forget (REL-09, D-04)"
    - "The real-pump harness registers the real BG_PASSWORD and the real Auth0 bearer token with the redacting logger before either can appear in any log line, reusing the same createRedactingLogger contract the production platform and features/support/world.ts already use (AUTH-02, carried forward)"
    - "The discovery scenario asserts the account's configured Gemini is discovered within a bounded timeout; if the account genuinely returns zero devices the scenario fails with a clear timeout message rather than hanging indefinitely — this suite assumes, per D-04/CONTEXT.md, that the configured account carries at least one real Gemini (REL-09) — resolved empty-input edge"
  artifacts:
    - features/real-pump/support/realWorld.ts
    - features/real-pump/discovery.feature
    - test/realPumpCommandBlock.test.ts
  prohibitions:
    - { statement: "MUST NOT allow any code path in features/real-pump/ to reach CommandPort.send or an equivalent write against the real device — D-04's \"hard-blocked in the test transport itself\" is the single load-bearing safety property of this whole suite (REL-09)", status: unverified, flagged: true }
    - { statement: "MUST NOT wire features/real-pump/**/*.feature into the default Cucumber profile or any profile npm test/CI invokes automatically — it must stay reachable only via the explicit opt-in real profile a human runs deliberately (REL-09)", status: unverified, flagged: true }
  key_links:
    - "cucumber.json default.tags -> features/real-pump/**/*.feature exclusion"
    - "features/real-pump/support/realWorld.ts -> src/runtime/accountRuntime.js createAccountRuntimeFromConfig -> src/protocol.js PROTOCOL"
    - "BG_EMAIL/BG_PASSWORD env vars -> realWorld.ts -> createAuthClient (real Auth0 grant)"
---

<objective>
Fix the Cucumber default-profile tag gap before it can leak a real-account scenario into CI, and
build the opt-in real-pump suite's observation-only harness plus its discovery/initial-state
scenarios, with command-sending structurally unreachable from anywhere in the suite.

Purpose: REL-09/D-04 require the full scope this phase can deliver — discovery, initial REST
state, and full shadow state in this plan; heartbeats, natural updates, restart, and shutdown in
06-08 — built against the one real Gemini on the account, with commands hard-blocked in the
transport itself, not left to scenario-author discipline. RESEARCH.md measured that cucumber.json's
"default" profile has no tag exclusion today, so the moment a real-pump feature file exists it
would match npm test's own glob.
Output: cucumber.json's default profile excludes @real; a real, credentialed, command-free
account-runtime harness; a discovery feature; a structural test proving commands are unreachable.
</objective>

<execution_context>
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-validated-release-candidate/06-RESEARCH.md
@.planning/phases/06-validated-release-candidate/06-PATTERNS.md
@features/support/world.ts
@src/protocol.ts
@src/runtime/commandPort.ts
@dev/README.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Close the default-profile tag gap</name>
  <files>cucumber.json</files>
  <read_first>cucumber.json</read_first>
  <action>
    In cucumber.json, add "tags": "not @real" to the "default" profile object (currently: paths,
    import, format only). Leave the existing "real" profile's "tags": "@real and @read-only"
    unchanged.
  </action>
  <verify>
    <automated>O=$(node -e "const c=require('./cucumber.json'); process.exit(c.default.tags === 'not @real' ? 0 : 1)"); R=$?; test $R -eq 0</automated>
    <fails_when>non-zero exit, meaning cucumber.json's default profile does not carry exactly "not @real"</fails_when>
  </verify>
  <done>cucumber.json's default profile excludes @real-tagged scenarios; the existing test suite still passes with no behavior change (no @real-tagged feature exists yet in this task).</done>
</task>

<task type="auto">
  <name>Task 2: Observation-only real-pump harness, with commands structurally unreachable</name>
  <files>features/real-pump/support/realWorld.ts, test/realPumpCommandBlock.test.ts</files>
  <read_first>features/support/world.ts, src/protocol.ts, src/runtime/commandPort.ts, src/logging.ts, src/runtime/clock.ts, src/runtime/monotonicClock.ts, src/device/registry.ts, test/accessories/hapImportScope.test.ts</read_first>
  <action>
    Create features/real-pump/support/realWorld.ts, a Cucumber World for the real-pump suite that
    is deliberately narrower than features/support/world.ts: it never builds a
    BasementGuardianPlatform, a DiscoveryContext, or any BasementGuardianAccessory, because this
    suite proves the account-runtime/store layer only — with no HomeKit accessory layer built,
    there is no write handler in the suite that could ever reach a CommandPort in the first place.

    On construction (or in a Before hook), read process.env.BG_EMAIL and process.env.BG_PASSWORD;
    throw a clear error naming both variable names if either is unset or empty, so a maintainer
    who forgets to export them gets an immediate, named failure rather than an opaque Auth0
    rejection. Create a scratch directory with mkdtemp(join(tmpdir(),
    'basement-guardian-real-pump-')) as this scenario's storagePath; register its removal
    (rm(path, { recursive: true, force: true })) in an After hook so no real bearer token is ever
    left on disk after the run.

    Build a RedactingLogger via createRedactingLogger({ delegate: <a console-backed Logging
    object>, secrets: [] }) exactly as features/support/world.ts's logger() does, then register
    process.env.BG_PASSWORD as a secret immediately (the account runtime itself registers the
    granted bearer token as it authenticates, via the registerSecret callback
    src/cloud/auth.ts's AuthClientOptions already exposes) — so neither the real password nor a
    real token can appear in a log line before it is redacted.

    Call createAccountRuntimeFromConfig from '../../../src/runtime/accountRuntime.js' with: config
    { name: 'Basement Guardian (real-pump suite)', email: process.env.BG_EMAIL, password:
    process.env.BG_PASSWORD, clientId: PROTOCOL.clientId (the bundled public Auth0 client — do not
    override), pollIntervalSeconds: 300 (the minimum CONF-05 allows, since this suite is
    short-lived), offlineConfirmationPollCount: 2, ignoredFaults: [] }; constants: PROTOCOL (the
    real bundled endpoints, imported from '../../../src/protocol.js' — never the harness's fake
    constants); registry: createFamilyRegistry(); storagePath: the scratch directory;
    clock/monotonic: reuse the real production ports (systemClock from
    '../../../src/runtime/clock.js', systemMonotonicClock from
    '../../../src/runtime/monotonicClock.js') — never a scenario-controlled fake, because this
    suite proves the plugin's own real timing; log: the redacting logger; connect: the real
    connect from 'mqtt' (the same import src/platform.ts and features/support/world.ts already
    use); createSalt: a fresh randomBytes(16).toString('hex') per run;
    onTrustworthyInventory/onDeviceRemoved: push the reported deviceIds into a plain array field
    this World exposes read-only (no accessory registration of any kind); onMonitoringHealth: push
    a short summary string into an observations array this World exposes read-only.

    Expose only observation methods on the World: deviceIds() (from the recorded array), snapshot
    (deviceId) delegating to the runtime's store.snapshot(deviceId), observations() (the recorded
    monitoring-health summaries), and start()/stop() wrapping runtime.start()/runtime.stop(). Do
    NOT expose a method, field, or getter anywhere in this file whose name or body contains
    "commands" — the file must never reference AccountRuntime.commands, CommandPort, or
    sendCommand in any form; this is the property Task 2's structural gate proves mechanically.

    Create test/realPumpCommandBlock.test.ts, mirroring test/accessories/hapImportScope.test.ts's
    static-gate shape (read raw file text, assert an absence). Recursively enumerate every .ts
    file under features/real-pump/ (a fresh, small enumeration local to this test — do not import
    hapImportScope.test.ts's helper across a test-to-test dependency; copy the same
    readdirSync-based recursion pattern). For each file's raw text, assert it contains none of the
    literal substrings ".commands", "CommandPort", or "sendCommand". Assert the enumeration found
    at least one file (a floor of 1, since this task creates the first one), so the gate cannot
    pass by silently reading an empty directory.
  </action>
  <verify>
    <automated>O=$(npm run build:test 2>&1 && node --test dist-test/test/realPumpCommandBlock.test.js 2>&1); R=$?; test $R -eq 0 && printf '%s\n' "$O" | grep -qE '^ℹ fail 0$'</automated>
    <fails_when>non-zero exit, or no line reading exactly "ℹ fail 0" appears in the output</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit (the harness must type-check even though no credentials are set in this environment, since it never executes at import time — only inside a Before hook)</fails_when>
  </verify>
  <done>features/real-pump/support/realWorld.ts exists, builds the real account runtime with no HomeKit accessory layer and no path to CommandPort; test/realPumpCommandBlock.test.ts proves that mechanically and passes.</done>
</task>

<task type="auto">
  <name>Task 3: Discovery and initial-state scenarios</name>
  <files>features/real-pump/discovery.feature</files>
  <read_first>features/discovery.feature, features/CLAUDE.md</read_first>
  <action>
    Create features/real-pump/discovery.feature, tagged @real @read-only at the Feature level
    (matching cucumber.json's "real" profile filter exactly, and the default profile's exclusion).
    Follow features/CLAUDE.md's Gherkin conventions (sentence-cased scenario titles, lowercase
    steps, no "And", present tense, "the" for something specific). Write scenarios covering: (1)
    the account's configured Gemini is discovered within a bounded timeout (document the timeout
    value chosen and why, e.g. long enough for a real Auth0 grant plus a REST inventory round
    trip, short enough to fail fast in CI-scale wait budgets); (2) the discovered device's initial
    snapshot is non-empty and carries a valid deviceId once the first REST inventory completes;
    (3) the discovered device's snapshot gains shadow-sourced fields once the initial shadow "get"
    response arrives, within its own bounded timeout distinct from (1)'s discovery timeout. Do not
    assert a specific water level, pump state, or any other live value — REL-09 requires no
    scenario to depend on a particular natural state, only that state of some valid shape arrives.
  </action>
  <verify>
    <automated>O=$(npm run build:test 2>&1 && npx cucumber-js --profile real --dry-run 2>&1); R=$?; test $R -eq 0</automated>
    <fails_when>non-zero exit, or the dry run reports an undefined/ambiguous step (proves every step in this feature resolves to a step definition without needing live vendor credentials, since --dry-run never executes a step body)</fails_when>
  </verify>
  <done>features/real-pump/discovery.feature exists, tagged @real @read-only, with scenarios covering discovery, initial REST state, and initial full shadow state, none requiring a specific natural value.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| features/real-pump/ <-> the real vendor account | The only place in this repository's test tree that touches a live account, live credentials, and a live device |
| features/real-pump/ <-> CI | Must never cross — the suite is opt-in and human-run only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15 | Elevation of Privilege | features/real-pump/ command reachability | high | mitigate | No accessory/HomeKit layer is built in this harness at all, and test/realPumpCommandBlock.test.ts mechanically proves no file under features/real-pump/ references .commands, CommandPort, or sendCommand |
| T-06-16 | Tampering | cucumber.json default profile picking up @real scenarios | high | mitigate | "tags": "not @real" added to the default profile in the same task that adds the first features/real-pump/ file (Task 1 lands before Task 3) |
| T-06-17 | Information Disclosure | real BG_PASSWORD / real Auth0 bearer token | high | mitigate | Both are registered with the redacting logger before use; the token cache lives in a per-scenario mkdtemp directory removed in an After hook, never in a committed path |
| T-06-18 | Information Disclosure | real device/account identifiers in scenario output | low | accept | Consistent with D-027, the same deviceId-in-logs precedent already accepted for the production platform; no account-management fields are ever read by this suite |

</threat_model>

<verification>
- `npm run build:test && node --test dist-test/test/realPumpCommandBlock.test.js` passes.
- `npx cucumber-js --profile real --dry-run` resolves every step in discovery.feature with no live credentials required.
- `npm test` (the default profile) does not attempt to run anything under features/real-pump/.
</verification>

<success_criteria>
The default Cucumber profile cannot pick up a real-pump scenario; the real-pump harness reaches
the real account runtime with no path to a command write; discovery and initial-state scenarios
exist and dry-run clean.
</success_criteria>

<output>
Create `.planning/phases/06-validated-release-candidate/06-07-SUMMARY.md` when done
</output>
