---
phase: 06-validated-release-candidate
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - dev/README.md
  - test/documentation.test.ts
autonomous: true
requirements: [REL-08]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "README.md discloses all five REL-08 items: plaintext password storage, the child-bridge recommendation with its pairing/re-creation warning, the prerelease/vendor-alarm instruction, the battery-percentage estimate (already present), and the no-Critical-Alerts-guarantee statement (REL-08, D-023, D-026, D-036)"
    - { statement: "If README.md were empty or truncated, test/documentation.test.ts's five regex assertions would all fail rather than false-pass, because a match against an empty string never succeeds (REL-08)", verification: backstop }
    - "test/documentation.test.ts's assertions are case-insensitive regex matches against README.md's raw UTF-8 text, so a reworded or re-headed disclosure still satisfies the same check as long as the substantive phrase survives (REL-08)"
    - "dev/README.md's Networking section states the host mDNS/multicast prerequisite, a fail-fast precheck command, the firewall ports involved, and a tunnel-based workaround for a host without multicast"
  artifacts:
    - test/documentation.test.ts
  prohibitions:
    - { statement: "MUST NOT phrase the Critical Alerts disclosure in a way that could be read as a guarantee (e.g. \"should deliver\", \"is expected to notify\") — REL-08 requires an explicit no-guarantee statement (REL-08)", status: unverified, flagged: true }
  key_links:
    - "README.md disclosure prose -> test/documentation.test.ts regex assertions"
---

<objective>
Close the three REL-08 documentation gaps RESEARCH.md's line-by-line README audit found, and add
the mDNS/multicast prerequisite dev/README.md's Networking section is missing.

Purpose: REL-08 names five distinct disclosures. RESEARCH.md measured README.md against each one
this session: the battery-estimate disclosure is already correct (lines 227-231); the plaintext
password fact exists only in config.schema.json's GUI text; the child-bridge warning and the
Critical Alerts non-guarantee do not exist anywhere in README.md; the prerelease banner exists but
lacks the specific vendor-alarm instruction. Treat this as a five-item checklist, not one diff.
Output: README.md carries all five disclosures; dev/README.md documents the mDNS prerequisite;
test/documentation.test.ts gates all five README disclosures.
</objective>

<execution_context>
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-validated-release-candidate/06-RESEARCH.md
@README.md
@dev/README.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: The five REL-08 disclosures and their test gate</name>
  <files>README.md, test/documentation.test.ts</files>
  <read_first>README.md, config.schema.json (headerDisplay/password description, for parity wording)</read_first>
  <action>
    Edit README.md, matching the existing "Values that are estimates" section's register (short
    declarative paragraphs, one topic each):

    1. In the "Homebridge configuration" section, add one sentence noting that Homebridge stores
       the account password in plain text in config.json and in backups, for parity with
       config.schema.json's GUI-rendered disclosure (D-023) — a reader who only browses the repo
       should see the same fact a reader who opens the Settings GUI sees.
    2. Add a new section (after "Project structure" or near "Homebridge configuration")
       recommending a child bridge, per D-036: state that a child bridge isolates plugin crashes,
       startup delays, and restarts from other plugins; that it needs its own separate HomeKit
       pairing; and that changing bridge mode (main to child or back) can recreate accessories and
       disrupt existing rooms, scenes, and automations.
    3. Extend the existing "in development" banner near the top (currently: "This plugin is in
       development and is not released...") with the specific instruction D-026 requires: tell
       users to keep the vendor alarm and vendor notifications enabled while running a prerelease
       build.
    4. Leave the existing battery-estimate disclosure (lines 227-231) unchanged — it already
       satisfies REL-08.
    5. Add a sentence, near the Leak Sensor / `Sump Pit Flood` description or in a new short
       section, stating plainly that this plugin makes no guarantee of Apple's Critical Alerts
       delivery for the flood Leak Sensor — phrase it as a flat non-guarantee ("makes no
       guarantee that...", not "should" or "is expected to").

    Create test/documentation.test.ts. Copy the REPOSITORY_ROOT pattern from
    test/packageManifest.test.ts. Read README.md's raw text once into a module-level constant.
    Add five node:test cases, each titled with a closing "(REL-08, ...)" citation naming the
    relevant decision: readme matches /plain text/i; readme matches /child bridge/i; readme
    matches /vendor alarm/i; readme matches /estimate/i; readme matches /Critical Alerts/i.
  </action>
  <verify>
    <automated>O=$(npm run build:test 2>&1 && node --test dist-test/test/documentation.test.js 2>&1); R=$?; test $R -eq 0 && printf '%s\n' "$O" | grep -qE '^ℹ fail 0$'</automated>
    <fails_when>non-zero exit, or no line reading exactly "ℹ fail 0" appears in the output (any of the five disclosure checks failing)</fails_when>
    <automated>O=$(pre-commit run mdformat --files README.md 2>&1 && pre-commit run markdownlint-cli2 --files README.md 2>&1); R=$?; test $R -eq 0</automated>
    <fails_when>non-zero exit, or a hook line prints "Failed"</fails_when>
  </verify>
  <done>README.md contains all five REL-08 disclosures; test/documentation.test.ts's five cases pass.</done>
</task>

<task type="auto">
  <name>Task 2: dev/README.md mDNS/multicast prerequisite</name>
  <files>dev/README.md</files>
  <read_first>dev/README.md</read_first>
  <action>
    Extend dev/README.md's "Networking" section (currently: "The container uses host networking
    because HAP pairing needs mDNS and direct reachability from the phone. Only the bridge itself
    is exposed to the network; the UI stays on loopback.") with a new paragraph stating: a host
    lacking multicast (no firewall path for mDNS, no bridged network) will silently fail pairing
    with zero discovered responders, matching the observed `floyd` host's "0 responders where the
    LAN shows 25" failure recorded in STATE.md's pending todos. Add a fail-fast precheck command a
    developer can run before attempting to pair (e.g. `avahi-browse -a -t` or `dns-sd -B
    _hap._tcp` — pick the one appropriate to the host's mDNS stack and name it explicitly), state
    the firewall ports involved (mDNS UDP 5353, plus the HAP TCP port range), and describe the
    tunnel-based workaround for a host without multicast (running the container's network stack on
    a host that does have multicast reachability, or an mDNS reflector/relay across the network
    boundary).
  </action>
  <verify>
    <automated>grep -qi "multicast" dev/README.md && grep -qi "5353" dev/README.md</automated>
    <fails_when>either grep finds no match, meaning the multicast prerequisite or the mDNS port was not documented</fails_when>
  </verify>
  <done>dev/README.md's Networking section documents the multicast prerequisite, a precheck command, the ports involved, and a workaround.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin documentation <-> installing user | The one surface where safety/privacy caveats reach a user before they configure a live account |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-13 | Repudiation | README.md safety/privacy disclosures | medium | mitigate | test/documentation.test.ts fails if any of the five REL-08 disclosures is later removed or reworded past the point its regex still matches |
| T-06-14 | Information Disclosure | dev/README.md mDNS guidance | low | accept | Documentation-only change with no runtime effect; the precheck command runs locally and touches no vendor account |

</threat_model>

<verification>
- `npm run build:test && node --test dist-test/test/documentation.test.js` passes.
- `pre-commit run --files README.md dev/README.md` passes.
</verification>

<success_criteria>
README.md carries all five REL-08 disclosures under a passing test gate; dev/README.md documents
the mDNS prerequisite.
</success_criteria>

<output>
Create `.planning/phases/06-validated-release-candidate/06-06-SUMMARY.md` when done
</output>
