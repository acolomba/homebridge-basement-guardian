---
phase: 06-validated-release-candidate
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - SECURITY.md
  - .github/ISSUE_TEMPLATE/bug-report.md
  - .github/ISSUE_TEMPLATE/support-request.md
  - test/repositoryGovernance.test.ts
autonomous: true
requirements: [REL-06]

estimate:
  tokens: 27000
  raw_tokens: 27000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SECURITY.md exists, states support is best-effort with no promised response time or SLA, and directs vulnerability reports to private GitHub Security Advisories rather than the public issue tracker (REL-06, D-025)"
    - "Private vulnerability reporting is enabled on the repository via the GitHub API, and repeating that same PUT call is safe because HTTP PUT is idempotent — invoking it twice leaves the setting in the same enabled state (REL-06) — resolved concurrency edge"
    - "Neither bug-report.md nor support-request.md instructs a reporter to paste their complete config.json — D-025 explicitly forbids asking for complete configuration files, and the prior wording did exactly that (REL-06)"
  artifacts:
    - SECURITY.md
    - test/repositoryGovernance.test.ts
  prohibitions:
    - { statement: "MUST NOT state or imply a response-time guarantee, SLA, or promise of continued compatibility anywhere in SECURITY.md (REL-06, D-025)", status: unverified, flagged: true }
  key_links:
    - "SECURITY.md private-advisory instruction -> GitHub's private-vulnerability-reporting feature (enabled via gh api)"
    - "bug-report.md / support-request.md Config section -> test/repositoryGovernance.test.ts"
---

<objective>
Give this repository a support and vulnerability-disclosure policy that matches D-025 exactly, and
fix a real gap the templates currently have: they ask reporters to paste their complete
config.json, which is the one thing D-025 explicitly forbids asking for.

Purpose: REL-06 requires best-effort support via issue templates plus private security advisories.
RESEARCH.md judged the templates "already meet most of it," but line-by-line reading against
D-025's exact text ("Never ask for passwords, tokens, or complete configuration files") found the
Config sections do the opposite.
Output: SECURITY.md, private vulnerability reporting enabled, and both templates corrected.
</objective>

<execution_context>
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-validated-release-candidate/06-RESEARCH.md
@.github/ISSUE_TEMPLATE/bug-report.md
@.github/ISSUE_TEMPLATE/support-request.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: SECURITY.md and private vulnerability reporting</name>
  <files>SECURITY.md</files>
  <precondition>gh CLI is authenticated with repo-scope access to acolomba/homebridge-basement-guardian (confirm with `gh auth status` before invoking `gh api`; RESEARCH.md verified this scope this session)</precondition>
  <read_first>.planning/intel/decisions.md (D-025 section), .github/ISSUE_TEMPLATE/bug-report.md, .github/ISSUE_TEMPLATE/support-request.md</read_first>
  <action>
    Create SECURITY.md at the repository root. State, in D-025's own terms: support is provided
    best-effort through the public GitHub Issues tracker for the current stable release only; no
    response time, resolution time, continued-compatibility, or SLA is promised; routine fixes are
    not backported to older release lines, and a reporter is asked to reproduce a problem on the
    current stable release first. Add a "Reporting a Vulnerability" section directing reporters to
    use GitHub's private Security Advisories feature (link:
    https://github.com/acolomba/homebridge-basement-guardian/security/advisories/new) instead of a
    public issue, and state that a report should never include the account password, an
    authentication token, or a complete config.json.

    Enable GitHub's private vulnerability reporting on the repository by running: `gh api
    --method PUT /repos/acolomba/homebridge-basement-guardian/private-vulnerability-reporting`.
    Confirm it took effect with `gh api /repos/acolomba/homebridge-basement-guardian | grep -i
    security_and_analysis` or by re-running the PUT and confirming a 204/no-error response (the
    call is idempotent — repeating it is safe).
  </action>
  <verify>
    <automated>test -f SECURITY.md && grep -qi "best.effort" SECURITY.md && grep -qi "security/advisories" SECURITY.md</automated>
    <fails_when>SECURITY.md is missing, or does not mention best-effort support, or does not point to the private advisories path</fails_when>
  </verify>
  <done>SECURITY.md exists with the best-effort/no-SLA policy and the private-advisory pointer; private vulnerability reporting is enabled on the GitHub repository.</done>
</task>

<task type="auto">
  <name>Task 2: Stop asking for the complete config.json in issue templates</name>
  <files>.github/ISSUE_TEMPLATE/bug-report.md, .github/ISSUE_TEMPLATE/support-request.md, test/repositoryGovernance.test.ts</files>
  <read_first>.github/ISSUE_TEMPLATE/bug-report.md, .github/ISSUE_TEMPLATE/support-request.md</read_first>
  <action>
    In both .github/ISSUE_TEMPLATE/bug-report.md and .github/ISSUE_TEMPLATE/support-request.md,
    replace the "**Plugin Config:**" section — which currently reads "Show your Homebridge
    config.json here, remove any sensitive information." inside a ```json fenced block — with a
    narrower instruction: ask only for the `platform` block for `"platform": "BasementGuardian"`
    from config.json, with the email and password fields removed (not merely "sensitive
    information" left to the reporter's judgment), matching D-025's "never ask for... complete
    configuration files" rule exactly rather than trusting the reporter to redact enough on their
    own. Keep the existing fenced-code-block presentation and the surrounding heading style
    unchanged; change only the instructional sentence and its emphasis on email/password removal.

    Create test/repositoryGovernance.test.ts (alongside test/packageManifest.test.ts, not under
    test/packaging/ — this asserts on GitHub repo-governance files, not the npm package). Using
    the REPOSITORY_ROOT pattern from test/packageManifest.test.ts, add: one test asserting
    SECURITY.md exists and its text matches /best.effort/i; one test asserting it matches
    /security\/advisories/i; one test per template asserting the raw text does NOT match
    /show your homebridge config\.json here/i (the exact prior instruction this task removes) and
    DOES match /email/i and /password/i (proving the narrower, explicit redaction instruction
    replaced it rather than being silently deleted).
  </action>
  <verify>
    <automated>O=$(npm run build:test 2>&1 && node --test dist-test/test/repositoryGovernance.test.js 2>&1); R=$?; test $R -eq 0 && printf '%s\n' "$O" | grep -qE '^ℹ fail 0$'</automated>
    <fails_when>non-zero exit, or no line reading exactly "ℹ fail 0" appears in the output</fails_when>
  </verify>
  <done>Neither template instructs pasting the complete config.json; both explicitly call out removing email and password; the new test gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public GitHub issue <-> reporter's local config.json | The exact channel D-025 forbids leaking credentials through |
| Private GitHub Security Advisory <-> vulnerability reporter | The channel D-025 requires for anything sensitive |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11 | Information Disclosure | .github/ISSUE_TEMPLATE/bug-report.md, support-request.md | high | mitigate | The templates no longer instruct pasting the complete config.json (which carries the account password in plaintext, per D-023); the narrowed instruction is gated by test/repositoryGovernance.test.ts |
| T-06-12 | Information Disclosure | SECURITY.md vulnerability channel | medium | mitigate | Reports are directed to GitHub's private Security Advisories, not the public issue tracker; private vulnerability reporting is enabled on the repository |

</threat_model>

<verification>
- `npm run build:test && node --test dist-test/test/repositoryGovernance.test.js` passes.
- `gh api /repos/acolomba/homebridge-basement-guardian` shows private vulnerability reporting enabled.
</verification>

<success_criteria>
SECURITY.md matches D-025 exactly; private vulnerability reporting is on; neither template can
lead a reporter to paste a plaintext credential.
</success_criteria>

<output>
Create `.planning/phases/06-validated-release-candidate/06-05-SUMMARY.md` when done
</output>
