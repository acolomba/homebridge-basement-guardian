---
phase: 06-validated-release-candidate
plan: 10
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05", "06-06", "06-07", "06-08", "06-09"]
files_modified:
  - CHANGELOG.md
  - .fallowrc.json
autonomous: true
requirements: [REL-02, REL-07]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "npm run check passes locally with every plan in this phase's changes present together (REL-02) — the matrix and the whole test suite are exercised in CI per plan 06-01/06-02, but this task is the first point where every file this phase touched exists in the same tree at once"
    - "CHANGELOG.md's [Unreleased] section records this phase's user-visible changes: the CI compatibility matrix now covers three Homebridge versions, the license boundary and packaging metadata are publish-ready, README documents the child-bridge recommendation and the no-Critical-Alerts-guarantee, and an opt-in real-pump observation suite exists"
    - "This plan does not flip any REL-01..REL-09 row in REQUIREMENTS.md to Complete — REL-07 explicitly stays gated on G-001..G-004, which this phase's own checklists (06-09) record as pending, not passed; requirement-row reconciliation is left to /gsd-verify-work per this project's established convention for gate-blocked rows (see STATE.md's Deferred Verification table for the Phase 3/4 precedent)"
  artifacts: []
  prohibitions:
    - { statement: "MUST NOT mark REL-07 (or any REL-01..REL-09 row) Complete in REQUIREMENTS.md from this phase's own local evidence alone — the CI matrix has not yet run on GitHub Actions and G-001..G-004 remain unexecuted", status: unverified, flagged: true }
  key_links:
    - "Every plan 06-01 through 06-09's new file -> npm run check's aggregate result"
---

<objective>
Confirm the whole phase's changes hold together in one tree, and record what shipped in
CHANGELOG.md.

Purpose: nine plans across two waves each verified independently; this plan is the first point
where all of them exist together, which is exactly where a fallow duplication/health threshold, a
coverage regression, or a formatting drift between two plans' independent edits would first
surface. REL-02 requires the deterministic suites stay green; this plan proves that once, for the
combined tree, before the phase closes.
Output: a green `npm run check`, CHANGELOG.md updated, and any fallow exemption this phase's new
test/packaging/ files genuinely need (matching the project's existing production: false pattern).
</objective>

<execution_context>
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CHANGELOG.md
@.fallowrc.json
@.planning/phases/06-validated-release-candidate/06-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Full local gate across the combined phase changes</name>
  <files>.fallowrc.json</files>
  <read_first>.fallowrc.json</read_first>
  <action>
    Run `npm run check` (typecheck, lint, fallow, format:check, then the full test suite) against
    the tree with every plan in this phase applied. If fallow's dead-code, health, or duplicate
    gate reports a genuine finding caused by this phase's own new files (for example, several
    test/packaging/*.test.ts files sharing the same REPOSITORY_ROOT-resolution snippet tripping
    the duplicate-code threshold), resolve it the way the project already resolves this class of
    finding: either restructure the specific duplicated logic if it is a real one, or add a
    narrowly-scoped entry to .fallowrc.json's ignoreFindings array (mirroring the existing
    "src/device/events.ts" entry, which stays listed because it has no production consumer, not
    because the gate is wrong) with a comment explaining exactly why the flagged pattern is
    intentional repetition across independent policy-gate test files rather than a defect. Do not
    widen production: true/false or any other unrelated .fallowrc.json setting.
  </action>
  <verify>
    <automated>O=$(npm run check 2>&1); R=$?; test $R -eq 0</automated>
    <fails_when>non-zero exit anywhere in typecheck, lint, fallow, format:check, or the full test suite</fails_when>
  </verify>
  <done>npm run check passes with every plan 06-01 through 06-09's changes present in the same tree; any fallow exemption added is narrowly scoped and commented.</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>CHANGELOG.md</read_first>
  <action>
    Add entries to CHANGELOG.md's existing [Unreleased] section, matching its established voice
    (short declarative sentences, one change per bullet, no phase/plan references per this
    project's comment-and-changelog convention). Under "Added": an opt-in, read-only real-pump
    observation suite exists for a maintainer who wants to verify the plugin against their own
    real Gemini; a SECURITY.md documents the support policy and private vulnerability reporting.
    Under "Changed": continuous integration now verifies the plugin against three supported
    Homebridge versions in addition to both supported Node.js versions; the package's license
    metadata and file headers now state the Apache-2.0/MIT boundary between template-derived and
    original files; the README documents the child-bridge recommendation, keeping the vendor alarm
    and notifications enabled during a prerelease, and that flood notification delivery carries no
    Critical Alerts guarantee. Do not add an entry claiming any G-001..G-004 gate passed, and do
    not create a new version heading — everything this phase shipped stays under [Unreleased]
    until the maintainer actually publishes, per D-01.
  </action>
  <verify>
    <automated>grep -qi "real-pump" CHANGELOG.md && grep -qi "SECURITY" CHANGELOG.md && grep -qi "Homebridge version" CHANGELOG.md</automated>
    <fails_when>any grep finds no match, meaning one of the three added entries is missing</fails_when>
  </verify>
  <done>CHANGELOG.md's [Unreleased] section records this phase's user-visible changes without claiming any gate passed or creating a new version heading.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This plan's CHANGELOG entry <-> a future reader deciding whether to upgrade | The one place an overclaim about gate status would mislead a reader into thinking `1.0.0` is closer than it is |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-23 | Repudiation | CHANGELOG.md / REQUIREMENTS.md gate claims | high | mitigate | This plan's action explicitly forbids claiming a G-00X pass or a new version heading; the prohibition is stated in must_haves and checked by the executor against D-01/D-03 |

</threat_model>

<verification>
- `npm run check` passes.
- CHANGELOG.md's [Unreleased] section names the three new user-visible additions/changes.
</verification>

<success_criteria>
The combined phase change set is green under the full local gate, and CHANGELOG.md accurately
records what shipped without overclaiming gate status.
</success_criteria>

<output>
Create `.planning/phases/06-validated-release-candidate/06-10-SUMMARY.md` when done
</output>
