---
phase: 06-validated-release-candidate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/packaging/dependencyAllowlist.test.ts
  - test/packaging/dependencyLicenses.test.ts
  - test/packaging/dependencyTelemetry.test.ts
autonomous: true
requirements: [REL-03, REL-04]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "package.json's production dependencies object is exactly {\"mqtt\": \"^5.15.2\"} — a new runtime dependency added later must be a deliberate edit that this test names by failing (REL-03)"
    - "REL-03's own text ('review direct dependencies for telemetry behavior') is satisfied by a mechanical review implemented in this plan rather than left as a one-time desk check — resolved unclassified edge"
    - "No production (non-dev) package recorded in package-lock.json carries a GPL-family license identifier (REL-03)"
    - "No file under node_modules/mqtt's installed tree contains a known telemetry-SDK identifier (sentry, segment, amplitude, mixpanel, google-analytics, bugsnag, datadog) (REL-03)"
  artifacts:
    - test/packaging/dependencyAllowlist.test.ts
    - test/packaging/dependencyLicenses.test.ts
    - test/packaging/dependencyTelemetry.test.ts
  prohibitions:
    - { statement: "MUST NOT add any outbound network call to a host outside the vendor's own domains (Auth0 tenant, vendor REST API, AWS IoT endpoint) anywhere in this phase's changes (REL-03)", status: unverified, flagged: true }
  key_links:
    - "package.json dependencies -> dependencyAllowlist.test.ts"
    - "package-lock.json packages[].license -> dependencyLicenses.test.ts"
    - "node_modules/mqtt installed tree -> dependencyTelemetry.test.ts"
---

<objective>
Turn REL-03's "direct dependencies pass a telemetry review" and "no unnecessary dependency"
requirements into three mechanical, re-runnable gates instead of a one-time desk check.

Purpose: this project ships exactly one production dependency (mqtt). REL-03 requires that stay
true and stay clean of telemetry/tracking code and copyleft licensing; a test gate catches a future
`npm install <new-runtime-dep>` the way test/packageManifest.test.ts already catches an engines
drift.
Output: three new node:test files under test/packaging/, all passing against the current lockfile.
</objective>

<execution_context>
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-validated-release-candidate/06-RESEARCH.md
@.planning/phases/06-validated-release-candidate/06-PATTERNS.md
@test/packageManifest.test.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Exact production dependency allowlist</name>
  <files>test/packaging/dependencyAllowlist.test.ts</files>
  <read_first>test/packageManifest.test.ts, package.json</read_first>
  <action>
    Create test/packaging/dependencyAllowlist.test.ts. Copy the imports and REPOSITORY_ROOT
    resolution pattern from test/packageManifest.test.ts (two ".." segments, same depth under
    dist-test/test/packaging/). Read package.json's "dependencies" object and assert
    Object.keys(manifest.dependencies).sort() deepStrictEqual ['mqtt'] (REL-03). Give the test
    title a closing "(REL-03)" citation per this repository's convention.
  </action>
  <verify>
    <automated>O=$(npm run build:test 2>&1 && node --test dist-test/test/packaging/dependencyAllowlist.test.js 2>&1); R=$?; test $R -eq 0 && printf '%s\n' "$O" | grep -qE '^ℹ fail 0$'</automated>
    <fails_when>non-zero exit, or no line reading exactly "ℹ fail 0" appears in the output</fails_when>
  </verify>
  <done>test/packaging/dependencyAllowlist.test.ts exists and passes, asserting the production dependency set is exactly ['mqtt'].</done>
</task>

<task type="auto">
  <name>Task 2: Production dependency license audit</name>
  <files>test/packaging/dependencyLicenses.test.ts</files>
  <read_first>package-lock.json (packages object shape only — read the first ~80 lines to confirm the lockfile version and the per-package "license"/"dev" field shape)</read_first>
  <action>
    Create test/packaging/dependencyLicenses.test.ts, using the same REPOSITORY_ROOT pattern.
    Read and JSON.parse package-lock.json. Iterate its top-level "packages" object (npm lockfile
    v3 format: keys are paths like "node_modules/mqtt", the root package is the empty-string key).
    Skip the root entry and skip any entry whose value carries "dev": true (devDependency-only
    packages are out of scope for a production-dependency license review). For every remaining
    (production) entry that has a "license" string field, assert it does NOT match
    /^(L?GPL|AGPL)/i. Collect every failing package name into the assertion failure message so a
    future violation names the exact offending package rather than just "some package failed".
    Title the test with a closing "(REL-03)" citation.
  </action>
  <verify>
    <automated>O=$(npm run build:test 2>&1 && node --test dist-test/test/packaging/dependencyLicenses.test.js 2>&1); R=$?; test $R -eq 0 && printf '%s\n' "$O" | grep -qE '^ℹ fail 0$'</automated>
    <fails_when>non-zero exit, or no line reading exactly "ℹ fail 0" appears in the output</fails_when>
  </verify>
  <done>test/packaging/dependencyLicenses.test.ts exists and passes against the current package-lock.json, naming any GPL-family production package it would find.</done>
</task>

<task type="auto">
  <name>Task 3: Production dependency telemetry-signature review</name>
  <files>test/packaging/dependencyTelemetry.test.ts</files>
  <read_first>node_modules/mqtt/package.json (confirm the installed package's own file layout before scanning it)</read_first>
  <action>
    Create test/packaging/dependencyTelemetry.test.ts, using the same REPOSITORY_ROOT pattern.
    Recursively enumerate every .js file under node_modules/mqtt (the whole installed production
    dependency, not just its own source — mqtt.js's transitive tree is small and already
    installed). For each file's raw text, assert it contains none of these case-insensitive
    telemetry-SDK identifiers: 'sentry', 'segment.io', 'amplitude', 'mixpanel',
    'google-analytics', 'bugsnag', 'datadog'. Collect every matching file path into the assertion
    failure message. Title the test with a closing "(REL-03)" citation, and add a short comment
    noting this is a defensive signature scan of the one production dependency, not a substitute
    for RESEARCH.md's own review (npm audit clean, well-known OSS client), per Assumption A2.
  </action>
  <verify>
    <automated>O=$(npm run build:test 2>&1 && node --test dist-test/test/packaging/dependencyTelemetry.test.js 2>&1); R=$?; test $R -eq 0 && printf '%s\n' "$O" | grep -qE '^ℹ fail 0$'</automated>
    <fails_when>non-zero exit, or no line reading exactly "ℹ fail 0" appears in the output</fails_when>
  </verify>
  <done>test/packaging/dependencyTelemetry.test.ts exists and passes, scanning the installed mqtt tree for known telemetry-SDK identifiers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install -> production dependency tree | The one path a telemetry/tracking dependency could enter the shipped package |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Information Disclosure | production dependency tree (mqtt) | medium | mitigate | dependencyAllowlist.test.ts pins the exact dependency set; dependencyTelemetry.test.ts scans it for known telemetry signatures |
| T-06-08 | Tampering | package-lock.json license fields | low | accept | License strings are self-reported by each package's own package.json at publish time; a package that lies about its own license is a supply-chain risk outside this test's reach, and is the same residual risk npm audit already accepts |

</threat_model>

<verification>
- `npm run build:test && node --test dist-test/test/packaging/dependencyAllowlist.test.js dist-test/test/packaging/dependencyLicenses.test.js dist-test/test/packaging/dependencyTelemetry.test.js` passes.
</verification>

<success_criteria>
Three mechanical dependency gates exist and pass against the current lockfile and installed tree.
</success_criteria>

<output>
Create `.planning/phases/06-validated-release-candidate/06-03-SUMMARY.md` when done
</output>
