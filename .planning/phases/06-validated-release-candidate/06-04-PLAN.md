---
phase: 06-validated-release-candidate
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/settings.ts
  - src/cloud/api.ts
  - src/cloud/auth.ts
  - src/cloud/mqttTransport.ts
  - test/settings.test.ts
  - test/cloud/api.test.ts
  - test/cloud/auth.test.ts
  - test/cloud/mqttTransport.test.ts
autonomous: true
requirements: [REL-03, REL-04]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "One identity string, PLUGIN_USER_AGENT (renamed from COMMAND_USER_AGENT), is applied consistently to every outbound request this plugin makes: the REST read path, the REST command path, the Auth0 login request, and the MQTT WebSocket handshake (REL-03, REL-04 folded todo)"
    - "PLUGIN_USER_AGENT carries no version, hostname, operating-system detail, bridge name, account identifier, or device identifier, matching the existing COMMAND_USER_AGENT contract test/cloud/api.test.ts already asserts (AUTH-02, carried forward)"
  artifacts: []
  prohibitions:
    - { statement: "MUST NOT send the plugin's outbound identity header to, or otherwise add a new outbound destination beyond, the vendor's own Auth0 tenant, REST API, and AWS IoT endpoint (REL-03)", status: unverified, flagged: true }
  key_links:
    - "src/settings.ts PLUGIN_USER_AGENT -> src/cloud/api.ts requestInit() (both branches)"
    - "src/settings.ts PLUGIN_USER_AGENT -> src/cloud/auth.ts fetchGrant() headers"
    - "src/settings.ts PLUGIN_USER_AGENT -> src/cloud/mqttTransport.ts connect() wsOptions.headers"
---

<objective>
Make the plugin identify itself consistently on every outbound request it makes, not only on the
one command-issuing REST call that currently carries a User-Agent header.

Purpose: CONTEXT.md folds the "define cloud request header policy" todo into this phase's
REL-03/REL-04 privacy review. RESEARCH.md measured that only src/cloud/api.ts:146's command path
sets a User-Agent; the REST read path, the Auth0 login request, and the MQTT WebSocket handshake
send none. RESEARCH.md's Anti-Patterns section explicitly rejects homebridge-adt-pulse's
browser-impersonation approach (fake Sec-Fetch-*/Origin/Referer/telemetry headers) — this plan
does the honest opposite: one real, minimal, already-approved identity string, applied everywhere.
Output: a renamed, relocated PLUGIN_USER_AGENT constant applied on all four outbound call sites.
</objective>

<execution_context>
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-validated-release-candidate/06-RESEARCH.md
@src/cloud/api.ts
@src/cloud/auth.ts
@src/cloud/mqttTransport.ts
@src/settings.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Relocate the identity constant and apply it to both REST branches</name>
  <files>src/settings.ts, src/cloud/api.ts, test/settings.test.ts, test/cloud/api.test.ts</files>
  <read_first>src/settings.ts, src/cloud/api.ts, test/settings.test.ts, test/cloud/api.test.ts</read_first>
  <behavior>
    - src/settings.ts exports PLUGIN_USER_AGENT, equal to PLUGIN_NAME, carrying no "/" character
      (the same two assertions test/cloud/api.test.ts currently makes on COMMAND_USER_AGENT,
      relocated to test/settings.test.ts).
    - src/cloud/api.ts's requestInit() sets a 'User-Agent': PLUGIN_USER_AGENT header on BOTH the
      no-body (GET) branch and the body-carrying (command) branch, importing PLUGIN_USER_AGENT
      from '../settings.js' instead of declaring COMMAND_USER_AGENT locally.
    - test/cloud/api.test.ts no longer imports or asserts COMMAND_USER_AGENT from
      '../../src/cloud/api.js'; it asserts the GET-path request carries a 'User-Agent' header
      equal to PLUGIN_USER_AGENT, alongside its existing Authorization-only assertion for that
      branch.
  </behavior>
  <action>
    In src/settings.ts, add: `export const PLUGIN_USER_AGENT = PLUGIN_NAME;` with a JSDoc comment
    adapted from src/cloud/api.ts's current COMMAND_USER_AGENT doc comment (same rationale: no
    version, no hostname, no account/device identifier, does not impersonate the official vendor
    app), updated to note it now applies to every outbound request, not only commands.

    In src/cloud/api.ts: delete the local `export const COMMAND_USER_AGENT = PLUGIN_NAME;`
    declaration and its doc comment; import PLUGIN_USER_AGENT from '../settings.js' instead (the
    file already imports PLUGIN_NAME from there). In requestInit(), add `'User-Agent':
    PLUGIN_USER_AGENT` to the no-body branch's headers object (currently `{ Authorization:
    authorization }` alone), and change the body-carrying branch's `'User-Agent':
    COMMAND_USER_AGENT` reference to `'User-Agent': PLUGIN_USER_AGENT`. Rewrite the comment block
    above requestInit() (currently arguing the header belongs to the command branch alone as a
    deliberate decision) to instead state that every REST request now identifies itself the same
    way, and that Content-Type/Accept remain command-only because only a command carries a JSON
    body to describe.

    In test/settings.test.ts, add the two identity-shape assertions test/cloud/api.test.ts
    currently makes on COMMAND_USER_AGENT (equals PLUGIN_NAME; contains no '/'), retargeted at the
    newly exported PLUGIN_USER_AGENT.

    In test/cloud/api.test.ts, remove the COMMAND_USER_AGENT import and its two identity-shape
    assertions (now owned by test/settings.test.ts), and update its import to no longer reference
    COMMAND_USER_AGENT. Find the case(s) asserting the GET-path (devices/device/awsCredentials)
    request's exact fetch() header object and update the expected headers to include 'User-Agent':
    PLUGIN_USER_AGENT alongside the existing Authorization header, so the case still asserts the
    complete header set rather than a subset.
  </action>
  <verify>
    <automated>O=$(npm run build:test 2>&1 && node --test dist-test/test/settings.test.js dist-test/test/cloud/api.test.js 2>&1); R=$?; test $R -eq 0 && printf '%s\n' "$O" | grep -qE '^ℹ fail 0$'</automated>
    <fails_when>non-zero exit, or no line reading exactly "ℹ fail 0" appears in the output</fails_when>
  </verify>
  <done>src/settings.ts exports PLUGIN_USER_AGENT; src/cloud/api.ts applies it on both request branches; both test files pass with the header asserted on the GET path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the identity header to the Auth0 login request and the MQTT handshake</name>
  <files>src/cloud/auth.ts, src/cloud/mqttTransport.ts, test/cloud/auth.test.ts, test/cloud/mqttTransport.test.ts</files>
  <read_first>src/cloud/auth.ts, src/cloud/mqttTransport.ts, test/cloud/auth.test.ts, test/cloud/mqttTransport.test.ts, test/runtime/accountRuntime.test.ts (grep for any exact-shape assertion on the MQTT connect options object before editing MqttConnectOptions)</read_first>
  <behavior>
    - src/cloud/auth.ts's fetchGrant() sends a 'User-Agent': PLUGIN_USER_AGENT header alongside
      its existing 'Content-Type': 'application/json' header on the Auth0 grant POST.
    - src/cloud/mqttTransport.ts's MqttConnectOptions interface gains an optional wsOptions field
      shaped { headers: Record<string, string> }; createMqttTransport() passes wsOptions: {
      headers: { 'User-Agent': <the caller-supplied identity string> } } in the object it hands to
      options.connect(), without changing any existing connect-option field.
    - MqttTransportOptions gains a userAgent: string field the caller supplies (mirroring how
      clientId and url are already caller-supplied), so mqttTransport.ts stays free of a direct
      import from settings.ts and the identity string is wired at the one composition site
      (src/runtime/accountRuntime.ts) that already assembles MqttTransportOptions.
  </behavior>
  <action>
    In src/cloud/auth.ts: import PLUGIN_USER_AGENT from '../settings.js'. In fetchGrant()'s fetch
    call, change `headers: { 'Content-Type': 'application/json' }` to also include 'User-Agent':
    PLUGIN_USER_AGENT.

    In src/cloud/mqttTransport.ts: add `wsOptions?: { headers: Record<string, string> }` to the
    MqttConnectOptions interface (optional, since not every connect() caller in this codebase
    needs it — check whether any other module constructs a literal satisfying MqttConnectOptions
    besides createMqttTransport() itself before deciding optionality). Add `userAgent: string` to
    MqttTransportOptions. In createMqttTransport()'s call to options.connect(), add `wsOptions: {
    headers: { 'User-Agent': options.userAgent } }` to the options object literal.

    Locate where MqttTransportOptions is assembled for the real connection (in
    src/runtime/accountRuntime.ts, where createMqttTransport is wired) and add `userAgent:
    PLUGIN_USER_AGENT` there, importing PLUGIN_USER_AGENT from '../settings.js' if not already
    imported. Check features/support/world.ts's own call into the same seam (it builds
    MqttTransportOptions indirectly through createAccountRuntimeFromConfig) and confirm whether it
    needs a harness-side userAgent value too — if MqttTransportOptions.userAgent is a required
    field, the harness composition path must supply one (a placeholder string is fine, matching
    the harness's other placeholder values).

    Update test/cloud/auth.test.ts's case(s) asserting fetchGrant's exact request headers to
    include 'User-Agent': PLUGIN_USER_AGENT. Update test/cloud/mqttTransport.test.ts's case(s)
    asserting the exact connect-options object passed to a stubbed MqttConnect to include the new
    wsOptions.headers field. If test/runtime/accountRuntime.test.ts or features/support/world.ts
    contains an exact-shape (deepStrictEqual) assertion on the connect-options object that would
    now be missing the new field, update it too, per this task's read_first check.
  </action>
  <verify>
    <automated>O=$(npm run build:test 2>&1 && node --test dist-test/test/cloud/auth.test.js dist-test/test/cloud/mqttTransport.test.js dist-test/test/runtime/accountRuntime.test.js 2>&1); R=$?; test $R -eq 0 && printf '%s\n' "$O" | grep -qE '^ℹ fail 0$'</automated>
    <fails_when>non-zero exit, or no line reading exactly "ℹ fail 0" appears in the output</fails_when>
    <automated>O=$(npm run check 2>&1); R=$?; test $R -eq 0</automated>
    <fails_when>non-zero exit (full local gate — coverage, lint, typecheck, the whole suite, and the Cucumber harness's own connect() call site)</fails_when>
  </verify>
  <done>The Auth0 login request and the MQTT WebSocket handshake both carry the plugin's identity header; every affected test passes; npm run check is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin -> vendor Auth0 tenant / REST API / AWS IoT endpoint | Every outbound request this plugin makes, now uniformly identified |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09 | Information Disclosure | PLUGIN_USER_AGENT header value | low | accept | The identity string is the plugin name alone (AUTH-02) — no version, hostname, account, or device identifier; carried forward unchanged from the existing COMMAND_USER_AGENT contract test |
| T-06-10 | Spoofing | Outbound headers mimicking a browser or the official vendor app | low | mitigate | RESEARCH.md's Anti-Patterns explicitly reject the homebridge-adt-pulse browser-impersonation approach; this plan adds exactly one honest identity header and nothing else (no Sec-Fetch-*, Origin, Referer, or fabricated telemetry headers) |

</threat_model>

<verification>
- `npm run check` passes with the identity header present on all four outbound call sites.
</verification>

<success_criteria>
Every outbound vendor request (REST read, REST command, Auth0 login, MQTT handshake) carries the
same honest, minimal identity header; no new outbound destination is introduced.
</success_criteria>

<output>
Create `.planning/phases/06-validated-release-candidate/06-04-SUMMARY.md` when done
</output>
