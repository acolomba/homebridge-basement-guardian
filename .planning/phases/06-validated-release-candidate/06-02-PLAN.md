---
phase: 06-validated-release-candidate
plan: 02
type: execute
wave: 2
depends_on: ["06-01", "06-04"]
files_modified:
  - src/index.ts
  - src/settings.ts
  - src/platform.ts
  - src/accessories/alarmMute.ts
  - src/accessories/basementGuardian.ts
  - src/accessories/controls.ts
  - src/accessories/customCharacteristics.ts
  - src/accessories/customServices.ts
  - src/accessories/pumpRecords.ts
  - src/accessories/reconciliation.ts
  - src/accessories/serviceCatalogue.ts
  - src/accessories/services.ts
  - src/accessories/staleMarking.ts
  - src/cloud/api.ts
  - src/cloud/auth.ts
  - src/cloud/errors.ts
  - src/cloud/mqttTransport.ts
  - src/cloud/shadow.ts
  - src/cloud/sigv4.ts
  - src/cloud/types.ts
  - src/config.ts
  - src/device/events.ts
  - src/device/family.ts
  - src/device/gemini.ts
  - src/device/halo.ts
  - src/device/health.ts
  - src/device/registry.ts
  - src/device/state.ts
  - src/device/waterLevel.ts
  - src/logging.ts
  - src/persistence/accessoryContext.ts
  - src/protocol.ts
  - src/runtime/accessoryStore.ts
  - src/runtime/accountRuntime.ts
  - src/runtime/arrivalAnchors.ts
  - src/runtime/clock.ts
  - src/runtime/commandPort.ts
  - src/runtime/failureLog.ts
  - src/runtime/monitoringHealth.ts
  - src/runtime/monotonicClock.ts
  - src/runtime/retryPolicy.ts
  - src/runtime/timers.ts
  - test/packaging/licenseHeaders.test.ts
autonomous: false
requirements: [REL-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every .ts file under src/ carries a first-line SPDX-License-Identifier comment: Apache-2.0 on the three template-derived files, MIT on the other 39 (REL-05, D-035)"
    - "src/index.ts, src/settings.ts, and src/platform.ts additionally carry a one-line note identifying them as modified from the Homebridge plugin template, satisfying Apache section 4's 'mark changed files' requirement (REL-05, D-035)"
  artifacts:
    - test/packaging/licenseHeaders.test.ts (extended)
  prohibitions:
    - { statement: "MUST NOT remove or weaken the Apache-2.0 attribution on a file while it still contains template-derived material (REL-05, D-035)", status: unverified, flagged: true }
  key_links:
    - "test/packaging/licenseHeaders.test.ts per-file gate -> every src/*.ts file's first line"
---

<objective>
Apply the file-level SPDX license header convention plan 06-01 established, across every file
under src/, and make it a self-enforcing gate.

Purpose: D-035 requires "file-level license identifiers to distinguish Apache-derived files from
original MIT files" before the first public package. RESEARCH.md's own Assumption A3 flags the
src/platform.ts classification as a legal judgment call the maintainer should confirm rather than
one this plan should settle silently, which is why this plan opens with a decision checkpoint.
Output: every src/*.ts file carries the correct SPDX header; a per-file test gate enforces it.
</objective>

<execution_context>
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-validated-release-candidate/06-RESEARCH.md
@.planning/phases/06-validated-release-candidate/06-PATTERNS.md
@test/accessories/hapImportScope.test.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Confirm the src/platform.ts license classification</name>
  <decision>Classify src/platform.ts as Apache-2.0 (modified template material) or MIT (original work) for its SPDX header</decision>
  <context>
    RESEARCH.md's License Boundary finding: src/platform.ts traces its lineage to the Homebridge
    plugin template's platform file (D-035's default reading) but has grown from ~150 to ~625
    lines (600 insertions, 125 deletions since the template import). D-035 itself says "if the
    implementation replaces all material from the template, reexamine the Apache preservation
    requirement." A licensing statement on a file that is later published under an explicit SPDX
    identifier is not something a future edit can cleanly retract for copies already distributed
    under it — RESEARCH.md's own Assumption A3 flags this as a legal judgment call, not a
    technical one, with "medium" risk if wrong, so this plan asks rather than picks silently.
  </context>
  <options>
    <option id="apache">
      <name>Keep src/platform.ts Apache-2.0 (modified template material)</name>
      <pros>The safer, RESEARCH.md-recommended reading: the file is still the DynamicPlatformPlugin implementation the template scaffolded and has not replaced all its material; matches D-035's default</pros>
      <cons>Carries forward an Apache attribution on a file that is now over 80% new code</cons>
    </option>
    <option id="mit">
      <name>Reclassify src/platform.ts as MIT (original work)</name>
      <pros>Reflects the 600/725-line rewrite more literally</pros>
      <cons>Departs from D-035's own default and from RESEARCH.md's explicit recommendation; requires an affirmative reexamination finding, not just a line count</cons>
    </option>
  </options>
  <resume-signal>Select: apache or mit. If unattended, apache is taken as the documented default per RESEARCH.md's recommendation, and the SUMMARY records that this default was taken rather than chosen.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apache-2.0 headers on the three template-derived files</name>
  <reversibility rating="one-way">A licensing statement on a file that is later published under an explicit SPDX identifier is not something a future edit can cleanly retract for copies already distributed under it — RESEARCH.md's own Assumption A3 flags this as a legal judgment call, not a technical one, with "medium" risk if wrong.</reversibility>
  <files>src/index.ts, src/settings.ts, src/platform.ts</files>
  <read_first>src/index.ts, src/settings.ts, src/platform.ts, LICENSE, NOTICE</read_first>
  <action>
    Add, as the first lines of each file (before any existing content, including any existing
    JSDoc), a two-line license header: a comment reading exactly "SPDX-License-Identifier:
    Apache-2.0" followed by a comment reading "Modified from the Homebridge plugin template
    (https://github.com/homebridge/homebridge-plugin-template)." Apply this to src/index.ts and
    src/settings.ts unconditionally, and to src/platform.ts per Task 1's recorded decision — if
    the maintainer chose MIT instead, apply the MIT header from Task 3 to src/platform.ts instead
    of this Apache header, and remove it from this task's file list in the SUMMARY. Use `//`
    line comments, not JSDoc, so no documentation tool mistakes the header for API documentation.
  </action>
  <verify>
    <automated>O=$(npm run build:test 2>&1); R=$?; test $R -eq 0</automated>
    <fails_when>non-zero exit (a malformed header broke the TypeScript build)</fails_when>
  </verify>
  <done>src/index.ts and src/settings.ts (and src/platform.ts, unless Task 1 chose MIT) each open with the Apache-2.0 SPDX line and the template-modification note.</done>
</task>

<task type="auto">
  <name>Task 3: MIT headers on every other src file, and the per-file gate</name>
  <files>src/accessories/alarmMute.ts, src/accessories/basementGuardian.ts, src/accessories/controls.ts, src/accessories/customCharacteristics.ts, src/accessories/customServices.ts, src/accessories/pumpRecords.ts, src/accessories/reconciliation.ts, src/accessories/serviceCatalogue.ts, src/accessories/services.ts, src/accessories/staleMarking.ts, src/cloud/api.ts, src/cloud/auth.ts, src/cloud/errors.ts, src/cloud/mqttTransport.ts, src/cloud/shadow.ts, src/cloud/sigv4.ts, src/cloud/types.ts, src/config.ts, src/device/events.ts, src/device/family.ts, src/device/gemini.ts, src/device/halo.ts, src/device/health.ts, src/device/registry.ts, src/device/state.ts, src/device/waterLevel.ts, src/logging.ts, src/persistence/accessoryContext.ts, src/protocol.ts, src/runtime/accessoryStore.ts, src/runtime/accountRuntime.ts, src/runtime/arrivalAnchors.ts, src/runtime/clock.ts, src/runtime/commandPort.ts, src/runtime/failureLog.ts, src/runtime/monitoringHealth.ts, src/runtime/monotonicClock.ts, src/runtime/retryPolicy.ts, src/runtime/timers.ts, test/packaging/licenseHeaders.test.ts</files>
  <read_first>test/accessories/hapImportScope.test.ts, test/packaging/licenseHeaders.test.ts</read_first>
  <action>
    Add, as the first line of each of the 39 files listed (every src/*.ts file except
    index.ts/settings.ts/platform.ts from Task 2), a single `//` comment reading exactly
    "SPDX-License-Identifier: MIT" — no JSDoc, no template note.

    Extend test/packaging/licenseHeaders.test.ts with the per-file gate. Copy
    typeScriptFilesUnder() verbatim from test/accessories/hapImportScope.test.ts, adjusting only
    the directory list to ['src'] and recomputing REPOSITORY_ROOT's ".." depth for this file's own
    location. Declare a module-level constant APACHE_DERIVED_FILES listing 'src/index.ts',
    'src/settings.ts', and 'src/platform.ts' (or, if Task 1 chose MIT for platform.ts, only the
    first two). Declare SRC_FILE_FLOOR = 42 (this repository's current src/*.ts count) and assert
    the enumeration finds at least that many files, mirroring hapImportScope.test.ts's own floor
    assertion. Add one test asserting every file in APACHE_DERIVED_FILES matches both
    /SPDX-License-Identifier:\s*Apache-2\.0/ and /Modified from the Homebridge plugin template/.
    Add a second test asserting every OTHER enumerated src file matches
    /SPDX-License-Identifier:\s*MIT/ and does not match /SPDX-License-Identifier:\s*Apache-2\.0/.
  </action>
  <verify>
    <automated>O=$(npm run build:test 2>&1 && node --test dist-test/test/packaging/licenseHeaders.test.js 2>&1); R=$?; test $R -eq 0 && printf '%s\n' "$O" | grep -qE '^ℹ fail 0$'</automated>
    <fails_when>non-zero exit, or no line reading exactly "ℹ fail 0" appears in the output</fails_when>
    <automated>O=$(npm run check 2>&1); R=$?; test $R -eq 0</automated>
    <fails_when>non-zero exit (lint, format, fallow, typecheck, or the full test suite regressed from the new headers)</fails_when>
  </verify>
  <done>All 39 non-template src files open with the MIT SPDX line; the extended gate in test/packaging/licenseHeaders.test.ts passes and fails if a header is removed or misclassified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source file -> distributed package | The header text a downstream consumer of the published package reads to know their rights |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Repudiation | src/*.ts SPDX headers | medium | mitigate | test/packaging/licenseHeaders.test.ts's per-file gate fails if a header is removed, changed, or misclassified after this plan lands |
| T-06-06 | Tampering | src/platform.ts classification | medium | mitigate | Task 1's checkpoint:decision surfaces the ambiguity to the maintainer instead of a subagent silently picking a classification |

</threat_model>

<verification>
- `npm run check` passes with all 42 src files carrying a header.
- test/packaging/licenseHeaders.test.ts's per-file assertions pass and are proven non-vacuous by the file-count floor.
</verification>

<success_criteria>
Every src/*.ts file carries the correct SPDX header and a test gate enforces the classification.
</success_criteria>

<output>
Create `.planning/phases/06-validated-release-candidate/06-02-SUMMARY.md` when done
</output>
