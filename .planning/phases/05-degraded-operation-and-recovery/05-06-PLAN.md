---
phase: 05-degraded-operation-and-recovery
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - features/support/steps/homekit.ts
  - src/accessories/serviceCatalogue.ts
  - src/accessories/basementGuardian.ts
  - test/accessories/serviceCatalogue.test.ts
  - test/accessories/basementGuardian.test.ts
  - features/degradedOperation.feature
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [RES-03, RES-04]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low
  # `estimate-calibration`: factor 1.00, applied false, sample_count 0, min_samples 3. Confidence is
  # derived from the sample count, never self-rated. Projected on the read-set scale the five
  # executed plans of this phase used, so it is comparable with them; all five recorded realized
  # diffs an order of magnitude smaller on the chars/4 scale, and that mismatch is a measurement
  # difference rather than a five-fold over-estimate. Inside the 100000 smart-zone budget.

must_haves:
  truths:
    - "A poll that arrives on a working transport during a monitoring outage reaches HomeKit. A flooded pit reported by a healthy REST poll while the shadow is silent reads `Leak Detected = 1` on the tile, and `Status Active` alone carries the doubt (ROADMAP SC-2, RES-03, D-01, D-02, D-04)."
    - "A trust withdrawal retains every published value and changes only whether the plugin vouches for it. This restates 05-01's truth of the same words, which the shipped code did not do: the withdrawal also stopped the scope publishing, so fresh valid values from a working transport were discarded (PROJECT D-014, D-02)."
    - "A value the plugin never received is never published. A row publishes what arrived and nothing else, so with both transports down nothing new arrives and every tile holds the value the last trustworthy observation left, marked (PROJECT D-014)."
    - "Invalid data still withholds. A field that failed family validation, and a scope poisoned by a lost controller link, publish nothing at all, exactly as they do today; only `unreachable` marks without withholding (PROJECT D-014, RES-01, RES-02)."
    - "Under a total transport blackout nothing reads as trustworthy, `Pump Controller Link Lost` included. The monitoring cause is applied before the controller-link cause, so `unreachable` — which no row tolerates — beats `controller-link-lost`, which one row does, while `invalid` still beats both (ROADMAP SC-2, D-02, RES-02)."
    - "Every scope a blackout costs reports `unreachable`, the cause that actually happened, rather than `controller-link-lost`. `TRUST_SCOPES` at `src/accessories/basementGuardian.ts:181` has eight members, so a blackout withdraws all eight; the set the controller-link layer would otherwise claim is `NON_CONNECTIVITY_SCOPES`, which is every scope but `connectivity` and therefore seven (D-03, RES-03)."
    - "A monitoring outage activates no safety adapter. With both transports down and the controller link intact, neither `Basement Guardian Offline` nor `Pump Controller Link Lost` becomes activated by the outage itself: each keeps the verdict its last trustworthy observation produced, marked. This is the property probe P3 recorded, and after the publishing predicate widens it needs an assertion of its own rather than the side effect that used to hold it (ROADMAP SC-2, RES-03)."
    - "A press is still refused while a transport is down. The write gate reads the withdrawn scopes directly and is not routed through the publishing predicate, so a monitoring outage that keeps values flowing still leaves the plugin unable to vouch for the device's current state (D-07, RES-04)."
    - "A Cucumber step claiming a service answers a read fails when the characteristic is absent, so no scenario can pass on an absence (RES-04, D-09)."
    - "`REQUIREMENTS.md` stops claiming RES-04 is complete while two of its four clauses are unmet (RES-04)."
  artifacts:
    - path: src/accessories/serviceCatalogue.ts
      provides: "isRowPublishable, the predicate that decides whether a row may publish what arrived, separate from the predicate that decides whether the plugin vouches for it"
      contains: "isRowPublishable"
    - path: src/accessories/basementGuardian.ts
      provides: "distrustReasonsOf with the monitoring cause applied ahead of the controller-link cause"
      contains: "distrustReasonsOf"
    - path: features/support/steps/homekit.ts
      provides: "readOutcome, the three-way answer that tells an absent characteristic from one that answers and one that refuses"
      contains: "readOutcome"
    - path: features/degradedOperation.feature
      provides: "The scenario that polls a changed, flooded payload during shadow silence, and the scenario that puts the controller-link row under a total blackout"
      contains: "flooded"
  key_links:
    - from: src/accessories/basementGuardian.ts
      to: src/accessories/serviceCatalogue.ts
      via: "the monitoring cause reaches every row as an untrusted scope carrying reason unreachable, which the publishing predicate tolerates and the vouching predicate does not"
      pattern: "isRowPublishable"
    - from: src/accessories/serviceCatalogue.ts
      to: features/degradedOperation.feature
      via: "a changed flooded payload polled during shadow silence is asserted on the Leak Detected characteristic, not only on Status Active"
      pattern: "Leak Detected"
  prohibitions:
    - statement: "MUST NOT let `invalid` or `controller-link-lost` start publishing. Those two reasons say the value itself is doubtful, and publishing a value that failed family validation is the false normal the whole plugin exists to prevent. Only a reason that means the plugin is seeing less is exempted, and today that is exactly one reason."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT route the control write gate through the publishing predicate. `reportedControlValue` withholds a control's reported value from the binder whenever that capability's scope is untrusted, which is what refuses a press while the plugin cannot vouch for the device's current state. Widening it in step with the publishing predicate would let a press leave the plugin during a transport outage."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT make `Status Active` tolerate the monitoring cause. `Status Active` is the one signal carrying the doubt, and a row that both publishes and calls itself active during an outage reports a false normal with no marker at all."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT add `stale` to the exempted reasons. No production code assigns it; the type declares it and nothing produces it. Exempting a reason nothing produces buys nothing and would silently change behaviour the day something starts producing it."
      status: flagged-unverified
      verification: test
    - statement: "MUST NOT weaken a scenario to make it pass. Every assertion added here names a value an owner reads off the tile; replacing one with an existence check reproduces the shape that let three blockers ship green."
      status: flagged-unverified
      verification: manual
---

<objective>
Separate "the plugin cannot vouch for this scope" from "the plugin must not publish this scope", so
a monitoring outage marks what it can no longer watch instead of discarding what is still arriving.
Then make the two causes compose in the order that keeps a blind plugin from reading trustworthy.

Purpose: `05-VERIFICATION.md` CR-01 reproduced, twice and independently, that with the shadow silent
and REST healthy a poll reporting `water_level: 31` produces `leakDetected = 0`. The reading is
decoded, validated by the family, and thrown away. Apple Home draws no `Status Active` row on a
tile, so the owner sees "no leak" while the plugin holds "leak", and no automation fires. That is
the exact false normal this project exists to prevent, and it fails ROADMAP success criterion 2 on
its own. WR-01 fails the same criterion by the other door: with the controller link lost and both
transports down, `Pump Controller Link Lost` still publishes an activated sensor with
`Status Active = true`, so a device adapter reads trustworthy while the plugin is blind.

Output: a publishing predicate distinct from the vouching predicate, a reason precedence that puts
the broader cause first, a Cucumber read step that cannot pass on an absent characteristic, and
assertions written as what a basement owner must observe.
</objective>

## The `D-014` reading this plan acts on

`PROJECT.md` `D-014` is ADR-locked and reads, in full:

> **D-014 — Preserve untrusted state:** Retain the last family-valid values through invalid data or
> communication loss, mark only the truthful scope faulty/inactive, and require fresh valid input
> for recovery.

**This plan does not amend, narrow, or reinterpret `D-014`, and inserts no decision checkpoint.**
The argument is from `D-014`'s own three clauses:

1. **"Retain the last family-valid values."** Withholding is the *mechanism* by which retention is
   achieved when the arriving value is bad: publishing a field that failed family validation would
   overwrite a retained good value with a bad one, so the row must publish nothing. That mechanism
   serves the clause. It does not serve it when the arriving value is family-valid and was delivered
   by a transport that is working — withholding then retains nothing, because nothing was
   threatening the retained value. It replaces a fresh valid reading with a staler valid one.
2. **"Mark only the truthful scope faulty/inactive."** The instruction for a scope the plugin cannot
   vouch for is to *mark* it. `D-014` names marking, and names it as the whole of the response
   beside retention. It nowhere says discard.
3. **"Require fresh valid input for recovery."** Under the shipped behaviour fresh valid input
   arrives, is validated, and is ignored. The clause is not merely unserved; it is inverted.

So the reading is that `D-014` always meant *preserve the value, mark the scope*, and the
implementation over-applied its withholding mechanism to a cause `D-014` never covered.

The locked phase decision says the same thing twice, in its own words. `05-CONTEXT.md` `D-02` states
that a monitoring degradation "marks every scope on every accessory" and then, in the same
paragraph: **"Preserve-and-mark still holds: values are retained, only trust is withdrawn."** And
`D-02`'s narrowing note of 2026-09-01 already identified `toRow` returning `[]` for an untrusted row
as a defect — it recorded that applying `D-02` literally "would silence `Basement Guardian Offline`"
because "`toRow` returns `[]` for an untrusted row". The narrowing worked around that by shrinking
the withdrawn scope set rather than by fixing the predicate that discards the values. This plan fixes
the root of a problem the locked decision had already named and routed around. `05-01-PLAN.md`'s own
prohibition said the same in as many words: "preserve-and-mark must not become preserve-and-hide".
All three are served by this change rather than bent by it.

One structural guarantee makes the narrower predicate safe rather than merely defensible: **a row
can only publish a value some transport delivered.** The projection reads the last decoded snapshot,
and a snapshot exists only because a poll or a shadow message carried it. With both transports down
nothing new arrives, so "publish what arrived" cannot publish anything from a dead transport — every
tile holds exactly the value the last trustworthy observation left, marked. The change moves which
values reach HomeKit only in the case where a transport is still delivering them.

The change is `reversible`: it is one predicate, added beside the existing one, and reverting it
restores today's behaviour by pointing two call sites back at the old name.

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-degraded-operation-and-recovery/05-CONTEXT.md
@.planning/phases/05-degraded-operation-and-recovery/05-PATTERNS.md
@.planning/phases/05-degraded-operation-and-recovery/05-VALIDATION.md
@.planning/phases/05-degraded-operation-and-recovery/05-VERIFICATION.md
@.planning/phases/05-degraded-operation-and-recovery/05-REVIEW.md
@.planning/phases/05-degraded-operation-and-recovery/05-01-SUMMARY.md
@src/accessories/serviceCatalogue.ts
@src/accessories/basementGuardian.ts
@src/device/health.ts
@features/support/steps/homekit.ts
@features/support/publishedServices.ts
@features/degradedOperation.feature
@features/CLAUDE.md
@.claude/rules/typescript-style-guide.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md

<interfaces>
<!-- Every line below was read from the working tree on 2026-09-02, not inherited from a summary. -->

The two predicates as they stand, and their three call sites:
```typescript
// src/accessories/serviceCatalogue.ts:214-215
export function isRowTrusted(row: RowTrust, untrustedScopes: readonly UntrustedScope[]): boolean {
  return !untrustedScopes.some((untrusted) => untrusted.scope === row.scope && !row.toleratedDistrust.includes(untrusted.reason));
}

// src/accessories/serviceCatalogue.ts:230-231 — what `Status Active` reports
export function isRowFullyTrusted(row: ServiceRow, untrustedScopes: readonly UntrustedScope[]): boolean {
  return row.readScopes.every((scope) => isRowTrusted({ scope, toleratedDistrust: row.toleratedDistrust }, untrustedScopes));
}

// src/accessories/serviceCatalogue.ts:279 — inside trustedGroup, the per-scope-group narrowing
if (!isRowTrusted({ scope, toleratedDistrust: trust.toleratedDistrust }, input.untrustedScopes)) {

// src/accessories/serviceCatalogue.ts:597 — inside toRow, the whole withholding mechanism
return isRowTrusted(this, input.untrustedScopes) ? values(input, this) : [];
```

The reason vocabulary. `stale` is declared and nothing in `src/` assigns it:
```typescript
// src/device/health.ts:36
export type DistrustReason = 'stale' | 'unreachable' | 'invalid' | 'controller-link-lost';
```

The layering as it stands, controller link before monitoring:
```typescript
// src/accessories/basementGuardian.ts:323-345
function distrustReasonsOf(
  violated: ReadonlySet<TrustScope>,
  controllerLinkLost: boolean,
  monitoringDegraded: ReadonlySet<TrustScope>,
): ReadonlyMap<TrustScope, DistrustReason> {
  const reasons = reasonsOf(violated, 'invalid');

  if (controllerLinkLost) {
    for (const scope of NON_CONNECTIVITY_SCOPES) {
      if (!reasons.has(scope)) {
        reasons.set(scope, 'controller-link-lost');
      }
    }
  }

  for (const scope of monitoringDegraded) {
    if (!reasons.has(scope)) {
      reasons.set(scope, 'unreachable');
    }
  }

  return reasons;
}
```

The one row that tolerates the controller-link cause, which is why the order matters:
```typescript
// src/accessories/serviceCatalogue.ts:741-748
kind: 'pump-controller-link-lost',
displayName: 'Pump Controller Link Lost',
scope: 'fault',
toleratedDistrust: ['controller-link-lost'],
```

The write gate this plan must leave alone — it reads the withdrawn scopes directly and never through
a catalogue predicate:
```typescript
// src/accessories/basementGuardian.ts:517-526
function reportedControlValue(control: ControlDefinition): boolean | undefined {
  if (untrusted.some((scope) => scope.scope === control.capability)) {
    return undefined;
  }
  ...
}
```

The step that passes on an absence, and the predicate it inverts:
```typescript
// features/support/steps/homekit.ts:130-160
function readThrows(service: FakeHapService | undefined, displayName: string): boolean {
  const characteristic = service?.characteristics.find((candidate) => candidate.displayName === displayName);

  if (characteristic === undefined) {
    return false;          // absent reads as "did not throw"
  }
  ...
}
// assertNoReadAnswered waits for readThrows === true   (correct: an absence fails by name)
// assertReadAnswered  waits for readThrows === false    (wrong: an absence passes)
```

The comment that now states the opposite of what the code does (WR-04):
```
// src/accessories/basementGuardian.ts:681-686, above republishPublishedRows at :694
//   "The connectivity row is the one this leaves alone, because nothing about it
//    stopped being knowable ... it keeps publishing its current verdict and stays
//    active -- which is what `untrusted` has always reported for that scope"
// True when the only caller was the unresolved-family branch. `markMonitoring` (:798) is now a
// second caller, and a REST degradation puts `connectivity` into `untrusted` with reason
// `unreachable`, which the phase's own scenario "Polling failure alone leaves the live values
// trustworthy" asserts goes inactive.
```

Introduced by this plan:
```typescript
// src/accessories/serviceCatalogue.ts (NEW export, beside the existing predicate)
export function isRowPublishable(row: RowTrust, untrustedScopes: readonly UntrustedScope[]): boolean;

// features/support/steps/homekit.ts (NEW, replacing the inverted predicate)
type ReadOutcome = 'answered' | 'refused' | 'absent';
function readOutcome(service: FakeHapService | undefined, displayName: string): ReadOutcome;
```
</interfaces>
</context>

## Artifacts this phase produces

This plan produces `isRowPublishable` on `src/accessories/serviceCatalogue.ts`, the reordered
`distrustReasonsOf` on `src/accessories/basementGuardian.ts`, `readOutcome` on
`features/support/steps/homekit.ts`, and two scenarios in `features/degradedOperation.feature`. It
publishes no new HomeKit identity, adds no characteristic, adds no configuration key, and installs no
package. Plans 05-07 through 05-10 produce the rest of this gap-closure round; the complete
phase-wide artifact list from the first round is in `05-01-PLAN.md`.

## The rule every new assertion in this round obeys

`05-VALIDATION.md` recorded that every behaviour in the first round had its named mutation applied,
watched to fail, and reverted — and CR-01 shipped anyway. The mutations tested whether the tests
could detect the *implemented* behaviour changing, not whether the implemented behaviour was
*correct*. `retains every published value across a lost monitoring path and moves the trust flag
alone` is a well-written test of the wrong question: what it asserts is the defect's own signature.

So every assertion added in this round states **what a basement owner must observe**, and every
paired mutation is one a *correct* implementation survives and the current one fails. An assertion
that only says "nothing moved" is not acceptable evidence in this plan.

## WR-08, and why this plan closes it

`05-REVIEW.md` WR-08 reports that `runPoll` awaits `applyDevices()` before `recordPollSuccess()`, so
every `update()` in a poll runs against the previous tick's `MonitoringTrust`; a row whose service
was never created because its scope was untrusted at creation time therefore stays absent for one
extra poll after recovery, which at the 3600-second maximum is an hour.

This plan closes it, and the argument is checkable in three steps a reader can follow in the source:

1. **Only the monitoring cause arrives late.** `update()` recomputes `deviceDistrust` from the very
   snapshot it is applying (`basementGuardian.ts:866`, `violatedScopesOf(validation)` and
   `isControllerLinkLost(decoded)`) and then runs `publishRows` (`:901`), which is the pass that
   *adds* services through `ensureService`. So a scope recovering from `invalid` or from
   `controller-link-lost` recovers on the same tick and gains its service on that tick. The stale
   trust WR-08 names reaches the projection through `monitoringDegradedScopes(monitoring)` alone.
2. **The monitoring recovery is the one that cannot add a service.** `markMonitoring` (`:798`) is the
   only path carrying it, and it republishes through `republishPublishedRows`, which walks
   `publishedService()` and adds nothing. That is the whole of WR-08's mechanism.
3. **After this plan the service already exists.** `ensureService` returns `undefined` only when the
   row projected nothing and is not `alwaysPublish` (`serviceCatalogue.ts:875-883`). A row untrusted
   for the monitoring cause alone now projects its retained values, so its service is created by the
   poll that arrives *during* the outage — before the recovery, not after it. `republishPublishedRows`
   then finds it and pushes the restored `StatusActive` on the recovery tick.

So the fix is real rather than incidental, and it is asserted rather than assumed: task 2 carries a
behaviour requiring the row's service to exist while the outage is in force. WR-08's ordering in
`runPoll` is left as it is, because with that behaviour holding it costs nothing observable.

## Review findings this round does not act on

Recorded here so `/gsd-execute-phase` sees a disposition for every current `05-REVIEW.md` finding
rather than an absence. Neither is a task; neither is silently dropped.

**WR-05 — the nine-member `DiscoveryContext` literal written three times
(`src/platform.ts:496-545`). Deferred.** Plan 05-09 does open `platform.ts`, but at
`configureAccessory`, not at the three literals in the launch closure. Folding a composition-root
refactor into a plan whose subject is a restored control's refused press would put an unrelated
structural change in the same commit range as four safety fixes, which is the shape that makes a
bisect useless. The drift WR-05 warns about — a member whose *value* is wrong in one of the three
copies — has not happened: the reviewer read all three and found them byte-identical. Deferred to a
change that owns the composition root.

**IN-03 — shadow silence measured against a wall clock that can jump
(`src/runtime/monitoringHealth.ts:123-125`). Deferred.** The finding asks for a note in the intel
document, not a code change, and says so itself: "No change required for this release." No plan in
this round owns `.planning/intel/`, and this round's file list is the source and test surface of the
named gaps plus the two planning documents. Deferred to the next intel refresh, where a
monotonic-clock decision would have the reason recorded beside the other clock constraints.

<tasks>

<task type="auto">
  <name>Make the read step tell an absent characteristic from one that answers</name>
  <read_first>
    `features/support/steps/homekit.ts` in full — `characteristicValue` and the `pushedValue`
    resolution it goes through, `readThrows` with the comment explaining why an absence deliberately
    reads as "did not throw" for the negative step, `assertNoReadAnswered`, `assertReadAnswered`, and
    the `PUBLISH_DEADLINE_MS` / `STEP_TIMEOUT_MS` deadline discipline at the head of the file.
    `features/support/publishedServices.ts` — how a service is resolved by display name and how
    `pushedValue` tells a published value from a HAP format default.
    `features/support/fakeHap.ts` `handleGetRequest` — what a read does when a status is stored and
    when it is not.
    `features/degradedOperation.feature` lines 185-200, the scenario resting on this step.
    `05-REVIEW.md` WR-06.
  </read_first>
  <files>features/support/steps/homekit.ts</files>
  <action>
    Replace the two-valued predicate with a three-valued one. Name the type `ReadOutcome` with the
    members `answered`, `refused` and `absent`, and have one function `readOutcome` resolve the
    characteristic once and answer which of the three happened: no characteristic at all is
    `absent`; a `handleGetRequest` that throws is `refused`; anything else is `answered`.

    Point both steps at it. `Then the {string} service answers no read for {string}` waits for
    `refused`, which keeps today's behaviour of failing by name on an absence rather than passing on
    it. `Then the {string} service answers a read for {string}` waits for `answered`, so an absent
    characteristic now fails.

    Give each step a failure message that says which of the three outcomes it saw, so a scenario that
    fails here tells the reader whether the characteristic was missing or refusing. The message
    currently says only that the service "stopped answering a read", which reads as a refusal and is
    exactly the wrong diagnosis in the absent case this task exists to catch.

    Keep the comment above the function, and rewrite it: it currently justifies the two-valued
    inversion, and leaving that justification beside a three-valued answer would leave a false
    statement in a file this repository treats as load-bearing.
  </action>
  <verify>
    <automated>npm run test:cucumber -- --name "A transport outage leaves every service readable"</automated>
    <fails_when>The command exits non-zero, or its output does not contain the line `1 scenario (1 passed)`.</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm run test:cucumber` runs all 89 existing scenarios green with the rewritten steps.
    - Named mutation, applied and watched: point the positive step at a characteristic the service does not carry — add `Then the "Sump Pit Flood" service answers a read for "Water Level"` to the transport-outage scenario — and confirm that step fails, naming the absent outcome. Revert the added line and confirm green. Record the failure message in the summary.
    - The `Sump Pit Level` service is where `Water Level` lives, so the planted line asserts an absence rather than a typo; confirm that by reading the catalogue before applying the mutation.
  </acceptance_criteria>
  <done>No scenario in this repository can pass a read assertion on a characteristic that does not exist.</done>
</task>

<task type="tracer" tdd="true">
  <name>End-to-end: a flooded pit polled while the live path is silent reaches Apple Home</name>
  <read_first>
    `src/accessories/serviceCatalogue.ts` — the `RowTrust` and `ServiceRow` declarations, both
    predicates at :206-231 with their full doc comments, `trustedGroup` at :279, `toRow` at :584-600,
    `published()` and how it drops `undefined` candidates, `controlValues` at :490-494 and its comment
    on withholding `On`, `controllerLinkValues` at :562-573 and `offlineValues` at :579-581 — the two
    adapters the blackout behaviour is about, both of which derive their activation from a decoded or
    counted value rather than from the trust state — `ensureService` at :875-883, whose empty-projection
    gate is why a widened predicate is what creates the service during an outage, and `publishValue`
    at :954.
    `src/accessories/basementGuardian.ts` — `monitoringDegradedScopes` at :306, `publishRow` at :573,
    `publishRows` at :642, `republishPublishedRows` at :694, `markMonitoring` at :798.
    `test/accessories/serviceCatalogue.test.ts` — the existing cases over both predicates, so the new
    ones match the file's `// arrange` `// act` `// assert` bands and its fake-HAP setup.
    `features/degradedOperation.feature` lines 34-54, the scenario this task strengthens, and the
    `Given these devices` / `When the vendor changes these device fields` steps it will use.
    `src/device/gemini/` — the water-level code that means a flooded pit, so the payload the scenario
    polls is the family's own flood code rather than a number chosen here.
    `PROJECT.md` `D-014`; `05-CONTEXT.md` D-01, D-02, D-03, D-04; `05-REVIEW.md` CR-01.
  </read_first>
  <files>src/accessories/serviceCatalogue.ts, test/accessories/serviceCatalogue.test.ts, features/degradedOperation.feature</files>
  <behavior>
    - A row whose scope carries the monitoring cause publishes every value it can project, and the row's `Status Active` reads false at the same time.
    - A row whose scope carries the validation cause publishes nothing, exactly as today, and its `Status Active` reads false.
    - A row whose scope carries the controller-link cause publishes nothing unless it tolerates that cause, exactly as today.
    - A row carrying both the validation cause and the monitoring cause on the same scope publishes nothing: the stricter cause wins, because the value itself is doubtful.
    - A row that reads a second scope group publishes that group while the group's scope carries only the monitoring cause, and withholds it when the group's scope failed validation.
    - `Status Active` is unchanged for every combination above: it counts the monitoring cause as untrusted, so a row that is publishing under an outage never calls itself active.
    - A row with an empty tolerated list and no untrusted scope publishes and reads active, unchanged.
    - With every scope carrying the monitoring cause, and the last trustworthy snapshot reporting a present controller link and a device that is not offline, neither `Pump Controller Link Lost` nor `Basement Guardian Offline` projects an activated contact. Both project the verdict they held, marked. The outage marks; it raises no alarm, and no safety adapter is activated by the monitoring failure itself.
    - A row untrusted for the monitoring cause alone projects a non-empty value list, so `ensureService` creates its service during the outage rather than leaving it absent until trust returns (WR-08).
    - End to end: with the shadow silent and REST healthy, a poll carrying the family's flood code activates `Sump Pit Flood` in HomeKit while that service's `Status Active` reads false.
  </behavior>
  <action>
    Add one exported predicate beside `isRowTrusted` rather than changing what `isRowTrusted` means.
    Name it `isRowPublishable`. It answers whether the row may publish what arrived: a scope is
    disqualifying only when its reason is neither tolerated by the row nor one of the reasons that
    mean the plugin is seeing less rather than that the value is doubtful. Declare that exempt set
    once, as a module constant holding exactly `unreachable`, and give it a doc comment stating the
    reasoning above: a monitoring outage says the plugin is seeing less, never that the value it did
    receive is doubtful, and a row can only publish a value some transport delivered.

    Point exactly two call sites at the new predicate — the projection inside `toRow` and the
    per-scope-group narrowing inside `trustedGroup`. Leave `isRowFullyTrusted` reading the existing
    predicate, unchanged and un-narrowed, because it is what `Status Active` reports and the marker
    must keep counting the outage. Rewrite `isRowTrusted`'s doc comment, which today claims its one
    rule drives both halves of the safety contract; that is now two rules and the comment must say
    which is which and why they differ.

    Then strengthen the existing scenario `Shadow silence withdraws trust while polling continues`
    and add the new one. The existing scenario polls unchanged telemetry after a dry reading and
    asserts the flood sensor is not activated, which passes whether or not the value flows; keep it
    and add the changed-payload assertions to it, or add a second scenario named
    `A flooded pit reaches Apple Home while the live path is silent` — either way one scenario must:
    start the plugin, let the device publish a heartbeat, move the scenario clock past two missed
    heartbeats so the live path is silent, assert the flood service reports `Status Active` as
    `false`, then have the vendor change the device fields to the family's flood water-level code,
    and assert the `Sump Pit Flood` sensor is activated while its `Status Active` still reads
    `false`. Read the flood code out of the Gemini family rather than writing a literal chosen here.

    Existing unit cases that assert a monitoring-degraded row publishes nothing are now asserting the
    defect. Update each to assert what the owner must observe — the value flows and the marker is
    false — and say in the summary which cases moved. Do not touch a case asserting withholding under
    the validation cause or under the controller-link cause; those two are unchanged and a case that
    starts passing for a new reason is worse than one that fails.
  </action>
  <verify>
    <automated>npm run test:coverage:direct -- dist-test/src/accessories/serviceCatalogue.js dist-test/test/accessories/serviceCatalogue.test.js</automated>
    <fails_when>The command exits non-zero, or its output reports a line, branch, or function coverage figure below 100 for `dist-test/src/accessories/serviceCatalogue.js`.</fails_when>
    <automated>npm run test:cucumber -- --name "flooded pit reaches Apple Home"</automated>
    <fails_when>The command exits non-zero, or its output does not contain the line `1 scenario (1 passed)`.</fails_when>
  </verify>
  <acceptance_criteria>
    - With the shadow silent and REST healthy, a poll carrying the flood code produces `Leak Detected = 1` in HomeKit and `Status Active = false` on the same service. This is the criterion; it is stated as what the owner sees.
    - Named mutation, applied and watched: make the projection read the un-narrowed predicate again, so a monitoring-degraded row projects nothing. The new scenario must fail on the `Sump Pit Flood` sensor never activating. Revert and confirm green.
    - Named mutation, applied and watched: add the validation cause to the exempt set. A unit case must fail on a row publishing a field that failed family validation. Revert and confirm green.
    - Named mutation, applied and watched: add the controller-link cause to the exempt set. A unit case must fail on a fault-scope row publishing controller-derived state while the link is lost. Revert and confirm green.
    - With every scope carrying the monitoring cause, the link present and the device not offline, neither `Pump Controller Link Lost` nor `Basement Guardian Offline` projects an activated contact. Named mutation, applied and watched: make `controllerLinkValues` derive its activation from the row's trust — activate whenever the row is not fully trusted — instead of from the decoded `controllerLinkPresent`. That case must fail on the link adapter activating under an outage that said nothing about the link. Revert and confirm green.
    - The row untrusted for the monitoring cause alone has a service after the outage poll, not only after recovery (WR-08). Assert the service exists while the outage is in force.
    - `npm run test:cucumber` is green across all scenarios, and `npm run test:unit` is green.
    - Every mutation above, and the exact assertion each one broke, are recorded in the summary.
  </acceptance_criteria>
  <reversibility rating="reversible">One predicate added beside the existing one and two call sites repointed; reverting restores today's behaviour by name.</reversibility>
  <done>A flooded reading delivered by the transport that is still working reaches the tile, and the tile says the plugin cannot vouch for it.</done>
</task>

<task type="auto" tdd="true">
  <name>Put the broader cause first, so nothing reads trustworthy while the plugin is blind</name>
  <read_first>
    `src/accessories/basementGuardian.ts` :181 (`TRUST_SCOPES`, the eight-member constant every count
    in this task is derived from), :185-220 (the rest of the scope sets, including the
    `NON_CONNECTIVITY_SCOPES` comment at :198-206 whose "same five" is a count of a seven-member set),
    :306-320 (`monitoringDegradedScopes` and its comment on which transport costs which scopes),
    :323-345 (`distrustReasonsOf` and the comment claiming no cause overwrites another), :348-365
    (`untrustedScopesOf`), :517-526 (`reportedControlValue`, which this task must leave alone),
    :681-694 (the comment WR-04 names, above `republishPublishedRows`), :725-745
    (`reportDegradation` and `reportControllerLink`), :798-825 (`markMonitoring`, its four-member
    `unchanged` comparison and the store comment IN-02 names).
    `src/accessories/serviceCatalogue.ts` :741-748 — the one row with a non-empty tolerated list.
    `test/accessories/basementGuardian.test.ts` — the case at :2541 named
    `retains every published value across a lost monitoring path and moves the trust flag alone`,
    which asserts the defect's own signature, and the case at :2598 named
    `keeps a lost monitoring path withdrawn across the polls that arrive during it`, which polls
    unchanged telemetry. Read both in full before editing either.
    `05-REVIEW.md` WR-01 and WR-04; `05-VERIFICATION.md` probe P4; `05-CONTEXT.md` D-02, D-03.
    `.planning/REQUIREMENTS.md` lines 62-70 and 138-150.
  </read_first>
  <files>src/accessories/basementGuardian.ts, test/accessories/basementGuardian.test.ts, features/degradedOperation.feature, .planning/REQUIREMENTS.md</files>
  <behavior>
    - With the controller link lost and both transports down, every non-connectivity scope reports the monitoring cause and `Pump Controller Link Lost` reads `Status Active` as false.
    - With the controller link lost and both transports down, `Pump Controller Link Lost` still publishes the verdict the last trustworthy snapshot produced, because the value is retained rather than blanked.
    - With the controller link lost and both transports healthy, every non-connectivity scope reports the controller-link cause and the row reads `Status Active` as true, unchanged from today.
    - A scope whose own field failed validation reports the validation cause whichever of the other two causes is also in force.
    - A monitoring outage with the link intact reports the monitoring cause on the scopes that outage costs, and on no others.
    - The existing unit case about a lost monitoring path asserts that a value arriving during the outage reaches the accessory's published services, rather than that nothing moved.
    - A press on a control whose capability scope carries the monitoring cause is still refused, so the write gate did not widen with the publishing predicate.
  </behavior>
  <action>
    Move the monitoring loop above the controller-link loop in `distrustReasonsOf`, leaving the
    validation layer first. The monitoring cause is tolerated by no row and the controller-link cause
    is tolerated by one, so filling the monitoring scopes first is what stops that one row reading
    trustworthy while the plugin is blind, and it is also what makes every scope a blackout costs
    report the cause that actually happened. Derive the counts from `TRUST_SCOPES` at :181 rather than
    from any document: it has eight members, so a blackout withdraws eight and `NON_CONNECTIVITY_SCOPES`
    — the set the controller-link layer would otherwise claim — holds seven. Rewrite the comment above
    the function: it explains the precedence and must explain the new one, including why the broader
    cause of the two wins. While you are in that comment, note that the one above
    `NON_CONNECTIVITY_SCOPES` at :198-206 says a lost controller link "poisons the same five" of a set
    that holds seven; correct the number there from the constant rather than leaving a second wrong
    count standing beside the one this plan is fixing.

    Correct the comment above `republishPublishedRows` (WR-04). The sentence about the connectivity
    row was true when the unresolved-family branch was the only caller; the monitoring fan-out is now
    a second caller and a REST degradation does withdraw that scope, which the phase's own scenario
    asserts. Scope the existing sentence to the unresolved-family caller and state the monitoring
    caller's case beside it.

    Correct the comment inside `markMonitoring` at :813-818 in the same pass (IN-02). It is the same
    class of defect as WR-04 and sits about ten lines from code this task edits: it justifies the
    unconditional store by saying "a store skipped by an unchanged-looking report would leave it
    answering a fact the runtime has already superseded", which cannot happen, because the `unchanged`
    comparison directly above it covers all four members of `MonitoringTrust`, so the assignment can
    change nothing when `unchanged` is true. The placement is right; the stated reason is not. Restate
    it as what it actually buys: the store is unconditional so that a member added to `MonitoringTrust`
    and forgotten in the comparison still reaches the write path. Change no code here — this is the
    comment only, and the comparison stays as it is.

    Strengthen the two blind unit cases. The one at :2541 asserts that nothing moved, which is the
    defect's signature; rewrite it so a value that arrives during the outage is asserted to reach the
    service, with the marker false — and rename it if the name no longer says what it checks. The one
    at :2598 polls unchanged telemetry across the outage; give it telemetry that changes, so
    "withdrawn across the polls" is checked against a value that had somewhere to move.

    Add a scenario named `A blind plugin vouches for no controller-link verdict`: start the plugin
    with the device reporting the controller link lost, confirm the link sensor is activated, then
    take REST down and move the clock past two missed heartbeats, and assert the
    `Pump Controller Link Lost` service reports `Status Active` as `false` while the sensor is still
    activated. The second half is the preserve-and-mark half and must not be dropped.

    Finally, correct `.planning/REQUIREMENTS.md`. Return RES-04 to its pending state in both places
    it is recorded — the checkbox on its own line and its row in the coverage table — because two of
    its four clauses are unmet and a row claiming otherwise makes a later audit read a gap as
    shipped. Plan 05-10 restores it once the clauses hold. Change nothing about RES-01, RES-02 or
    RES-03.
  </action>
  <verify>
    <automated>npm run test:coverage:direct -- dist-test/src/accessories/basementGuardian.js dist-test/test/accessories/basementGuardian.test.js</automated>
    <fails_when>The command exits non-zero, or its output reports a line, branch, or function coverage figure below 100 for `dist-test/src/accessories/basementGuardian.js`.</fails_when>
    <automated>npm run test:cucumber -- --name "blind plugin vouches for no controller-link verdict"</automated>
    <fails_when>The command exits non-zero, or its output does not contain the line `1 scenario (1 passed)`.</fails_when>
  </verify>
  <acceptance_criteria>
    - Under a total transport blackout with the link already lost, an owner's `Pump Controller Link Lost` tile keeps the verdict it had and says the plugin cannot vouch for it. Nothing on the accessory reads trustworthy.
    - Named mutation, applied and watched: move the monitoring loop back below the controller-link loop. The new scenario must fail on `Status Active` still reading true. Revert and confirm green.
    - Named mutation, applied and watched: route `reportedControlValue` through the publishing predicate instead of reading the withdrawn scopes directly. A unit case must fail on a press being accepted during a monitoring outage. Revert and confirm green.
    - The comment above `republishPublishedRows` names both of its callers, and the sentence about the connectivity row is scoped to the unresolved-family caller rather than stated unconditionally. Read it back after editing and quote it in the summary.
    - The `markMonitoring` store comment (IN-02) gives a reason that holds under the comparison above it, and the comparison and the store are otherwise unchanged. Quote the new sentence in the summary beside the WR-04 one.
    - Every scope count stated in a comment in this file matches `TRUST_SCOPES`. Count the members rather than repeating a number from a document, and record in the summary which counts you corrected.
    - `.planning/REQUIREMENTS.md` no longer records RES-04 as complete, in either place.
    - `npm run check` passes.
  </acceptance_criteria>
  <done>Three causes compose in an order an owner can rely on, the two blind unit cases now check the value rather than its absence, and the requirement row agrees with what shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendor transport -> decoded snapshot -> HomeKit characteristic | The path a flood verdict travels. A break anywhere on it presents as a normal reading. |
| Distrust reason -> row projection | The one place where "cannot vouch" and "must not publish" are decided; conflating them is CR-01. |
| Cucumber step -> assertion | A step that passes on an absence turns a whole scenario into no evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-19 | Denial of Service (against the owner's own safety monitoring) | the row projection under a monitoring cause | critical | mitigate | The publishing predicate exempts only the reason that means the plugin is seeing less; the end-to-end scenario asserts a flooded reading on the `Leak Detected` characteristic, and the paired mutation restores the un-narrowed predicate and must break it. |
| T-05-20 | Spoofing (a device adapter asserting a device fact the plugin cannot observe) | `Pump Controller Link Lost` under a blackout | high | mitigate | The monitoring cause is applied first, so the one row with a tolerated cause stops reading active; asserted directly and pinned by the reordering mutation. |
| T-05-21 | Tampering (a validation failure publishing through the widened predicate) | the exempt reason set | high | mitigate | The set is one module constant holding exactly one reason, with a unit case that fails if the validation cause joins it. |
| T-05-22 | Elevation of Privilege (a press leaving the plugin while it cannot vouch for device state) | `reportedControlValue` | high | mitigate | The write gate reads the withdrawn scopes directly and is explicitly out of the change; a named mutation routes it through the publishing predicate and must break a unit case. |
| T-05-23 | Repudiation (a green scenario that examined nothing) | `assertReadAnswered` | medium | mitigate | The step distinguishes absent from answering and names which outcome it saw; the mutation plants an absent characteristic and watches the step fail. |
| T-05-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | This plan installs no package. `package.json` and `package-lock.json` are unchanged, so no `[ASSUMED]`, `[SUS]` or `[SLOP]` package is introduced and the phase's legitimacy audit is unaffected. |
</threat_model>

<verification>
Both focused source-test pairs reach 100% direct line, branch and function coverage. All Cucumber
scenarios pass, including the two added here, and the existing count of 89 has grown rather than
shifted. Every named mutation above has been applied, watched to fail its named assertion, and
reverted. `npm run check` and `npm run test:coverage:all` pass, and `npm run fallow` reports no new
finding — the one pre-existing clone-group finding on `features/support/steps/hap.ts` is expected and
is not a regression. Run the final gate on both installed Node versions: the default `node`
(v26.7.0) and `/usr/bin/node` (v22.22.2).
</verification>

<success_criteria>
A basement owner whose live connection has gone quiet while the vendor's REST service keeps answering
sees a flooded pit on the tile, with a row under Details saying the plugin cannot currently vouch for
it — instead of a tile that reads "no leak" while the plugin holds "leak". An owner whose plugin can
see nothing at all finds no service on the accessory claiming to be trustworthy.
</success_criteria>

<output>
Create `.planning/phases/05-degraded-operation-and-recovery/05-06-SUMMARY.md` when done.
Record: the exact wording of the `D-014` reading this plan acted on, including the `05-CONTEXT.md`
`D-02` corroboration, and that no checkpoint was raised; every unit case whose expectation moved from
withholding to publishing, by name; the failure message the planted absent characteristic produced;
each named mutation with the exact assertion it broke; the assertion that closed WR-08 and the two
comment corrections (WR-04 and IN-02) quoted back; that WR-05 and IN-03 were carried forward as
deferred rather than done; and the two Node versions the final gate ran on.
When committing, run `pre-commit run --files <changed files>` until clean first; then `git add` the
paths and run a plain `git commit` with no pathspec, and confirm with
`git show --name-only --format="" HEAD` that the commit is not empty. The pre-commit configuration
runs lint, format, typecheck and fallow but not tests, so a red `test(...)` commit followed by a
green `feat(...)` commit is the intended shape. Never `--no-verify`, never rebase, never commit to
`main`.
</output>
