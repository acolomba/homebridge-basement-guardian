---
phase: 05.1-per-pump-trust-and-monotonic-silence
plan: 07
type: execute
wave: 7
depends_on:
  - 05.1-06
files_modified:
  - .planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-VALIDATION.md
  - .planning/WINDOWS.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - RES-01
  - RES-02
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Single-device behaviour is unchanged, measured on the one real Gemini as well as on the suite (D-09, V12).
    - Ledger entry 41 carries a recorded ruling on whether the silence timer arms on telemetry or on any message, naming the evidence the ruling rests on and what that evidence does not cover (criterion 4, D-13).
    - Every premise this phase corrected is written into the records that carried it, rather than left to be rediscovered.
    - The phase's records say plainly what its evidence does not cover - no real multi-device evidence, no Node 24 result, and a suspend consequence cited rather than observed.
  artifacts:
    - 05.1-VALIDATION.md carries a task ID and a status for all fifteen rows and records the manual-only verification result.
    - .planning/WINDOWS.md carries the dispositions for entries 6, 17, 18, 19 and 41, and the reason entry 14 stays open.
    - .planning/STATE.md records the four rulings as built and the five corrected premises.
    - .planning/REQUIREMENTS.md carries dated notes under RES-01 and RES-02 saying what this phase narrowed and completed.
  key_links:
    - Every correction in STATE.md and WINDOWS.md is sourced from a plan SUMMARY of this phase, not restated from the plan that predicted it.
  prohibitions:
    - No record may claim real multi-device evidence, because none exists and none can be obtained.
    - No record may claim a Node 24 result, because Node 24 is not installed here.
    - The suspend behaviour must not be written up as observed.
    - Ledger entry 14 must not be marked fixed, because it carries a second defect this phase does not touch.
    - No verify in this plan may gate on mdformat, markdownlint-cli2, the texthooks, or the repository format check for a .planning path, because none of them reaches .planning and such a gate can never fail.
---

<objective>
Close the phase: run the whole gate on both installed Node versions, check the change against the one
real device, and put every ruling, correction and limit into the records that will be read next.

Purpose: three of this phase's four items were decided at the close of Phase 5 and left unbuilt; the
fourth is a ruling rather than a change, and criterion 4 is satisfied only by a record. The phase's
own discipline requires those records to state their limits rather than read cleaner than the
measurement. Four premises this phase inherited did not survive measurement, and a fifth - the
assumption-delta detector reporting no singular-to-plural transition - was a detector miss rather than
an absence.

Output: a green gate on both installed runtimes, a live single-device check, and records that carry
the corrections rather than the beliefs.
</objective>

<execution_context>
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-CONTEXT.md
@.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-VALIDATION.md
@.planning/phases/05-degraded-operation-and-recovery/.continue-here.md
@.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-01-SUMMARY.md
@.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-02-SUMMARY.md
@.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-03-SUMMARY.md
@.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-04-SUMMARY.md
@.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-05-SUMMARY.md
@.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-06-SUMMARY.md
</context>

<phase_ground_rules>
1. **`pipefail` is OFF.** Never pipe a suite command into a filter.
2. **Concurrent suite runs produce false reds.** One suite at a time; re-run a red gate alone before
   believing it, and say in the summary if you had to.
3. **`.planning/` is excluded** from the markdown hooks, and the repository format check covers only
   `js`, `json`, `mjs` and `ts`. A verify gated on any of those for a `.planning/` path can never
   fail. None is used below.
4. **Node 24 is not installed.** The default `node` is newer than v24 and `/usr/bin/node` is v22.22.2.
   `.github/workflows/build.yml:31` runs `[22.x, 24.x]`; 24.x is CI's alone.
5. **Stay on `features/phase-05-degraded-operation-and-recovery`.**
6. **Git:** `pre-commit run --files <paths>` then fix, restage, re-run, then `git commit`. Never
   `--no-verify`, never rebase. Verify by content afterwards with
   `git show --name-only --format="" HEAD`. The GSD commit query has been observed reporting success
   while committing nothing, so use plain git and check.
7. **The untracked `.planning/milestone.lock` belongs to another session and must never be staged.**
</phase_ground_rules>

<artifacts_this_plan_produces>
| Record | File | What it gains |
|---|---|---|
| Per-task verification map | `05.1-VALIDATION.md` | a task ID and a status for all fifteen rows, plus the manual-only result |
| Validation front matter | `05.1-VALIDATION.md` | `status: validated`, `wave_0_complete: true`, `nyquist_compliant: true` |
| Ledger dispositions | `.planning/WINDOWS.md` | entries 6, 17, 18 and 19 fixed; entry 41 waived with D-13's ruling; entry 14 left open with its reason recorded |
| Rulings and corrections | `.planning/STATE.md` | the four rulings marked built, the five corrected premises, and the three stated limits |
| Requirement notes | `.planning/REQUIREMENTS.md` | dated notes under RES-01 and RES-02 |
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: The whole gate, on both installed runtimes, with the counts written down</name>

  <files>none — this task runs gates and writes no file</files>

  <read_first>
    - `package.json` scripts: `check`, `test`, `test:unit`, `test:cucumber`, `test:coverage:all`,
      `fallow`, `lint`, `typecheck`, `format:check`.
    - `05.1-VALIDATION.md` section *Test Infrastructure* for the measured baseline this phase started
      from: exit 0, 1388 unit tests with 0 failures, 102 scenarios all passed, 1120 steps all passed.
    - The six plan summaries of this phase, for the counts each recorded.
  </read_first>

  <action>
    Run the whole gate once, alone, on the default `node`, and record the exact counts: unit tests,
    passes, failures, scenarios, steps, and the three coverage percentages. Then run the test suite
    again under `/usr/bin/node`, the v22 runtime, so the phase carries a result on both installed
    versions. State plainly that Node 24 was not exercised because it is not installed here, and that
    CI is what covers it.

    If any gate goes red, re-run it alone before believing it. Two overlapping runs were measured at
    1383 passes with 2 failures against a solo run at 1385 with 0. Say in the summary if you had to
    re-run.

    Compare against the recorded baseline and account for every difference. A rise in unit tests, in
    scenarios and in steps is expected, and each increment should trace to a named plan of this phase.
    A fall in any of the three is a finding rather than a detail: it means an assertion left the
    suite, and the summary must name which one and why.

    Commit nothing in this task. It is a measurement.
  </action>

  <verify>
    <automated>O=$(npm run check 2>&amp;1); R=$?; test $R -eq 0 &amp;&amp; printf '%s\n' "$O" | grep -qE '^ℹ fail 0$' &amp;&amp; printf '%s\n' "$O" | awk '/^[0-9]+ scenarios \([0-9]+ passed\)$/ { if ($1 &gt;= 104) ok=1 } END { exit ok ? 0 : 1 }'</automated>
    <fails_when>The command exits non-zero, or the line `ℹ fail 0` is absent from the captured output, or no `N scenarios (N passed)` line with N at least 104 appears. Typecheck, lint, fallow, the format check and both test tiers all run under this one script, so any of them failing shows here as a non-zero status.</fails_when>
  </verify>

  <verify>
    <automated>O=$(npm run test:coverage:all 2>&amp;1); R=$?; test $R -eq 0 &amp;&amp; printf '%s\n' "$O" | grep -qE '^ℹ fail 0$'</automated>
    <fails_when>The command exits non-zero, or `ℹ fail 0` is absent. Any module under `dist-test/src/` below 100 on lines, branches or functions prints a `coverage required` error and appears in the table under 100.</fails_when>
  </verify>

  <verify>
    <automated>O=$(npm run build:test 2>&amp;1 &amp;&amp; /usr/bin/node -p "process.version" 2>&amp;1 &amp;&amp; /usr/bin/node --test 'dist-test/test/**/*.test.js' 2>&amp;1 &amp;&amp; /usr/bin/node ./node_modules/.bin/cucumber-js 2>&amp;1); R=$?; test $R -eq 0 &amp;&amp; printf '%s\n' "$O" | grep -qE '^v22\.' &amp;&amp; printf '%s\n' "$O" | grep -qE '^ℹ fail 0$' &amp;&amp; printf '%s\n' "$O" | awk '/^[0-9]+ scenarios \([0-9]+ passed\)$/ { if ($1 &gt;= 104) ok=1 } END { exit ok ? 0 : 1 }'</automated>
    <fails_when>The command exits non-zero, or no line beginning `v22.` appears, or `ℹ fail 0` is absent, or no all-passed scenario line with at least 104 appears. Both tiers are run by the v22 binary directly rather than through `npm`, because `npm` resolves its own node and a version printed by a separate command would pass whatever runtime actually ran the suite. If the direct invocation cannot work here, say so and record the exact command you did use rather than reporting a v22 result this one did not produce.</fails_when>
  </verify>

  <acceptance_criteria>
    - The whole gate is green on the default runtime, run alone, at 100/100/100 coverage.
    - The test suite is green on the v22 runtime, and the exact command used is recorded.
    - The unit, scenario and step counts are recorded and every difference from the 1388 / 102 / 1120
      baseline is accounted for by a named plan.
    - The summary states that no Node 24 result is claimed and why.
    - Any gate that had to be re-run alone is named.
  </acceptance_criteria>
  <done>The whole gate is green on the default runtime at full coverage, the suite is green on the v22 runtime, and every count difference from the baseline is accounted for (D-09).</done>
</task>

<task type="checkpoint:human-verify">
  <name>Checkpoint: the one real Gemini still behaves as it did</name>

  <precondition>
    The live dev Homebridge container is running against the vendor account that holds exactly one
    Gemini, and the vendor probe credentials are at `~/bg.env` (the probe README's reference to
    `~/.gs-uat.env` is stale and that file does not exist). If the container is not up, halt and say
    so rather than reporting an untested result.
  </precondition>

  <read_first>
    - `05.1-VALIDATION.md` section *Manual-Only Verifications* and the two recorded limits under it.
    - `.planning/phases/05-degraded-operation-and-recovery/.continue-here.md` section
      *Infrastructure State* for what the container held at the close of Phase 5: all 17 services with
      real telemetry, restoring cleanly across restarts.
  </read_first>

  <action>
    Build the plugin from this branch, install it into the live dev Homebridge container, and check
    that a single-device account behaves exactly as it did before this phase. This is a regression
    check, not a new assertion: D-09 asks for it precisely because every other claim this phase makes
    about more than one pump rests on the harness.

    What to look at, in order:
    1. All 17 services publish, with real telemetry, and each reads a plausible value.
    2. The controller-link warning, if it appears, reads as one line, names the device, quotes no
       credential, says the values are retained rather than refreshed, and names services rather than
       scope words. If the link never drops during the session, say that it was not observed rather
       than inferring the line from the code.
    3. Restart the container. The restored services mark stale, then recover on the first successful
       poll.
    4. The new anchor file exists under the Homebridge storage directory after a poll, holds only
       device identifiers and numbers, and holds no credential material. Read it and say what is in it.
    5. The live-reporting log lines, if any appear, name the device.

    Record the before and after of what you could compare, and record plainly what the session did not
    cover. **This container holds exactly one Gemini, so it can neither reproduce nor refute any
    multi-device behaviour, and a healthy container is not evidence for the per-pump work.** Say that
    in the record.
  </action>

  <what-built>
    Phase 05.1 in full, installed into the live dev Homebridge container from this branch: shadow
    silence withdrawn per pump instead of account-wide, a controller-link warning derived from the
    services the accessory actually published, silence measured on a forward-only clock clamped by a
    persisted wall-clock arrival anchor, and the live-reporting log line now naming its device.
  </what-built>

  <how-to-verify>
    1. Build this branch and install it into the live dev Homebridge container, then start it.
    2. In Apple Home or the Homebridge UI, confirm all 17 services publish with real telemetry and
       each reads a plausible value.
    3. Restart the container. Confirm the restored services mark stale and then recover on the first
       successful poll.
    4. List the Homebridge storage directory and open the arrival-anchor file. Confirm it holds only
       device identifiers and numbers, and no credential material.
    5. Read the Homebridge log. If a controller-link warning appeared, copy it verbatim and confirm it
       is one line, names the device, quotes no credential, says the values are retained rather than
       refreshed, and names services rather than scope words. If it did not appear, say so — do not
       infer the line from the code.
    6. If a live-reporting warning or recovery line appeared, confirm it names the device.
  </how-to-verify>

  <resume-signal>
    Reply with the 17-service count, the restart result, the anchor file's contents in shape only, and
    either the controller-link line verbatim or a statement that it did not appear during the session.
    Say "continue" to proceed to task 3. Reply "stop" with what you saw if fewer than 17 services
    publish, if a restarted container shows a restored service reading as current before its first
    successful poll, or if the anchor file carries anything beyond device identifiers and numbers.
  </resume-signal>

  <acceptance_criteria>
    - The live check is performed and its result recorded, including what it did not cover.
    - The record states that the container holds one Gemini and is therefore no evidence about
      multi-device behaviour (D-10).
    - The anchor file's actual contents are described from reading it, not from the code.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: The records carry the corrections, the rulings and the limits</name>

  <files>.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-VALIDATION.md, .planning/WINDOWS.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>

  <read_first>
    - All six plan summaries of this phase. Where a summary and its plan disagree, the summary is what
      shipped.
    - `.planning/WINDOWS.md` header, for the exact ledger verbs, and the rows for entries 6, 14, 17,
      18, 19 and 41.
    - `.planning/STATE.md` lines 128 to 200, the four post-Phase-05 rulings.
    - `.planning/REQUIREMENTS.md` lines 64 to 70, RES-01 and RES-02 and the dated note under them,
      which is the style to follow.
    - `05.1-CONTEXT.md` section *Corrections after research*, the four-row table, and D-13.
    - `05.1-VALIDATION.md` in full: the fifteen rows, the Wave 0 list, and the sign-off block.
  </read_first>

  <action>
    Fill in `05.1-VALIDATION.md`: put the owning plan and task in the Task ID column of all fifteen
    rows, set each Status from what the summaries actually recorded, and mark the Wave 0 items done.
    Where a row's mutation failed nothing, say so in that row rather than leaving it reading as pinned
    - V11 and V9 both anticipate that outcome. Record the manual-only verification's result under its
    table. Set the front matter to `status: validated`, `wave_0_complete: true` and
    `nyquist_compliant: true` only if every row genuinely carries an automated verify or a recorded
    reason it cannot.

    Disposition the ledger with the tool's own verbs, running them from the repository root and
    checking the file afterwards rather than trusting the command's own report:
    - Entries 6, 17, 18 and 19: mark fixed.
    - Entry 41: waive, with D-13's ruling as the reason. The silence timer keeps arming on any
      message. The reason must name the evidence and its limits in the same breath: a 90-minute probe
      of one device in steady state, seven messages, six heartbeats, and no pump cycle, fault, power
      event, reconnect or firmware update, so event-driven vendor reports are unmeasured. Adopting the
      change would guard a shape nobody has observed, which is how entry 34's original false reason
      was born.
    - **Entry 14 stays open**, and this is the honest answer rather than an oversight. It carries two
      dispositions: IN-03, which this phase closes, and WR-05, the nine-member context literal written
      three times in the platform, which this phase does not touch. The ledger has no verb for closing
      half an entry, so leave it open and record in STATE.md which half is now closed and by what.

    Update `.planning/STATE.md` under Decisions. Mark the three built rulings as built, naming the
    plan that built each. Record the corrections, each sourced from the summary that measured it, not
    from the plan that predicted it:
    - The controller-link log named five scope words, not eleven services. The figure 11 in STATE.md
      and in the Phase 5 handoff is not derivable from any source. The measured counts are 17
      catalogue rows, 16 non-connectivity, 15 that stop vouching, across 7 scopes.
    - Entry 18's stated mechanism was false. The subscription is one awaited call covering every
      device, so a wait cannot return on one device's subscription. The real defect was a cumulative,
      never-reset topic count that returned instantly on any reconnect.
    - Entry 17's helpers were not dead leftovers. They had ten live callers. The real defect was a
      second accessory-resolution rule shadowing the shared, documented one, and the topic helper's
      single-device coupling is narrower than the entry implies, because the four scenarios reaching
      it seed no devices at all.
    - A single map-valued callback would have been empty on a launch the vendor refused, deleting the
      credential branch's only input. The shipped shape carries the account-wide facts on their own
      channel.
    - The assumption-delta detector reported no singular-to-plural transition in this phase. That was
      a detector miss, not an absence: the phase is exactly one account-wide boolean becoming a
      per-device map. The identity question it exists to force was asked and answered in D-01, D-01a,
      D-02 and D-14. Record the miss so the next reader does not trust the detector's silence.

    Record the three limits in STATE.md, in the phase's own voice rather than softened:
    - No real multi-device evidence exists and none can be obtained. Every two-pump claim rests on the
      Cucumber harness and on named mutations.
    - Node 24 is not installed here. No Node 24 result is claimed.
    - The monotonic source's behaviour across a system suspend is cited from `man 2 clock_gettime`,
      not observed. This host has no accumulated suspend time, so it is recorded as no observation
      rather than as a finding.

    Add dated notes under RES-01 and RES-02 in `.planning/REQUIREMENTS.md`, following the style of the
    existing note under RES-02, saying what this phase narrowed and completed. Both rows already read
    Complete; do not change the status cells.

    Then run the pre-commit hooks over exactly the paths you changed, fix anything they raise, restage,
    re-run until clean, and commit. Verify the commit by content afterwards.
  </action>

  <verify>
    <automated>awk -F'|' '{ gsub(/ /,"",$2); gsub(/ /,"",$8); s[$2]=$8 } END { exit (s["6"]=="fixed" &amp;&amp; s["17"]=="fixed" &amp;&amp; s["18"]=="fixed" &amp;&amp; s["19"]=="fixed" &amp;&amp; s["41"]=="waived" &amp;&amp; s["14"]=="open") ? 0 : 1 }' .planning/WINDOWS.md</automated>
    <fails_when>Exit status 1, which is what the ledger returns while any of entries 6, 17, 18 or 19 still reads `open`, or entry 41 does not read `waived`, or entry 14 has been marked something other than `open`. Measured against the ledger as this plan was written, this command exits 1, so it is a gate that can fail.</fails_when>
  </verify>

  <verify>
    <automated>O=$(grep -c '| pending |' .planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-VALIDATION.md 2>&amp;1); test "$O" = "0"</automated>
    <fails_when>The count is anything other than `0`, meaning at least one of the fifteen verification rows still carries `pending` in its Task ID column. Measured as this plan was written, the count is 15.</fails_when>
  </verify>

  <verify>
    <automated>O=$(grep -cE '^nyquist_compliant: true$' .planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-VALIDATION.md 2>&amp;1); test "$O" = "1"</automated>
    <fails_when>The count is `0`, meaning the front matter still reads `nyquist_compliant: false` or the key is missing.</fails_when>
  </verify>

  <verify>
    <automated>O=$(git -C . show --name-only --format="" HEAD 2>&amp;1); R=$?; test $R -eq 0 &amp;&amp; printf '%s\n' "$O" | grep -q 'WINDOWS.md' &amp;&amp; printf '%s\n' "$O" | grep -q 'STATE.md' &amp;&amp; printf '%s\n' "$O" | grep -q 'REQUIREMENTS.md'</automated>
    <fails_when>The command exits non-zero, or the committed file list does not name all three records — which is what an empty commit looks like from outside, and the GSD commit query has been observed reporting success while committing nothing.</fails_when>
  </verify>

  <acceptance_criteria>
    - All fifteen rows of `05.1-VALIDATION.md` carry an owning plan and task and a status taken from
      the summaries, with any mutation that failed nothing said so in its own row (D-11).
    - The validation front matter reads `status: validated`, `wave_0_complete: true` and
      `nyquist_compliant: true`, and every row has an automated verify or a recorded reason it cannot.
    - Ledger entries 6, 17, 18 and 19 read fixed; entry 41 reads waived carrying D-13's ruling and the
      limits of its evidence (criterion 4); entry 14 still reads open, with the half this phase closed
      recorded in STATE.md.
    - `.planning/STATE.md` records the three built rulings with the plan that built each, all five
      corrected premises sourced from the summaries that measured them, and the three limits stated
      plainly (D-10, D-12, D-13).
    - `.planning/REQUIREMENTS.md` carries dated notes under RES-01 and RES-02 and no changed status
      cell.
    - The commit exists and names all three records, verified by content.
    - `.planning/milestone.lock` was never staged.
  </acceptance_criteria>
  <done>All fifteen validation rows carry an owner and a status, the ledger is dispositioned with entry 14 left open for its stated reason, and STATE.md carries the five corrections and the three limits (D-09, D-10, D-12, D-13).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin → live vendor account | The checkpoint task installs this branch into a container talking to the real account. No command is sent to the pump; the session is read-only observation plus a restart. |
| working tree → committed records | Four `.planning/` files are committed. Nothing under `.planning/` may carry credential material, an account identifier, or local-network data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.1-21 | Information disclosure | the four committed records, and the anchor-file contents quoted into them | high | mitigate | The checkpoint reads the anchor file and describes it. Quote only the shape and the value types, never an account identifier, an email, a token, or any local-network address. The vendor `deviceId` is permitted in accessory context and logs under the Phase 2 ruling but `D-027` keeps it out of public artifacts, so use the placeholder identifiers the harness already uses when a record needs to name one. Run `pre-commit` over the changed paths, which includes the secret scan. |
| T-05.1-22 | Repudiation | a record that reads cleaner than the measurement | high | mitigate | Three limits are required in STATE.md by name, and the prohibitions block forbids each of the three overclaims. The Phase 5 handoff names an agent measuring a premise its own plan asserted and reporting it false as the most valuable behaviour it observed; this task exists to keep those reports. |
| T-05.1-23 | Tampering | a commit that reports success while committing nothing | medium | mitigate | Plain git is used and the commit is verified by content with `git show --name-only`. This failure has been observed on this project. |
| T-05.1-24 | Tampering | staging another session's lock file | low | mitigate | `.planning/milestone.lock` is untracked and belongs to another session. Commit named paths only; never `git add -A`. |
| T-05.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package is installed by this phase. The live check installs the plugin built from this branch into the dev container, which is this repository's own artifact and not a registry fetch. |
</threat_model>

<verification>
- `npm run check` green on the default runtime, run alone, with `ℹ fail 0` and at least 104 scenarios
  all passed.
- `npm run test:coverage:all` at 100/100/100.
- The test suite green on the v22 runtime, with the exact command recorded.
- The live single-device check performed, its result and its limits recorded.
- The ledger status gate passes: entries 6, 17, 18 and 19 fixed, 41 waived, 14 still open.
- `05.1-VALIDATION.md` carries no `pending` Task ID and reads `nyquist_compliant: true`.
- The commit names `WINDOWS.md`, `STATE.md` and `REQUIREMENTS.md`, verified by content.
</verification>

<success_criteria>
The phase's four success criteria are all satisfied and all recorded: per-pump trust withdrawal, a
controller-link diagnostic that cannot drift, a forward-only silence clock, and a written ruling on
ledger entry 41 naming the evidence it rests on and what that evidence does not cover. The records say
what the phase did not measure as plainly as what it did.
</success_criteria>

<output>
Create `.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-07-SUMMARY.md` when done.
Record: the gate counts on both runtimes and the exact v22 command; the live check's observations and
what it did not cover; every ledger disposition and the reason entry 14 stays open; the five corrected
premises with the summary each came from; and the three limits stated plainly.
</output>
