# Phase 05.1: Per-Pump Trust and Monotonic Silence - Context

**Gathered:** 2026-09-03
**Status:** Ready for planning

<domain>
## Phase Boundary

This phase builds three rulings that Phase 5 decided and left unbuilt, and records a
fourth. Every one of them is already argued and settled under **Decisions** in
`.planning/STATE.md`; nothing here reopens them.

1. **Ledger entry 19** — shadow silence becomes per-pump. Today one quiet pump makes every
   accessory on the account stop vouching, so a two-pump owner is told the plugin cannot
   vouch for a pump that is reporting perfectly.
2. **Ledger entry 6** — the controller-link log names 11 of the 16 services it marks,
   silently omitting the Self-Test and Alarm Mute switches.
3. **Ledger entry 14 / IN-03** — shadow silence is measured against a jumpable wall clock,
   so a backwards jump larger than the window leaves a dead pump reading as trustworthy.
4. **Ledger entry 41** — a recorded ruling on whether the silence timer should arm on
   telemetry rather than on any message. The ruling, not the change.

Pulled in during discussion because this phase's own evidence rests on them:

5. **Ledger entries 17 and 18** — two-device defects in the Cucumber harness.

**Not in this phase.** The remaining open ledger entries (11, 35, 36, 37, 38, 39, 40) are
dispositioned in Phase 6 as release-readiness work. No new safety semantics, no new
services, no change to which scopes a cause withdraws.

</domain>

<decisions>
## Implementation Decisions

### Per-device trust signal (entry 19)

- **D-01:** `onMonitoringHealth` becomes map-valued — `ReadonlyMap<deviceId, MonitoringTrust>`.
  The runtime resolves per-device trust and `applyMonitoringHealth` looks each accessory up.
  Chosen over keeping one account-wide struct beside a `silentDeviceIds` list, which is a
  smaller diff but leaves the callback saying something other than what it means.
  `MonitoringTrust` itself is unchanged, so `markMonitoring`, `monitoringDegradedScopes` and
  every accessory-tier trust literal stay as they are; the churn is in `src/platform.ts`,
  `src/runtime/accountRuntime.ts`, `features/support/world.ts` and the runtime tests.
  — **Reversibility:** costly — the signature is bound by the platform, the Cucumber harness
  and the runtime test suite, so undoing it touches all four call sites at once.

- **D-02:** An accessory with no per-device answer reads **silent**. A device admitted after
  the last push, or an accessory restored from cache before discovery completes, does not
  vouch. This follows the project constraint that unknown never becomes a normal default,
  and it matches `RES-04`, which already marks every restored service stale until a poll
  succeeds. Explicitly not chosen: falling back to the old any-pump-silent boolean, which
  would reintroduce the flatten on the path most likely to be hit at startup.

- **D-03:** The flatten at `src/runtime/monitoringHealth.ts:212`
  (`shadowSilent: silentDevices().length > 0`) is removed. `silentDevices()` already answers
  per device and needs no new tracking.

- **D-04:** Marking breadth still follows the cause, not the transport. `shadowSilent` and
  `controllerLinkLost` become per-pump; `restDegraded` and `credentialsRejected` stay
  account-wide, because those causes genuinely do affect every pump. This is the Phase 5
  ruling restated, not a new decision.

### Controller-link diagnostic (entry 6)

- **D-05:** The log message is **derived from the marked set** rather than hand-written, so
  it cannot drift from behaviour by construction. A test still pins the rendering. Not
  chosen: a static sentence naming all 16 with a drift test, and logging at seven-scope
  granularity instead of naming services.

### Monotonic silence (entry 14 / IN-03)

- **D-06:** A **separate `MonotonicClock` port**, injected where silence is measured.
  `Clock.now()` stays honest wall-clock for receipt times and for anything persisted, and the
  two time bases cannot be confused at a call site. Not chosen: adding `monotonicNow()` to
  the existing `Clock` (one interface carrying two incompatible bases), and clamping
  `systemClock.now()` forward-only (smallest diff, but it silently changes what `now()` means
  for persisted receipt timestamps and would swallow a real wall-clock correction).
  — **Reversibility:** costly — a second port is wired at the composition root and threaded
  to every silence measurement; undoing it means re-deriving which call sites wanted which
  time base.

- **D-07:** A wall-clock arrival anchor **is persisted**, and it is **clamped so it can only
  ever report silence sooner, never later**. Across a restart the anchor may lengthen a
  silence window and may never shorten one, so a backwards wall-clock jump cannot hide a
  dead pump — the failure IN-03 exists to close, kept closed on the restart path too.

- **D-08:** Nothing persists shadow-arrival times today. Measured during discussion:
  `AccessoryContext` in `src/persistence/accessoryContext.ts` carries `primaryPump`,
  `backupPump`, `watermarks` and `lastVendorName`, and no arrival timestamp exists anywhere
  in `src/`. D-07 therefore adds a new persisted field, and an install predating it carries
  no anchor and falls through to D-02 — silent until told otherwise.
  — **Reversibility:** one-way — the field enters `accessory.context`, which is written to
  Homebridge's cached accessory store on real installs. Removing it later needs a migration
  path for installs that already carry it, the same shape `PumpObservation` already has.

### Evidence bar

- **D-09:** The bar is the **two-pump Cucumber harness with named discriminating mutations,
  plus a live single-device regression check** against the one real Gemini, proving the
  change did not alter single-device behaviour.

- **D-10:** **No real multi-device evidence exists and none can be obtained.** The vendor
  account has exactly one Gemini and the dev Homebridge container holds that one device. This
  is recorded as a limit of the phase, not worked around. Any claim this phase makes about
  two-pump behaviour rests on the harness and on mutation, and must say so.

- **D-11:** Every safety-bearing assertion carries a discriminating mutation, per
  `05-VALIDATION.md`'s binding discipline: apply the mutation, watch the named test fail,
  revert, confirm green, and record which test failed on what line. Before trusting a new
  assertion, check whether its expected value is already what the field holds.

### Harness correctness (entries 17 and 18, pulled in)

- **D-12:** Entries 17 and 18 are fixed **in this phase, before the harness carries this
  phase's evidence.** Entry 18 is the load-bearing one: `awaitSubscription` waits on a
  cumulative published-topic count, so on a two-device account it returns once the client
  subscribed to *either* device — a check that passes because the thing it rests on already
  sits at the value the defect produces, which is this project's named blocking anti-pattern.
  Entry 17 is dead single-device leftovers in `harness.ts` and is fixed alongside it.

### Ledger entry 41

- **D-13:** Ruled **not adopted**, with the limits of the evidence recorded. The silence
  timer keeps arming on any message. The 90-minute live probe that informs this covers one
  device in steady state, seven messages, six heartbeats, and no pump cycle, fault, power
  event, reconnect or firmware update — so event-driven vendor reports are unmeasured.
  Adopting it would guard a shape nobody has observed, which is how entry 34's original false
  reason was born. The ruling closes entry 41; it does not claim the sample is strong.

### Claude's Discretion

- How the derived controller-link message is worded and how the marked set is rendered into
  it (D-05 fixes the source of truth, not the prose).
- Which monotonic source backs `MonotonicClock`, and where the port is constructed.
- Whether the persisted anchor lives in `AccessoryContext` or in a store of its own, provided
  D-07's clamp and D-08's absent-anchor fallback both hold.
- Plan and wave decomposition across the six items.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### The rulings this phase builds
- `.planning/STATE.md` § **Decisions** — the four post-Phase-05 rulings with their full
  reasoning. Authoritative for what was decided and why. Read this first.
- `.planning/WINDOWS.md` — ledger entries 6, 14, 17, 18, 19, 41. Entry text is the record of
  the defect; the STATE.md ruling is the record of the decision.
- `.planning/phases/05-degraded-operation-and-recovery/.continue-here.md` — the Phase 5
  handoff. Its **Critical Anti-Patterns** table is binding, and two of its rows are `blocking`.

### Binding test discipline
- `.planning/phases/05-degraded-operation-and-recovery/05-VALIDATION.md` — **binding.** Every
  row names the mutation that must fail it. The discipline in D-11 comes from here.
- `.planning/phases/05-degraded-operation-and-recovery/05-VERIFICATION.md` — the third
  verification, `passed` 4/4, every probe recorded so it can be re-run.

### Prior phase context
- `.planning/phases/05-degraded-operation-and-recovery/05-CONTEXT.md` — 13 binding decisions
  from Phase 5, including the ones this phase narrows.
- `.planning/PROJECT.md` § **ADR-Locked Decisions** — D-014 (preserve untrusted state),
  D-016 (monitoring-path failure distinct from device offline), D-031 (failed restart).
- `.planning/REQUIREMENTS.md` — RES-01 and RES-02. Both rows already read Complete; this
  phase narrows the scope RES-01 marks and completes the diagnostic RES-02 emits.

### Code the phase changes
- `src/runtime/monitoringHealth.ts` — the flatten at line 212, and `silentDevices()` above it.
- `src/runtime/accountRuntime.ts` — `monitoringTrustNow()` at 457, the three
  `onMonitoringHealth` pushes at 479, 533 and 967, and the `silentDevices()` read at 339.
- `src/platform.ts` — `applyMonitoringHealth` at 380 and the callback at 557.
- `src/accessories/basementGuardian.ts` — `reportControllerLink` at 822,
  `monitoringDegradedScopes` at 317, `markMonitoring` at 887.
- `src/runtime/clock.ts` — the `Clock` port and `systemClock`.
- `src/persistence/accessoryContext.ts` — `AccessoryContext`, for D-07's anchor.
- `features/support/world.ts` — the harness callback at 648, which stands in for the platform.
- `features/support/steps/harness.ts`, `features/support/steps/shadow.ts` — entries 17 and 18.

### External documentation
- [Homebridge Plugin Documentation](https://developers.homebridge.io/#/) — accessory context
  persistence, for D-07's new field.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- **`silentDevices()` already answers per device.** `src/runtime/monitoringHealth.ts:175-179`
  returns every admitted device silent for two heartbeats, in admission order. Entry 19 needs
  no new tracking — only the flatten at line 212 removed and the answer carried downstream.
- **`admitDevice` / `forgetDevice` / `recordShadowMessage` are already device-keyed.** The
  per-device map `lastShadowMessageAt` exists and its docblock already argues why the question
  has one answer per controller.
- **The `Clock` port is injected everywhere.** `src/runtime/clock.ts` is a one-method interface
  with `systemClock` wired at the composition root only, so D-06 adds a sibling rather than
  retrofitting injection.
- **The two-pump Cucumber harness** landed in plan 05-13 and is what D-09 rests on.
- **`PumpObservation` is the shape D-08's migration follows** — optional, absent on first run,
  with the seeding path treated as the only one that has ever existed.

### Established Patterns

- **Constructor dependency injection**, per PROJECT.md's open implementation preference.
  `MonotonicClock` follows it.
- **`markMonitoring` compares every member of `MonitoringTrust` by name**, with a comment
  explaining what makes each one load-bearing. A member added to the type and left out of that
  comparison is silent rather than harmless — read the comment at
  `src/accessories/basementGuardian.ts:887-907` before touching the type.
- **`applyMonitoringHealth` is exported for the harness's sake**, so a fan-out living only in
  the platform's callback would be copied into the harness and every scenario would assert
  against the copy. Keep the fan-out exported and shared.
- **The layering order in `distrustReasonsOf` is a decision, not an accident.** Monitoring
  scopes fill before controller-link scopes so a blackout reports the cause that happened.

### Integration Points

- `monitoringHealth.trustNow()` → `accountRuntime.monitoringTrustNow()` →
  `options.onMonitoringHealth(trust)` → `platform.applyMonitoringHealth(context, trust)` →
  `basementGuardianAccessory.markMonitoring(trust)`. D-01 changes the middle two links; the
  ends stay as they are.
- `features/support/world.ts:648` mirrors the platform callback and must change with it.

</code_context>

<specifics>
## Specific Ideas

- **Entry 18 is the reason entries 17 and 18 came into this phase.** A two-pump scenario that
  waits on a cumulative subscription count can return once *either* device subscribed, so it
  could pass for the wrong reason on exactly the multi-device behaviour this phase adds. Fix
  the harness before it carries the evidence.
- **The anchor clamp has a direction and the direction is the whole point.** Sooner, never
  later. A test should fail if the clamp is inverted.
- **Say what the evidence does not cover.** D-10 and D-13 both require the phase's records to
  state their limits rather than read cleaner than the measurement. The Phase 5 handoff names
  this as the most valuable behaviour it observed: an agent measuring a premise its own plan
  asserted and reporting it false, and null results reported as null.

</specifics>

<deferred>
## Deferred Ideas

- **Arming the silence timer on telemetry rather than on any message** — ruled not adopted
  (D-13). Recorded as available in ledger entry 41. Do not implement without a fresh ruling.
- **Ledger entries 11, 35, 36, 37, 38, 39, 40** — dispositioned in Phase 6 as release-readiness
  work. Entries 11 and 35 are single-tier coverage recordings; 36 through 40 are record
  corrections.

### Reviewed Todos (not folded)

Four pending todos were matched against this phase by keyword and none is Phase 05.1 work.
All four carry to Phase 6:

- **Document which services Apple Home renders** — user-facing documentation, REL-08.
- **Record G-002 natural water-level evidence** — release-gate evidence, REL-07.
- **Define cloud request header policy** — privacy and header policy, REL-03.
- **State the harness mDNS prerequisite** — developer-harness documentation, `dev/README.md`.

</deferred>

---

*Phase: 05.1-Per-Pump Trust and Monotonic Silence*
*Context gathered: 2026-09-03*
