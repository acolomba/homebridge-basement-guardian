---
phase: 05.1-per-pump-trust-and-monotonic-silence
verified: 2026-09-05T00:10:27Z
status: passed
score: 8/8 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 05.1: Per-Pump Trust and Monotonic Silence Verification Report

**Phase Goal:** A multi-pump owner keeps trust in every pump that is still reporting, reads a
controller-link diagnostic that names every service it withdrew, and gets silence measured by a
clock that cannot jump.
**Verified:** 2026-09-05T00:10:27Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A silent pump withdraws trust from only that pump's accessory; a pump that is still reporting keeps vouching for its own services | ✓ VERIFIED | `src/runtime/monitoringHealth.ts` carries no account-wide collapse (`grep` for `silentDevices().length/.some`, `shadowSilent).some/.length` returns nothing anywhere in `src/`, `features/`, `test/`). `src/platform.ts:384-399` `applyMonitoringHealth(context, account, byDevice)` joins per accessory on `deviceId`. `features/degradedOperation.feature:265` — `A silent pump stops vouching for itself while its neighbour keeps vouching` — passes in a suite I ran myself (104/104 scenarios). V1 mutation (restore the flatten) is recorded red at `features/degradedOperation.feature:265` in `05.1-02-SUMMARY.md`. |
| 2 | An accessory with no entry in the pushed map reads untrusted rather than a fabricated default | ✓ VERIFIED | `src/platform.ts:397` — `byDevice.get(basementGuardianAccessory.deviceId) ?? { ...account, shadowSilent: true }`, with the reason commented at the lookup. V2 mutation recorded red at `test/platform.test.ts:1290`; V13 (UUID-join mutation) recorded red across six cases. |
| 3 | The controller-link diagnostic names every published service the marking pass withdraws, derived from what the accessory actually published (not hand-written, cannot silently drift) | ✓ VERIFIED | `src/accessories/basementGuardian.ts:852` `withdrawnServiceNames()` reads the accessory's own `published` descriptors, joined to catalogue rows by kind + service-type identifier, filtered by non-connectivity scope minus tolerating rows. V4 (change catalogue, leave message alone) recorded red at three pinned renderings; V5 (derive from whole catalogue) recorded red at the suppression case; V6 (drop tolerating-row filter) recorded red at the tolerance case. |
| 4 | The message names only services this accessory actually published (a suppressed fault, or a row that never projected a value, is never named) | ✓ VERIFIED | Same derivation as #3, sourced from `published` rather than the static catalogue. Suppression case `test/accessories/basementGuardian.test.ts:1788` and V5 mutation red, confirmed in `05.1-06-SUMMARY.md`. |
| 5 | Shadow silence is measured against a forward-only (monotonic) elapsed time, clamped so a wall-clock correction can only ever report silence sooner, never later | ✓ VERIFIED | `src/runtime/monotonicClock.ts` exists, wired at the one composition root (`src/platform.ts`). `src/runtime/monitoringHealth.ts:208-211` `silenceElapsedMs` returns `Math.max(forwardOnlyElapsedMs, wallNow - anchor)` when an anchor exists. V7 (back the measurement with wall time) and V8 (invert the clamp to `Math.min`) both recorded red; `features/degradedOperation.feature:676` — `A backwards system clock does not restore trust in a pump that stopped speaking` — passes; both V10 variants recorded red. |
| 6 | A restart does not re-vouch for a pump that was already quiet: a persisted wall-clock anchor survives the process restarting | ✓ VERIFIED | `src/runtime/arrivalAnchors.ts` persists the anchor under `api.user.storagePath()`; `admitDevice` records an anchor only when the store holds none (`src/runtime/monitoringHealth.ts`). V9a (admission overwrites a restored anchor) recorded red across three cases. Live single-device check in `05.1-07-SUMMARY.md` observed the anchor file byte-identical across a deliberate restart on the real Gemini. |
| 7 | Ledger entry 41 (whether the silence timer arms on telemetry or on any message) carries a recorded ruling naming its evidence and the evidence's limits | ✓ VERIFIED | `.planning/WINDOWS.md` entry 41 reads `waived`, with the D-13 ruling text naming the 90-minute single-device probe and its stated limits (no pump cycle, fault, power event, reconnect, or firmware update observed), confirmed by direct read of the ledger file. |
| 8 | Single-device (pre-existing) behaviour is unchanged and the whole automated gate stays green | ✓ VERIFIED | I independently ran, on this machine, outside of any SUMMARY claim: `npm run test:unit` → 1424/1424 pass, 0 fail; `npm run test:cucumber` → 104 scenarios (104 passed), 1156 steps (1156 passed); `npm run test:coverage:all` → all files 100.00/100.00/100.00; `npm run typecheck` → exit 0; `npm run lint` → exit 0. |

**Score:** 8/8 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/runtime/monitoringHealth.ts` | Per-device silence answer, no account-wide flatten | ✓ VERIFIED | Confirmed by grep (no flatten anywhere) and by reading the clamp implementation. |
| `src/platform.ts` | `applyMonitoringHealth(context, account, byDevice)` with missing-entry rule | ✓ VERIFIED | Present at line 384, joined on `deviceId`, missing-entry rule at line 397. |
| `src/runtime/accountRuntime.ts` | Per-device latch, per-device failure-log kind, removal drop | ✓ VERIFIED | `liveReportingKind(deviceId)`, `reportedSilentDevices`, and the confirmed-removal branch dropping the kind via `FailureLog.forget` (per `05.1-03-SUMMARY.md`, cross-checked against `git log` commit `98eedd9`). |
| `src/runtime/monotonicClock.ts` | One-method `MonotonicClock` port + production instance, wired at composition root only | ✓ VERIFIED | File exists, read in full; `systemMonotonicClock` constructed once, in `src/platform.ts`. |
| `src/runtime/arrivalAnchors.ts` | Persisted anchor store, hand-written predicate, atomic write | ✓ VERIFIED | File exists (8124 bytes), created 2026-09-04 per `git log` (`184ba72`, `e295ce0`). |
| `src/accessories/basementGuardian.ts` | `withdrawnServiceNames()` deriving the controller-link warning | ✓ VERIFIED | Present at line 852, called at line 890. |
| `features/degradedOperation.feature` | Two-pump silence scenario and backwards-clock scenario | ✓ VERIFIED | Both scenarios present (lines 265, 676) and pass in a suite I ran myself. |
| `test/platform.test.ts`, `test/runtime/*.test.ts`, `test/accessories/basementGuardian.test.ts` | Unit pins for missing-entry, join key, refused launch, latch, kind, removal, clamp direction, suppression, tolerance, twin rows | ✓ VERIFIED | Coverage at 100/100/100 on every touched module; mutation records for V1–V15 documented per-plan and cross-checked against the ledger and my own coverage run. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `src/runtime/accountRuntime.ts` (`monitoringTrustByDevice`) | `src/platform.ts` (`applyMonitoringHealth`) | `onMonitoringHealth(account, byDevice)` push | ✓ WIRED | Confirmed by reading the push sites and the platform callback signature; end-to-end proof is the passing two-pump Cucumber scenario. |
| `src/accessories/basementGuardian.ts` (`published` descriptors) | `withdrawnServiceNames()` | Reads `published` one line after it is recomputed, at the existing `reportControllerLink` call site | ✓ WIRED | Confirmed by reading the call site (`:890`) and the suppression/tolerance/twin-row unit cases, which discriminate against a whole-catalogue derivation. |
| `src/platform.ts` (`systemMonotonicClock`) | `src/runtime/accountRuntime.ts` → `createMonitoringHealth` | Threaded exactly as `clock` is threaded | ✓ WIRED | Confirmed by reading the seam in `src/platform.ts` and `src/runtime/accountRuntime.ts`. |
| `src/runtime/accountRuntime.ts` (`start()`) | `src/runtime/arrivalAnchors.ts` (`restore()`) | Awaited before `launch()` | ✓ WIRED | Confirmed by reading `start()`; the live single-device restart check (anchor byte-identical across restart) is direct behavioral confirmation. |

### Behavioral Spot-Checks (independently reproduced, not taken from SUMMARY claims)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Whole unit suite green | `npm run test:unit` | `tests 1424 / pass 1424 / fail 0` | ✓ PASS |
| Whole Cucumber suite green, run alone | `npm run test:cucumber` | `104 scenarios (104 passed)`, `1156 steps (1156 passed)` | ✓ PASS |
| Whole-tree coverage at 100/100/100 | `npm run test:coverage:all` | `all files 100.00 / 100.00 / 100.00` | ✓ PASS |
| No type errors | `npm run typecheck` | exit 0 | ✓ PASS |
| No lint errors | `npm run lint` | exit 0 | ✓ PASS |
| No debt markers in phase-touched files | `grep -rnE "TBD\|FIXME\|XXX"` over the phase's production files | no matches | ✓ PASS |

Note: unit-test count (1424) is one higher than the phase-close figure recorded in `05.1-07-SUMMARY.md`
(1423) because of a later, out-of-phase commit (`b0f37fa`, `quick-260904-mkz`) that landed on this
branch after the phase closed. This is accounted for and is not a regression — it is an added case.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| RES-01 | 05.1-01, 05.1-02, 05.1-03, 05.1-04, 05.1-05, 05.1-07 | Narrows and completes the per-device and time-based halves of RES-01 (last valid value preserved, narrowest scope faulted, forward-only clamp) | ✓ SATISFIED | `.planning/REQUIREMENTS.md` line 66 carries the 2026-09-04 narrowing note; behaviour confirmed above. |
| RES-02 | 05.1-01, 05.1-06, 05.1-07 | Completes the controller-link diagnostic naming every withdrawn service | ✓ SATISFIED | `.planning/REQUIREMENTS.md` line 68 carries the 2026-09-04 completion note; behaviour confirmed above. |

No orphaned requirements: `05.1-*-PLAN.md` frontmatter declares only RES-01 and RES-02 across all seven
plans, matching the phase's stated requirement IDs exactly, and both rows in `REQUIREMENTS.md` carry
this phase's dated note without a status-cell change (as the plan required).

### Anti-Patterns Found

None. `grep` for `TBD|FIXME|XXX` and `TODO|HACK|PLACEHOLDER` across every production file this phase
touched (`monotonicClock.ts`, `arrivalAnchors.ts`, `monitoringHealth.ts`, `accountRuntime.ts`,
`platform.ts`, `basementGuardian.ts`) returns no matches. An independent code review
(`05.1-REVIEW.md`, dated 2026-09-04) covering all 21 changed files found 0 critical and 0 warning
findings; its single Info-level note (`IN-01`, an unrelated pre-existing harness gap in
`features/support/world.ts`'s `restartPlugin()`) was explicitly out of the fix scope and does not
touch any file this phase's must-haves depend on.

### Gaps Summary

None found. Every must-have truth, artifact, and key link is verified against the codebase, not merely
against SUMMARY claims. I independently re-ran the full unit suite, the full Cucumber suite (alone, as
the phase's own ground rules require), and the whole-tree coverage gate, and confirmed the results
match (and in one case, exceed by one test from an unrelated later commit) what the plan summaries
report. The ledger (`WINDOWS.md`), `STATE.md`, `REQUIREMENTS.md`, `README.md`, and `CHANGELOG.md` all
carry the corrections and rulings the phase's own plan required, verified by direct reads rather than
by trusting `05.1-07-SUMMARY.md`'s narrative.

One structural limitation is disclosed throughout the phase's own records rather than hidden, and is
noted here rather than treated as a gap: the vendor account backing this project holds exactly one
real Gemini device, so every multi-pump claim rests on the Cucumber/unit harness and on the mutation
records (D-10), not on real multi-device hardware. This is consistent with how prior phases (05, etc.)
have treated the same structural constraint, is stated plainly in `STATE.md`, `README.md` is silent on
it (correctly, since it is an internal evidentiary note rather than a user-facing fact), and the
phase's own `05.1-07` plan already ran the one compensating check available — a live single-device
regression check against the real hardware, which passed and additionally observed the persisted
anchor surviving a real restart byte-identical. No further human action is required beyond what the
phase's own checkpoint already obtained; this is noted for context rather than as an outstanding item.

---

_Verified: 2026-09-05T00:10:27Z_
_Verifier: Claude (gsd-verifier)_
