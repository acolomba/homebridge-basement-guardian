---
phase: 05.1-per-pump-trust-and-monotonic-silence
plan: 03
type: execute
wave: 3
depends_on:
  - 05.1-02
files_modified:
  - src/runtime/accountRuntime.ts
  - test/runtime/accountRuntime.test.ts
autonomous: true
requirements:
  - RES-01
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - A quiet pump does not suppress a noisy pump's recovery report, and a noisy pump does not re-report on a quiet neighbour's behalf (D-14).
    - Each device's live-reporting observation is rate-limited on its own cadence, so two pumps failing at different times each get their own warning.
    - A device the account confirmed removed leaves no failure-log kind and no latch entry behind, so neither can grow for the life of the process (V15).
  artifacts:
    - test/runtime/accountRuntime.test.ts holds two-device cases for the recovery latch and for the per-device live-reporting kind.
    - src/runtime/accountRuntime.ts drops a removed device's failure-log kind at the single pruning site it already uses for stamps.
  key_links:
    - The removal drop sits beside health.forgetDevice at src/runtime/accountRuntime.ts:382, which the surrounding comment at :377-381 establishes as the only place a removed device is pruned.
    - FailureLog.recordSuccess deletes the kind from warnedAt and reports whether it was failing, so dropping a removed device's kind reuses the shipped mechanism rather than reaching into the log.
  prohibitions:
    - The per-device latch must not be collapsed to a single boolean by any aggregate over the map.
    - A second pruning site for removed devices must not appear anywhere else in the runtime.
---

<objective>
Pin the two per-device changes the tracer had to make on the same lines it removed the flatten from,
and close the one real cost D-14 names.

Purpose: `reportedShadowSilent` and the `Live device reporting` failure-log kind both became per
device in plan 05.1-02, because leaving either as a single boolean would have re-created the collapse
D-03 removes. Neither has a unit case yet. And `warnedAt` retains a removed device's kind until that
kind recovers, which for a device the account no longer holds is never — so the map grows for the life
of the process unless removal drops it.

Output: two-device cases holding the latch and the per-device kind, and a removal path that drops both.
</objective>

<execution_context>
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-CONTEXT.md
@.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-PATTERNS.md
@.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-VALIDATION.md
@.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-02-SUMMARY.md
</context>

<phase_ground_rules>
1. **`pipefail` is OFF.** Never pipe a suite command into a filter.
2. **Concurrent suite runs produce false reds.** One suite at a time; re-run a red gate alone before
   believing it, and say in the summary if you had to.
3. **Coverage is a real gate** at 100/100/100 over `dist-test/src/**/*.js`.
4. **`npm run fallow`** runs `dead-code`, `health` and `dupes`.
5. **Node 24 is not installed.** Claim no Node 24 result.
6. **Stay on `features/phase-05-degraded-operation-and-recovery`.**
7. **Git:** `pre-commit run --files <paths>` → fix → restage → re-run → `git commit`. Never
   `--no-verify`, never rebase. Verify by content afterwards.
8. **Mutation discipline (D-11, binding):** apply the mutation, watch the named test fail, record
   which test failed on what line, revert, confirm green. A mutation that fails nothing is a finding.
9. **D-10:** the account has exactly one Gemini. Every two-pump claim here rests on the unit fixtures
   and on the mutations, and the SUMMARY must say so.
</phase_ground_rules>

<artifacts_this_plan_produces>
| Symbol / name | Home | New or changed |
|---|---|---|
| Two-device recovery-latch case | `test/runtime/accountRuntime.test.ts` | new |
| Two-device live-reporting cadence case | `test/runtime/accountRuntime.test.ts` | new |
| Removed-device drop case | `test/runtime/accountRuntime.test.ts` | new |
| The removal drop beside `health.forgetDevice` | `src/runtime/accountRuntime.ts:377-383` | changed |
</artifacts_this_plan_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two pumps, two latches, two reporting cadences</name>

  <files>test/runtime/accountRuntime.test.ts</files>

  <read_first>
    - `src/runtime/accountRuntime.ts:277-282` — the latch and the comment stating what it records and
      why, as plan 05.1-02 left it.
    - `src/runtime/accountRuntime.ts:45-59`, `:450-480`, `:633-676` — the failure-log kinds, the
      reporting helper, and the arrival callback with its 27-line ordering comment at `:638-664`.
    - `src/runtime/failureLog.ts` in full, 70 lines. `:42-45` states why a kind absent from `warnedAt`
      is not currently failing, and `:62-68` states why deleting reports whether it was.
    - `test/runtime/accountRuntime.test.ts:70-80` (the recovery-line constant), `:310-360` (the
      recorder shape), `:2005-2060` and `:2150-2175` — the four shipped live-reporting cases.
    - `05.1-VALIDATION.md` row V14.
  </read_first>

  <behavior>
    - Two admitted devices. One falls silent and the other keeps heartbeating.
    - The silent one's live-reporting kind warns; the reporting one's does not.
    - When the silent one speaks again, exactly one recovery line names that device, and the
      neighbour's state is untouched by it.
    - A heartbeat from the still-reporting pump does not clear the silent pump's latch, and does not
      trigger a report on the silent pump's behalf.
    - Collapsing the latch to a single boolean over the whole map makes at least one of these fail.
  </behavior>

  <action>
    Add the two-device cases the tracer's change has no unit coverage for, following the recorder
    shape already in the file at `test/runtime/accountRuntime.test.ts:310-360` and the four shipped
    single-device live-reporting cases as the template for driving silence and arrival.

    First case: two devices admitted, one goes silent past two heartbeats while the other heartbeats
    within the window, then the silent one delivers a message. Assert that the warning names the
    silent device and not its neighbour, and that exactly one recovery line arrives and names the
    device that recovered. Assert on the recorded log lines rather than on a count alone — a count
    cannot tell "the right pump recovered" from "a pump recovered".

    Second case: both devices fail at different moments inside one reminder interval. Assert each gets
    its own warning, which is the property a per-kind `warnedAt` gives for free and which an
    account-wide kind cannot give at all.

    The four shipped single-device cases and the recovery-line constant at
    `test/runtime/accountRuntime.test.ts:74` change with the per-device kind, since the kind is now
    the subject of the recovery sentence and names the device. Update them to the new text; do not
    weaken any of them to a substring match, because the whole line is what an owner reads and
    matching a fragment is how a message drifts.

    Then apply V14's mutation: collapse the per-device latch to a single boolean answered by whether
    any device is silent. Run the file's tests, record which case failed on what line, revert, confirm
    green. If the collapse fails nothing, say so as a null result and say where the behaviour is
    pinned instead — that outcome is a finding, not a failure to hide.
  </action>

  <verify>
    <automated>O=$(npm run build:test 2>&amp;1 &amp;&amp; node --test 'dist-test/test/runtime/accountRuntime.test.js' 2>&amp;1); R=$?; test $R -eq 0 &amp;&amp; printf '%s\n' "$O" | grep -qE '^ℹ fail 0$'</automated>
    <fails_when>The command exits non-zero, or the line `ℹ fail 0` is absent from the captured output.</fails_when>
  </verify>

  <verify>
    <automated>O=$(npm run test:unit 2>&amp;1); R=$?; test $R -eq 0 &amp;&amp; printf '%s\n' "$O" | grep -qE '^ℹ fail 0$'</automated>
    <fails_when>The command exits non-zero, or `ℹ fail 0` is absent — a shipped live-reporting case left on the old text surfaces here.</fails_when>
  </verify>

  <acceptance_criteria>
    - A case exists in which a silent pump's recovery is reported once, naming that pump, while its
      heartbeating neighbour produces no such line (V14, D-14).
    - A case exists in which two devices failing inside one reminder interval each produce their own
      warning.
    - The four shipped live-reporting cases assert the whole new line, not a fragment.
    - V14's mutation is applied, its outcome recorded with a test name and line — or as an explicit
      null result naming where the behaviour is pinned instead (D-11).
  </acceptance_criteria>
  <done>A quiet pump neither suppresses nor borrows its neighbour's recovery report, two devices failing in one interval each warn, and V14's outcome is recorded (D-14).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A removed device leaves nothing behind</name>

  <files>src/runtime/accountRuntime.ts, test/runtime/accountRuntime.test.ts</files>

  <read_first>
    - `src/runtime/accountRuntime.ts:377-383` — the confirmed-removal branch, and the comment at
      `:377-381` establishing this as the only place stamps are pruned and why every other candidate
      site is wrong.
    - `src/runtime/monitoringHealth.ts:200-202` — `forgetDevice`, the one-liner this mirrors.
    - `src/runtime/failureLog.ts:62-68` — `recordSuccess`, which deletes the kind and reports whether
      it was failing.
    - `test/runtime/accountRuntime.test.ts` — the shipped device-removal cases; find them by the
      `onDeviceRemoved` recorder at `:355-357`.
    - `05.1-VALIDATION.md` row V15.
  </read_first>

  <behavior>
    - A device whose live reporting is currently failing, then confirmed removed, leaves no entry
      under its kind: a later device reusing that identifier warns again rather than being silently
      rate-limited by its predecessor.
    - The latch entry for the removed device is dropped in the same place.
    - Deleting the drop makes the case fail.
  </behavior>

  <action>
    At `src/runtime/accountRuntime.ts:382`, beside `health.forgetDevice(deviceId)`, drop the removed
    device's live-reporting kind and its latch entry. Use the failure log's own success path rather
    than reaching into `warnedAt`: `recordSuccess` deletes the kind and reports whether it was
    failing, which is the shipped mechanism and keeps the log's own state and its lines from
    disagreeing. Extend the existing comment at `:377-381` to say that everything keyed by a device is
    pruned here and nowhere else — the comment already argues why this is the only correct site, so
    add the new subject to that argument rather than writing a second comment beside it.

    Be honest about one consequence and write it in the code comment: dropping the kind through the
    success path emits the recovery line for a device that did not recover, it was removed. Decide
    which reading an owner is better served by and say why in the comment. If you suppress the line
    instead, the suppression needs its own reason and its own case, and the SUMMARY must say which of
    the two shipped.

    Add the case: a device whose live reporting is failing, then confirmed absent across the two
    confirming polls the removal path requires, and an assertion that its kind no longer holds a
    warning timestamp — observed through behaviour, by having a device with that identifier fail again
    and warn rather than fall to debug.

    Then apply V15's mutation: delete the drop. The case must go red. Record which case failed on what
    line, revert, confirm green.

    Coverage is a real gate. Run the module-scoped coverage command below and add whatever case it
    names as uncovered.
  </action>

  <verify>
    <automated>O=$(npm run build:test 2>&amp;1 &amp;&amp; node --test 'dist-test/test/runtime/accountRuntime.test.js' 2>&amp;1); R=$?; test $R -eq 0 &amp;&amp; printf '%s\n' "$O" | grep -qE '^ℹ fail 0$'</automated>
    <fails_when>The command exits non-zero, or `ℹ fail 0` is absent from the captured output.</fails_when>
  </verify>

  <verify>
    <automated>O=$(npm run test:coverage:direct -- dist-test/src/runtime/accountRuntime.js 'dist-test/test/runtime/accountRuntime.test.js' 2>&amp;1); R=$?; test $R -eq 0</automated>
    <fails_when>The command exits non-zero; the coverage table names `accountRuntime.js` below 100 on lines, branches or functions and `node --test` prints the matching `coverage required` error.</fails_when>
  </verify>

  <verify>
    <automated>O=$(npm test 2>&amp;1); R=$?; test $R -eq 0 &amp;&amp; printf '%s\n' "$O" | grep -qE '^ℹ fail 0$' &amp;&amp; printf '%s\n' "$O" | awk '/^[0-9]+ scenarios \([0-9]+ passed\)$/ { if ($1 &gt;= 103) ok=1 } END { exit ok ? 0 : 1 }'</automated>
    <fails_when>The command exits non-zero, or `ℹ fail 0` is absent, or no `N scenarios (N passed)` line with N at least 103 appears. Run this alone — a concurrent run has been measured producing two false failures.</fails_when>
  </verify>

  <acceptance_criteria>
    - A confirmed removal drops the device's live-reporting kind and its latch entry, at
      `src/runtime/accountRuntime.ts:377-383` and nowhere else (V15, D-14).
    - The comment at `:377-381` is extended to cover the new subjects rather than duplicated.
    - The consequence of the chosen drop mechanism is written in the code and named in the SUMMARY.
    - V15's mutation is applied, the failing case recorded by name and line, reverted, green confirmed.
    - `dist-test/src/runtime/accountRuntime.js` is at 100/100/100.
    - `npm test`, run alone, is green at at least 103 scenarios and `ℹ fail 0`.
  </acceptance_criteria>
  <done>A confirmed removal drops the device's failure-log kind and latch entry at the single pruning site, pinned by a case, with V15's outcome recorded (D-14).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin → Homebridge log | The per-device live-reporting kind and reason cross here, carrying a `deviceId`. |
| vendor inventory → runtime device set | A confirmed removal crosses here and is what triggers the pruning this plan adds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.1-04 | Denial of service | `warnedAt` in `src/runtime/failureLog.ts:45` and the latch set in `src/runtime/accountRuntime.ts:282` | medium | mitigate | Both are dropped at the single confirmed-removal site, `src/runtime/accountRuntime.ts:382`. V15's mutation — delete the drop — must fail the new case. Growth is bounded by the devices the account actually holds. |
| T-05.1-07 | Repudiation | a recovery line emitted for a device that was removed rather than recovered | low | mitigate | The chosen mechanism and its consequence are written into the code comment and named in the SUMMARY, so a later reader is not left to infer why a removed device announced a recovery. |
| T-05.1-03 | Information disclosure | the per-device kind and reason strings | low | mitigate | Only the vendor `deviceId` is added, which the Phase 2 ruling of 2026-08-29 records as non-sensitive and permitted in logs. No token, URL, response body or account identifier enters either string. |
| T-05.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package is installed by this phase. |
</threat_model>

<verification>
- `npm test`, run alone, reports `ℹ fail 0` and at least 103 scenarios all passed.
- `dist-test/src/runtime/accountRuntime.js` at 100/100/100.
- `npm run typecheck`, `npm run lint`, `npm run fallow` all exit 0.
- V14 and V15 mutation outcomes recorded with test names and lines, or as explicit null results.
</verification>

<success_criteria>
The recovery latch and the live-reporting observation are per device and pinned as such, and nothing
keyed by a device outlives the device.
</success_criteria>

<output>
Create `.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-03-SUMMARY.md` when done.
Record: the V14 and V15 mutation outcomes with test names and lines; which drop mechanism shipped and
what an owner sees because of it; whether any gate had to be re-run alone; and the D-10 limit.
</output>
