---
phase: 05.1-per-pump-trust-and-monotonic-silence
plan: 06
type: execute
wave: 6
depends_on:
  - 05.1-05
files_modified:
  - src/accessories/basementGuardian.ts
  - test/accessories/basementGuardian.test.ts
autonomous: true
requirements:
  - RES-02
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - The controller-link log names every service the marking pass withdraws, including the Self-Test and Alarm Mute switches, and the name list cannot drift from the marked set without failing a test (criterion 2, D-05).
    - The message names only services this accessory actually published, so a fault an administrator suppressed is never named (D-05a).
    - The message does not name the one row that tolerates the cause, because that row keeps vouching.
    - Two catalogue rows sharing a kind — backup-battery appears twice with different display names — both appear in the derived message, neither merged nor dropped.
    - When no published row stops vouching, the log emits no sentence naming nothing.
    - Names appear in a deterministic published order, so the rendering is pinnable and the log does not churn between transitions.
    - statement: Service display names in the derived message are ASCII catalogue constants; no vendor-supplied name enters the list.
      verification: backstop
  artifacts:
    - src/accessories/basementGuardian.ts derives the withdrawn-service names from the descriptors it just published.
    - test/accessories/basementGuardian.test.ts pins the whole rendered line and replaces the hand-written constant at :134-136.
  key_links:
    - The derivation runs where reportControllerLink is already called, one line after published is recomputed, so it reads the marked set as it exists on this accessory now.
    - A descriptor is matched to its catalogue row by kind and serviceUuid, because toRow sets subtype to the kind verbatim and two rows share the kind backup-battery.
  prohibitions:
    - The derived message must not be rebuilt from the whole catalogue instead of from the accessory's published descriptors.
    - The message must not be filtered by the untrusted reason, which under-names during a simultaneous monitoring degradation.
    - The accessory's HomeKit display name, the vendor name, or any user rename must not enter the list.
    - The log-once discipline must not change; the message still prints once per transition.
---

<objective>
Make the controller-link log name every service the marking pass withdraws, derived from the marked
set rather than written by hand, so it cannot drift from behaviour by construction.

Purpose: ledger entry 6. **And the premise this phase inherited was wrong, so correct it here.**
`STATE.md` and the Phase 5 handoff both say the log "marks 16 and tells the operator about 11". It
names no services at all — it names five scope words, and the figure 11 is not derivable from any
source. Measured against the shipped catalogue: 17 rows, 16 non-connectivity, **15 that actually stop
vouching** across 7 scopes, with `Pump Controller Link Lost` tolerating its own cause. The success
criterion is unaffected by which wrong number preceded it, but the record must carry the correction.

Output: a message built from the descriptors this accessory just published, a test that pins the whole
rendered line, and cases holding the suppression, tolerance, adjacency, empty and ordering edges.
</objective>

<execution_context>
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-CONTEXT.md
@.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-RESEARCH.md
@.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-PATTERNS.md
@.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-VALIDATION.md
@.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-05-SUMMARY.md
</context>

<phase_ground_rules>
1. **`pipefail` is OFF.** Never pipe a suite command into a filter.
2. **Concurrent suite runs produce false reds.** One suite at a time; re-run a red gate alone before
   believing it, and say in the summary if you had to.
3. **Coverage is a real gate** at 100/100/100 over `dist-test/src/**/*.js`. The empty-list branch is
   not reachable from any Cucumber scenario and needs a unit case.
4. **`npm run fallow`** runs `dead-code`, `health` and `dupes`.
5. **Node 24 is not installed.** Claim no Node 24 result.
6. **Stay on `features/phase-05-degraded-operation-and-recovery`.**
7. **Git:** `pre-commit run --files <paths>` → fix → restage → re-run → `git commit`. Never
   `--no-verify`, never rebase. Verify by content afterwards.
8. **Mutation discipline (D-11, binding):** apply, watch the named test fail, record the test and the
   line, revert, confirm green. A mutation that fails nothing is a finding to write down.
</phase_ground_rules>

<artifacts_this_plan_produces>
| Symbol / name | Home | New or changed |
|---|---|---|
| `withdrawnServiceNames()` | `src/accessories/basementGuardian.ts` | new, closure-internal |
| the rendered controller-link warning | `src/accessories/basementGuardian.ts:833-836` | changed from a hand-written sentence to a derived one |
| `CONTROLLER_LINK_WARNING` | `test/accessories/basementGuardian.test.ts:134-136` | replaced |
</artifacts_this_plan_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive the sentence from what this accessory published</name>

  <files>src/accessories/basementGuardian.ts, test/accessories/basementGuardian.test.ts</files>

  <read_first>
    - `src/accessories/basementGuardian.ts:817-838` — `reportControllerLink` and the docblock stating
      the three properties the new prose must preserve: it names the `deviceId`, it quotes no
      credential, and it says values are retained rather than refreshed.
    - `src/accessories/basementGuardian.ts:181-208` — `TRUST_SCOPES` and `NON_CONNECTIVITY_SCOPES`,
      and the comment at `:199-207` saying a lost controller link poisons the same seven.
    - `src/accessories/basementGuardian.ts:496`, `:700-742` (`publishRows`, including the suppression
      gate at `:717-721` and the descriptor push at `:738`), `:995-1007` (the call site, one line
      after `published` is recomputed).
    - `src/accessories/services.ts:95-103` — `ServiceDescriptor`, and note `:99-100`: the service type
      identifier "is what tells two rows of one kind and subtype apart".
    - `src/accessories/serviceCatalogue.ts:121-135` (`RowTrust` and `ServiceRow`), `:628-645`
      (`toRow`, where the subtype is set to the kind verbatim), `:735-757` (the two rows sharing the
      kind `backup-battery` with different display names and different service classes),
      `:786-792` (the one row tolerating this cause), `:920-928` (`ensureService` returning nothing
      for a row that projected no value), `:812`, `:821` (the two rows that always publish).
    - `src/accessories/basementGuardian.ts:334-341` and `:347-369` — `distrustReasonsOf` and the
      comment saying the order of the last two is a decision rather than an accident. This is the
      reason the filter is **not** written over the untrusted reasons.
    - `05.1-RESEARCH.md` § *Q4* in full, and § *Pattern 3*.
  </read_first>

  <behavior>
    - With the controller link lost, the warning names each published service that stops vouching, in
      the catalogue's declared publication order, on one line.
    - The warning still names the device, quotes no credential, and says the values are retained
      rather than refreshed.
    - The warning is still emitted once per transition, not once per poll.
    - Adding a catalogue row under a non-connectivity scope changes the warning; a hand-written
      sentence would not.
  </behavior>

  <action>
    Add a closure-internal helper to `src/accessories/basementGuardian.ts` that answers the display
    names of the published services a lost controller link withdraws, and call it from
    `reportControllerLink`. `published` is a closure variable recomputed one line above the call at
    `:1004`, so the helper reads the marked set as it exists on this accessory on this update — which
    is the only source that respects two facts a catalogue-wide derivation misses: a fault an
    administrator suppressed is removed rather than published, and a row that has never projected a
    value is not published at all.

    **Match a descriptor to its catalogue row on the kind together with the service type identifier,
    and write the reason down.** The measured fact is that `toRow` sets the subtype to the kind
    verbatim (`src/accessories/serviceCatalogue.ts:633`), so matching on kind and subtype collapses to
    matching on kind alone — and two rows share the kind `backup-battery` with different display
    names and different service classes. The descriptor's service type identifier is documented at
    `src/accessories/services.ts:99-100` as exactly what tells those two apart. Matching on the
    descriptor's own display name would also be unique, and it is rejected for a stated reason: it
    couples the join to the very string being rendered, so a display-name change would silently
    unhook the join instead of failing.

    Filter the matched rows by membership of the non-connectivity scope set, minus rows that tolerate
    this cause. **Do not filter by the untrusted reasons**, and say in the comment why not: the
    layering in `distrustReasonsOf` fills monitoring scopes before controller-link scopes, so a scope
    already carrying a monitoring reason never receives this one, and a message built that way would
    under-name during a simultaneous monitoring degradation. That ordering is deliberate — the comment
    at `:334-341` says so — and a later reader will simplify the filter back unless the rejection is
    written down.

    Render one line. Keep all four properties the existing docblock states, join the names with a
    comma and a final conjunction, and keep the closing clause about values being retained rather than
    refreshed. Handle the case where the list is empty by emitting a sentence that names no services
    at all rather than a sentence with a dangling list — a log line naming nothing is worse than the
    prose it replaced. The log-once discipline above it is untouched.

    Replace the pinned constant at `test/accessories/basementGuardian.test.ts:134-136` with the whole
    new expected line, written out rather than matched on a fragment, keeping that constant's comment
    about why the whole line is asserted. The two log-once cases at `:1705` and `:1719` assert the
    *count* rather than the text, so they survive a rewording: **keep them passing untouched**, since
    they are the regression signal that the derived message did not break the transition latch.

    Then apply V4's mutation: flip the `Alarm Mute` row's scope to connectivity, or add a row under a
    non-connectivity scope, without touching the message. The pinned rendering must fail. Record which
    case failed on what line, revert, confirm green.

    **Carry the correction into the SUMMARY.** The message named five scope words, not eleven
    services; the figure 11 in `STATE.md` and in the Phase 5 handoff is not derivable from source. The
    real counts are 17 catalogue rows, 16 non-connectivity, 15 that stop vouching, across 7 scopes.
    Plan 05.1-07 carries this into the records from your summary.
  </action>

  <verify>
    <automated>O=$(npm run build:test 2>&amp;1 &amp;&amp; node --test 'dist-test/test/accessories/basementGuardian.test.js' 2>&amp;1); R=$?; test $R -eq 0 &amp;&amp; printf '%s\n' "$O" | grep -qE '^ℹ fail 0$'</automated>
    <fails_when>The command exits non-zero, or `ℹ fail 0` is absent from the captured output — the replaced constant not matching the rendered line surfaces here as a failing assertion naming both strings.</fails_when>
  </verify>

  <verify>
    <automated>O=$(npm run typecheck 2>&amp;1 &amp;&amp; npm run lint 2>&amp;1); R=$?; test $R -eq 0</automated>
    <fails_when>Either exits non-zero: `tsc` prints `error TS####` lines, ESLint prints a `problems (N errors` summary.</fails_when>
  </verify>

  <acceptance_criteria>
    - The warning is derived from the accessory's published descriptors at the existing call site
      (D-05, D-05a).
    - The descriptor-to-row join is on kind and service type identifier, and both the choice and the
      rejected alternative are written in the code.
    - The filter is over the non-connectivity scope set minus tolerating rows, with the rejection of
      the untrusted-reason filter written down.
    - The rendered line names the device, quotes no credential, says values are retained rather than
      refreshed, and fits one line.
    - `test/accessories/basementGuardian.test.ts:1705` and `:1719` still pass untouched.
    - V4's mutation applied, failing case recorded by name and line, reverted, green confirmed.
    - The SUMMARY carries the corrected counts and states that the 11-of-16 figure is not derivable
      from source.
  </acceptance_criteria>
  <done>The controller-link warning is derived from the descriptors this accessory published, the whole line is pinned, the log-once cases still pass, and V4 is recorded (D-05, D-05a).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The suppressed row, the tolerating row, the twin rows, the empty list, and the order</name>

  <files>test/accessories/basementGuardian.test.ts</files>

  <read_first>
    - `test/accessories/basementGuardian.test.ts:100-136` — `CONTROLLER_LINK_ROW`, `SELF_TEST_ROW`,
      `ALARM_MUTE_ROW`, `FAULT_READING_SERVICES` and the replaced warning constant.
    - `test/accessories/basementGuardian.test.ts:1448-1535` — the five shipped `ignoredFaults` cases,
      which are the fixture shape for a suppression case.
    - `test/accessories/basementGuardian.test.ts` — `accessoryWith`, `recordingLog`, `registryWith`,
      `linkOutcome`, `buildSnapshot`, `registryOver`; find them by their uses at `:1705-1730`.
    - `src/accessories/serviceCatalogue.ts:735-757` — the two `backup-battery` rows and their display
      names.
    - `src/accessories/services.ts:43-50` — the notification service kinds, six of seven of which are
      non-connectivity and therefore suppressible.
    - `05.1-VALIDATION.md` rows V5 and V6.
  </read_first>

  <behavior>
    - With a fault suppressed by configuration, the warning does not name that service, and the
      remaining names are unchanged.
    - The warning never names the one row that tolerates this cause, and that row's own status stays
      active.
    - Both services sharing the `backup-battery` kind appear in the warning, under their own display
      names, neither merged nor dropped.
    - With no published row that stops vouching, the warning carries no list and names no service.
    - Two consecutive transitions render the names in the same order.
    - Every name in the warning is a catalogue display name; a user rename of the accessory does not
      appear in it.
  </behavior>

  <action>
    Add the cases the derived message needs and that the existing file has no equivalent of.

    **Suppression (V5).** Build an accessory with a notification fault suppressed by configuration,
    following the shipped `ignoredFaults` fixtures, drive a lost link, and assert the whole rendered
    line — with the suppressed service absent from it and every other name present. Its mutation:
    build the message from the whole catalogue rather than from the published descriptors. The case
    must go red.

    **Tolerance (V6).** Assert that the one row tolerating this cause is absent from the names while
    its own status stays active in the same update, so the case proves the two facts together rather
    than one at a time. Its mutation: drop the tolerating-row term from the filter. The case must go
    red.

    **The twin rows.** Assert that both display names of the two rows sharing the `backup-battery`
    kind appear. This is the case that catches a join collapsing to the kind alone, and it is why the
    join carries the service type identifier. Its mutation: match on kind and subtype only. The case
    must go red, and if it does not, the join is not doing what task 1 says it does — report that.

    **The empty list.** Build an accessory that publishes no row which stops vouching, drive a lost
    link, and assert the rendered line names no service and reads as a sentence rather than as a
    sentence with a hole in it. This branch is unreachable from any Cucumber scenario, and the 100
    percent branch gate is why it must exist here.

    **The order.** Drive two transitions — lost, recovered, lost — and assert the two rendered lines
    are identical. A log that reorders its names between transitions churns and cannot be pinned.

    **The names' provenance.** Rename the accessory to the deliberately unlike name the file already
    keeps for this purpose (`USER_RENAME` at `test/accessories/basementGuardian.test.ts:140`), drive a
    lost link, and assert that name does not appear in the warning. The list is built from catalogue
    display names, which are ASCII constants declared in one place; this case is the backstop that
    keeps a vendor-supplied or user-supplied string out of a log line.

    Run each named mutation, record which case failed on what line, revert, confirm green after each.
    Then run the module-scoped coverage command and add whatever case it names as uncovered.
  </action>

  <verify>
    <automated>O=$(npm run test:coverage:direct -- dist-test/src/accessories/basementGuardian.js 'dist-test/test/accessories/basementGuardian.test.js' 2>&amp;1); R=$?; test $R -eq 0 &amp;&amp; printf '%s\n' "$O" | grep -qE '^ℹ fail 0$'</automated>
    <fails_when>The command exits non-zero, or `ℹ fail 0` is absent. Below-100 coverage prints a `coverage required` error and names `basementGuardian.js` in the table under 100 — the empty-list branch left unexercised is what surfaces there.</fails_when>
  </verify>

  <verify>
    <automated>O=$(npm test 2>&amp;1); R=$?; test $R -eq 0 &amp;&amp; printf '%s\n' "$O" | grep -qE '^ℹ fail 0$' &amp;&amp; printf '%s\n' "$O" | awk '/^[0-9]+ scenarios \([0-9]+ passed\)$/ { if ($1 &gt;= 104) ok=1 } END { exit ok ? 0 : 1 }'</automated>
    <fails_when>The command exits non-zero, or `ℹ fail 0` is absent, or no all-passed scenario line with at least 104 appears. Run alone; re-run a red alone before believing it.</fails_when>
  </verify>

  <verify>
    <automated>O=$(npm run test:coverage:all 2>&amp;1); R=$?; test $R -eq 0 &amp;&amp; printf '%s\n' "$O" | grep -qE '^ℹ fail 0$'</automated>
    <fails_when>The command exits non-zero, or `ℹ fail 0` is absent — a branch anywhere under `dist-test/src/` left below 100 by this plan surfaces here.</fails_when>
  </verify>

  <acceptance_criteria>
    - A suppression case exists and V5's mutation fails it.
    - A tolerance case exists, asserting both the absence from the names and the row's own active
      status, and V6's mutation fails it.
    - A case asserts both `backup-battery` display names appear, and the kind-and-subtype-only join
      fails it — or the null result is reported.
    - An empty-list case exists and the rendered line names no service.
    - An ordering case asserts two transitions render identically.
    - A case asserts a user rename of the accessory does not enter the warning.
    - `dist-test/src/accessories/basementGuardian.js` at 100/100/100, and
      `npm run test:coverage:all` at 100/100/100.
    - `npm test`, run alone, green at at least 104 scenarios and `ℹ fail 0`.
  </acceptance_criteria>
  <done>Suppression, tolerance, the twin backup-battery rows, the empty list, the ordering and the name provenance each have a case, with V5, V6 and the join mutation recorded (D-05a).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin → Homebridge log | The derived warning crosses here, now carrying a list of service names alongside the `deviceId` it already carried. |
| vendor payload / user configuration → the rendered list | Neither may reach the list. The names come from the catalogue; the accessory's HomeKit display name and the vendor name must not. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.1-17 | Information disclosure | the derived name list in `src/accessories/basementGuardian.ts:833-836` | medium | mitigate | Every name is a catalogue `displayName`, an ASCII constant declared once in `src/accessories/serviceCatalogue.ts`. The vendor name, the accessory's HomeKit display name and any user rename are excluded by construction and pinned by the rename case in task 2. The `deviceId` already in the line is the Phase 2 ruling's non-sensitive value. No token, URL, response body or account identifier can reach the line. |
| T-05.1-18 | Repudiation | a diagnostic that under-names what it withdrew | high | mitigate | The list is derived from the marked set rather than written by hand, so it cannot drift by construction, and V4's mutation — change the catalogue without touching the message — must fail the pinned rendering. The filter is over the scope set rather than the untrusted reasons, so a simultaneous monitoring degradation cannot silently shorten it. |
| T-05.1-19 | Repudiation | a diagnostic that over-names, listing a tile that does not exist | medium | mitigate | The source is the descriptors this accessory actually published, so a suppressed fault and an unprojected row are absent. V5's mutation — derive from the whole catalogue — must fail the suppression case. |
| T-05.1-20 | Denial of service | log volume | low | accept | The log-once discipline at `src/accessories/basementGuardian.ts:829-831` is untouched: the line prints once per transition, not per poll, and the two shipped count cases at `:1705` and `:1719` keep passing as the regression signal. |
| T-05.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package is installed. |
</threat_model>

<verification>
- `npm test`, run alone, reports `ℹ fail 0` and at least 104 scenarios all passed.
- `npm run test:coverage:all` at 100/100/100.
- `npm run typecheck`, `npm run lint`, `npm run fallow` all exit 0.
- V4, V5, V6, the twin-row join mutation and the ordering case each recorded with a test and a line,
  or as an explicit null result.
- The SUMMARY carries the corrected counts — 17 rows, 16 non-connectivity, 15 that stop vouching,
  7 scopes — and states that the 11-of-16 figure is not derivable from any source.
</verification>

<success_criteria>
The controller-link log names every service the marking pass withdraws, including the Self-Test and
Alarm Mute switches, and the name list cannot drift from the marked set without failing a test.
</success_criteria>

<output>
Create `.planning/phases/05.1-per-pump-trust-and-monotonic-silence/05.1-06-SUMMARY.md` when done.
Record: the rendered line in full, both for a default install and for the empty case; the mutation
outcomes with tests and lines; the corrected counts and the fact that the inherited figure was not
derivable; and whether any gate had to be re-run alone.
</output>
