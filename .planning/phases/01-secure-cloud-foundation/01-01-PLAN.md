---
phase: 01-secure-cloud-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .npmignore
  - CHANGELOG.md
  - tsconfig.test.json
  - eslint.config.js
  - cucumber.json
  - .gitignore
  - .prettierignore
  - test/hbConfig/config.json
  - test/hbConfig/config.example.json
  - src/platform.ts
  - src/platformAccessory.ts
  - src/@types/homebridge-lib.d.ts
  - test/plugin.test.mjs
  - test/index.test.ts
  - test/settings.test.ts
  - test/platform.test.ts
autonomous: true
requirements: [CONF-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The plugin installs and loads as a Homebridge dynamic platform with no runtime dependency other than `mqtt` (D-02, CONF-01)."
    - "A user who installs this build receives no example Lightbulb accessory and no example motion sensors, and no unmanaged interval keeps the process alive (D-01)."
    - "`configureAccessory` records a restored accessory in the platform map and performs no other action; no code path calls `unregisterPlatformAccessories` (D-03)."
    - "`src/index.ts` registers exactly one platform constructor under the name `BasementGuardian` and reads nothing from `api.hap` while doing so."
    - "Registering the platform a second time under the same name is prevented by `singular: true` in `config.schema.json`; `PLATFORM_NAME`, `pluginAlias`, and the `package.json` `name` stay mutually aligned (CONF-01)."
    - statement: "Restored accessories are recorded in `configureAccessory` call order and the `Map` preserves that insertion order; no Phase 1 behavior depends on that order."
      verification: backstop
    - "`npm run check` passes with `typecheck`, `lint`, all three `fallow` sub-commands, `format:check`, and both test suites (D-12)."
    - "`npm pack --dry-run` lists only `dist/`, `config.schema.json`, `CHANGELOG.md`, `package.json`, `README.md`, and `LICENSE` (D-21)."
  prohibitions:
    - statement: "MUST NOT unregister, delete, or otherwise remove any cached HomeKit accessory during this phase, for any reason (D-03)."
      status: resolved
      verification: judgment
    - statement: "MUST NOT include raw research material, vendor scripts, captured cloud responses, planning artifacts, or agent runtime directories in the packed npm artifact (D-21)."
      status: resolved
      verification: judgment
  artifacts:
    - path: "package.json"
      provides: "Dependency set, test/build scripts, and the `files` publish allowlist"
      contains: "\"files\""
    - path: "tsconfig.test.json"
      provides: "Second build configuration emitting src, test, and features into dist-test"
      contains: "dist-test"
    - path: "cucumber.json"
      provides: "default and real Cucumber profiles"
      contains: "\"real\""
    - path: "CHANGELOG.md"
      provides: "Keep a Changelog structure with an Unreleased section"
      contains: "## [Unreleased]"
    - path: "src/platform.ts"
      provides: "Dynamic platform composition root that records restored accessories only"
      exports: ["BasementGuardianPlatform"]
    - path: "test/index.test.ts"
      provides: "Registration assertion replacing the template smoke test"
  key_links:
    - from: "eslint.config.js"
      to: "tsconfig.test.json"
      via: "parserOptions.project lists both tsconfig files so typed rules parse test and feature sources"
      pattern: "tsconfig\\.test\\.json"
    - from: "package.json"
      to: "tsconfig.test.json"
      via: "build:test compiles with the second project file"
      pattern: "tsc -p tsconfig\\.test\\.json"
    - from: "src/index.ts"
      to: "src/platform.ts"
      via: "registerPlatform receives the platform constructor"
      pattern: "registerPlatform\\(PLATFORM_NAME"
---

<objective>
Make this repository able to build, lint, test, and pack the Phase 1 runtime, and remove the Homebridge template scaffold that would otherwise ship fake accessories into a user's HomeKit.

Purpose: three quality gates fail on the very first TypeScript test file that lands, and `npm pack` currently ships 755 files including the raw research archive. Every later plan in this phase depends on this one being green. This plan also executes the `D-01` teardown, `D-02` dependency removal, `D-09` compile-then-test build model, `D-12` combined test script, `D-21` packaging allowlist, and `D-23` changelog bootstrap.

Output: a repository where `npm run check` passes, `npm pack --dry-run` lists only intended files, and `src/` holds no template code.
</objective>

<execution_context>
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secure-cloud-foundation/01-CONTEXT.md
@.planning/phases/01-secure-cloud-foundation/01-RESEARCH.md
@.planning/phases/01-secure-cloud-foundation/01-PATTERNS.md
@CLAUDE.md
@.claude/rules/typescript-style-guide.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@.claude/rules/changelog.md
</context>

<interfaces>
<!-- Contracts the executor needs. Do not go looking for these in the codebase. -->

The `Logging` interface from `homebridge` is a CALLABLE interface with seven members, identical in
`homebridge@1.8.0` and `homebridge@2.4.0`:

```typescript
export interface Logging {
  prefix: string;
  (message: string, ...parameters: any[]): void;
  info: (message: string, ...parameters: any[]) => void;
  success: (message: string, ...parameters: any[]) => void;
  warn: (message: string, ...parameters: any[]) => void;
  error: (message: string, ...parameters: any[]) => void;
  debug: (message: string, ...parameters: any[]) => void;
  log: (level: LogLevel, message: string, ...parameters: any[]) => void;
}
```

Existing symbols this plan keeps unchanged:

```typescript
// src/settings.ts
export const PLATFORM_NAME = 'BasementGuardian';
export const PLUGIN_NAME = 'homebridge-basement-guardian';

// src/index.ts
export default (api: API): void => { api.registerPlatform(PLATFORM_NAME, BasementGuardianPlatform); };
```

Symbols this plan DELETES from `src/platform.ts`: `BasementGuardianDevice`, `EXAMPLE_DEVICES`,
`CustomServiceConstructor`, `CustomServices`, `CustomCharacteristics`, `discoveredCacheUUIDs`,
`discoverDevices`, `discoverDevice`, `registerAccessory`, `restoreAccessory`,
`removeStaleAccessories`, and the `EveHomeKitTypes` import.

Symbols this plan KEEPS in `src/platform.ts`: the class declaration, `implements DynamicPlatformPlugin`,
the parameter-property constructor, `BasementGuardianAccessoryContext`,
`BasementGuardianPlatformAccessory`, and `accessories` as a `Map<string, BasementGuardianPlatformAccessory>`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Swap the dependency set, add the build and test scripts, and close the packaging leak</name>
  <read_first>
    - `package.json` (current dependencies, devDependencies, and scripts)
    - `.npmignore` (the denylist being replaced)
    - `.claude/rules/changelog.md` (binding entry rules)
    - `.planning/phases/01-secure-cloud-foundation/01-RESEARCH.md` sections "Standard Stack", "Package Legitimacy Audit", "Common Pitfalls" pitfall 6, and "Code Examples" subsection "Cucumber ESM + TypeScript configuration"
  </read_first>
  <files>package.json, package-lock.json, .npmignore, CHANGELOG.md</files>
  <action>
Run `npm uninstall homebridge-lib ts-node` (D-02; `ts-node` has no consumer under the D-09 compile-then-test model). Run `npm install mqtt` to add the only new runtime dependency at `^5.15.2` (D-05 as resolved by research). Run `npm install --save-dev @cucumber/cucumber aedes ws strong-mock`. The package legitimacy audit in `01-RESEARCH.md` approved all five; none declares an `install` or `postinstall` script, so no human-verify gate applies. Expect `EBADENGINE` warnings because the development machine runs Node 26 while `engines.node` declares `^22.10.0 || ^24.0.0`; do not widen `engines`.

Add these `scripts` entries, keeping the existing ones intact: `build:test` as `rimraf ./dist-test && tsc -p tsconfig.test.json`; `test:unit` as `npm run build:test && node --test "dist-test/test/**/*.test.js"`; `test:cucumber` as `npm run build:test && cucumber-js`; `test` as `npm run test:unit && npm run test:cucumber` (D-12, so nothing can be skipped by accident); `test:coverage:direct` as a `node --test` invocation carrying `--experimental-test-coverage`, `--test-coverage-lines=100`, `--test-coverage-branches=100`, `--test-coverage-functions=100`, and a `--test-coverage-include` argument the caller supplies; `test:coverage:all` as the same flag set over the whole emitted suite. Both coverage scripts are referenced by `.claude/rules/typescript-unit-testing.md` and do not exist yet.

Replace denylist packaging with an allowlist (D-21): add `"files": ["dist", "config.schema.json", "CHANGELOG.md"]` to `package.json` and delete `.npmignore` entirely. npm force-includes `package.json`, `README*`, and `LICENSE` regardless. Leave `private: true`, `license: "Apache-2.0"`, `version`, and `main` untouched — STATE.md assigns those to a later phase.

Create `CHANGELOG.md` using Keep a Changelog structure with a `## [Unreleased]` section (D-23), holding `### Removed` and `### Changed` subsections that record this plan's two user-visible changes: the example accessories are gone, and the published package now contains only the compiled plugin and its settings schema. Apply the `simple-english` and `humanizer` skills before writing, keep each bullet to one sentence of 25 words or fewer, and cite no planning artifacts.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const d=Object.keys(p.dependencies||{}).sort(); if(JSON.stringify(d)!==JSON.stringify(['mqtt'])) throw new Error('runtime dependencies are '+JSON.stringify(d)); for (const s of ['build:test','test:unit','test:cucumber','test','test:coverage:direct','test:coverage:all']) if(!p.scripts[s]) throw new Error('missing script '+s); if(JSON.stringify(p.files)!==JSON.stringify(['dist','config.schema.json','CHANGELOG.md'])) throw new Error('files allowlist is '+JSON.stringify(p.files));"</automated>
    <automated>test ! -e .npmignore</automated>
    <automated>npm run build &amp;&amp; npm pack --dry-run --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const allowed=new Set(['package.json','README.md','LICENSE','CHANGELOG.md','config.schema.json']);const files=JSON.parse(s)[0].files.map(f=>f.path);const bad=files.filter(f=>!f.startsWith('dist/')&amp;&amp;!allowed.has(f));if(bad.length)throw new Error('unexpected packed files: '+bad.join(', '));if(!files.includes('config.schema.json'))throw new Error('config.schema.json missing from pack');process.stdout.write('packed '+files.length+' files\n')})"</automated>
    <automated>node -e "const t=require('fs').readFileSync('CHANGELOG.md','utf8'); if(!t.includes('## [Unreleased]')) throw new Error('CHANGELOG.md lacks an Unreleased section');"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies` has exactly one key, `mqtt`.
    - `package.json` `devDependencies` contains `@cucumber/cucumber`, `aedes`, `ws`, and `strong-mock`, and no longer contains `ts-node`.
    - `package.json` `scripts` contains all six new entries and `test` invokes both `test:unit` and `test:cucumber`.
    - `package.json` `files` deep-equals `["dist", "config.schema.json", "CHANGELOG.md"]`.
    - `.npmignore` does not exist.
    - `npm pack --dry-run --json` lists only paths under `dist/` plus `package.json`, `README.md`, `LICENSE`, `CHANGELOG.md`, and `config.schema.json`.
    - `CHANGELOG.md` exists, opens with Keep a Changelog headings, and contains `## [Unreleased]`.
    - `package-lock.json` is updated and committed alongside `package.json`.
  </acceptance_criteria>
  <done>The dependency set is `mqtt` at runtime plus the four new development packages, the six scripts exist, and `npm pack --dry-run` lists only allowlisted files.</done>
</task>

<task type="auto">
  <name>Task 2: Add the second TypeScript project and repair the typed-lint and ignore configuration</name>
  <read_first>
    - `tsconfig.json` (note `rootDir: "src"` and the narrow `include`)
    - `eslint.config.js` (note `projectService: true` in the `**/*.ts` block and the `ignores` list)
    - `.prettierignore`, `.gitignore`
    - `.planning/phases/01-secure-cloud-foundation/01-RESEARCH.md` "Common Pitfalls" pitfalls 1, 2, and 5
    - `.planning/phases/01-secure-cloud-foundation/01-RESEARCH.md` "Code Examples" subsection "Cucumber ESM + TypeScript configuration (verified working)"
  </read_first>
  <files>tsconfig.test.json, eslint.config.js, cucumber.json, .gitignore, .prettierignore, test/hbConfig/config.json, test/hbConfig/config.example.json</files>
  <action>
Create `tsconfig.test.json` extending `./tsconfig.json` with `compilerOptions` `rootDir: "."`, `outDir: "dist-test"`, `declaration: false`, and `include` of `["src", "test", "features"]`. This is the `D-09` second build configuration and it is simultaneously the fix for typed ESLint refusing to parse any `.ts` outside the root project's `include`.

In `eslint.config.js`, inside the block whose `files` is `['**/*.ts']`, replace `projectService: true` with `project: ['./tsconfig.json', './tsconfig.test.json']`, keeping `tsconfigRootDir: import.meta.dirname`. Add `'dist-test/**'` to the top-level `ignores` array in alphabetical position after `'dist/**'`. Append a new config block after the existing `**/*.ts` block, with `files: ['test/**/*.ts', 'features/**/*.ts']` and `rules: { '@typescript-eslint/no-floating-promises': 'off' }`; `node:test`'s `test()` returns a promise and `strictTypeChecked` makes every call site an error under `--max-warnings=0`.

Create `cucumber.json` with two top-level profile keys. The `default` profile sets `paths` to `["features/**/*.feature"]`, `import` to `["dist-test/features/**/*.js"]`, and `format` to `["summary"]`. The `real` profile sets `paths` to `["features/real-pump/**/*.feature"]`, the same `import`, `tags` of `"@real and @read-only"`, and the same `format`. Use `import` rather than `require`: compiled output under `module: nodenext` in a `"type": "module"` package is ESM. `paths` points at uncompiled `.feature` sources; only support code comes from `dist-test/`.

Add `dist-test` to `.gitignore` beside the existing `dist` entry, and `dist-test/` to `.prettierignore` beside `dist/`.

Close the development-credential exposure noted in `01-RESEARCH.md` "Runtime State Inventory": copy `test/hbConfig/config.json` to `test/hbConfig/config.example.json`, run `git rm --cached test/hbConfig/config.json`, and add `test/hbConfig/config.json` to `.gitignore`. That file will hold a real account password once a developer uses `npm run watch`, and it must not be tracked when that happens.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.test.json --noEmit</automated>
    <automated>node -e "const c=require('./cucumber.json'); for (const p of ['default','real']) { if(!c[p]) throw new Error('missing profile '+p); if(!Array.isArray(c[p].import)||!c[p].import[0].startsWith('dist-test/')) throw new Error(p+' does not import from dist-test'); } if(c.real.tags!=='@real and @read-only') throw new Error('real profile tags are '+c.real.tags);"</automated>
    <automated>printf 'import assert from %s;\nimport { test } from %s;\n\ntest(%s, () =&gt; {\n  assert.equal(1, 1);\n});\n' "'node:assert/strict'" "'node:test'" "'probe'" &gt; test/lintProbe.test.ts &amp;&amp; npx eslint test/lintProbe.test.ts --max-warnings=0; STATUS=$?; rm -f test/lintProbe.test.ts; exit $STATUS</automated>
    <automated>git ls-files --error-unmatch test/hbConfig/config.example.json &amp;&amp; git check-ignore -q test/hbConfig/config.json</automated>
  </verify>
  <acceptance_criteria>
    - `tsconfig.test.json` exists, extends `./tsconfig.json`, and sets `rootDir` to `"."`, `outDir` to `"dist-test"`, `declaration` to `false`, and `include` to `["src", "test", "features"]`.
    - `eslint.config.js` contains the string `tsconfig.test.json` inside `parserOptions.project` and no longer sets `projectService`.
    - A temporary `test/*.test.ts` probe file lints clean at `--max-warnings=0`, proving both the parsing fix and the floating-promise override.
    - `cucumber.json` declares `default` and `real` profiles, both importing from `dist-test/features/**/*.js`.
    - `.gitignore` contains `dist-test` and `test/hbConfig/config.json`; `.prettierignore` contains `dist-test/`.
    - `test/hbConfig/config.example.json` is tracked and `test/hbConfig/config.json` is ignored.
  </acceptance_criteria>
  <done>`npx tsc -p tsconfig.test.json --noEmit` succeeds, a probe test file under `test/` lints clean, and the Cucumber profiles resolve.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove the template scaffold and reduce the platform to a composition root</name>
  <read_first>
    - `src/platform.ts` in full, and `src/platformAccessory.ts` in full, before deleting anything
    - `src/index.ts`, `src/settings.ts`, `src/@types/homebridge-lib.d.ts`
    - `test/plugin.test.mjs` (the assertion shape worth preserving)
    - `.claude/rules/typescript-unit-testing.md` sections "Pairing and coverage", "Case structure", "Naming values", and "Test doubles"
    - `.claude/rules/typescript-style-guide.md` (named exports, `.js` extensions on relative imports, import ordering, semicolons)
    - `.planning/phases/01-secure-cloud-foundation/01-PATTERNS.md` section "`src/platform.ts` (composition root, event-driven) — rewrite"
  </read_first>
  <files>src/platform.ts, src/platformAccessory.ts, src/@types/homebridge-lib.d.ts, test/plugin.test.mjs, test/index.test.ts, test/settings.test.ts, test/platform.test.ts</files>
  <behavior>
    - `test/index.test.ts`: the default export calls `registerPlatform` exactly once with `'BasementGuardian'` and a constructor function, and touches nothing else on the injected API object.
    - `test/settings.test.ts`: `PLATFORM_NAME` equals `'BasementGuardian'` and matches `pluginAlias` in `config.schema.json`; `PLUGIN_NAME` equals `'homebridge-basement-guardian'` and matches `name` in `package.json`.
    - `test/platform.test.ts`: constructing the platform with a stub `Logging`, an empty `PlatformConfig`, and a stub `API` registers no listener and creates no accessory.
    - `test/platform.test.ts`: `configureAccessory` stores the accessory in `accessories` keyed by its `UUID`, and calling it twice with the same UUID leaves exactly one entry.
    - `test/platform.test.ts`: `configureAccessory` calls nothing on the injected `API` — a strict mock with no stated expectations proves the platform never reaches `registerPlatformAccessories`, `updatePlatformAccessories`, or `unregisterPlatformAccessories` (D-03).
  </behavior>
  <action>
Delete `src/platformAccessory.ts`, `src/@types/homebridge-lib.d.ts`, and `test/plugin.test.mjs`. The `@types` directory becomes empty and should be removed with it.

Rewrite `src/platform.ts` so it exports `BasementGuardianAccessoryContext`, `BasementGuardianPlatformAccessory`, and `BasementGuardianPlatform`. The class keeps `implements DynamicPlatformPlugin`, the parameter-property constructor `(log: Logging, config: PlatformConfig, api: API)`, and a public readonly `accessories` typed `Map<string, BasementGuardianPlatformAccessory>`. The constructor body logs one debug line naming the configured platform and does nothing else — no listener registration, no accessory work; configuration validation and runtime start-up arrive in later plans. `configureAccessory(accessory: PlatformAccessory): void` logs one info line and stores the accessory under `accessory.UUID` (D-03). Add no removal path of any kind: this phase must not delete anything from a user's HomeKit, and the conservative removal policy belongs to a later phase. `BasementGuardianAccessoryContext` becomes an interface extending `UnknownContext` with no members yet; a later phase adds the device field.

Write the three test modules named above under `test/`, mirroring the source paths, using `node:test`, `node:assert/strict`, and `strong-mock` only. Follow the rule file's structure: `test()` never `it()`, one top-level `describe()` per exported entrypoint, `// arrange` / `// act` / `// assert` phase comments, values named for their production role, and `verify(mock)` last in every case that uses a mock. Write semicolons, which `eslint.config.js` enforces even though the rule file's examples omit them. Cite `D-03` in the test title that proves no accessory is unregistered; do not cite plans, phases, waves, or tasks in any comment or title.

Run `npm run check` and fix everything it reports before committing. Expect `fallow dead-code --unused-deps` to confirm the removed packages are gone.
  </action>
  <verify>
    <automated>test ! -e src/platformAccessory.ts &amp;&amp; test ! -e src/@types &amp;&amp; test ! -e test/plugin.test.mjs</automated>
    <automated>npm run build:test &amp;&amp; node --test dist-test/test/index.test.js dist-test/test/settings.test.js dist-test/test/platform.test.js</automated>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `src/platformAccessory.ts`, `src/@types/homebridge-lib.d.ts`, and `test/plugin.test.mjs` do not exist.
    - `src/platform.ts` exports `BasementGuardianPlatform`, `BasementGuardianPlatformAccessory`, and `BasementGuardianAccessoryContext`, and imports nothing from `homebridge-lib`.
    - `src/platform.ts` contains no call to `registerPlatformAccessories`, `updatePlatformAccessories`, or `unregisterPlatformAccessories`.
    - `test/index.test.ts`, `test/settings.test.ts`, and `test/platform.test.ts` exist and pass under `node --test`.
    - `npm run test:coverage:direct` reports 100 percent lines, branches, and functions for each of the three source-test pairs run alone.
    - `npm run check` exits zero.
  </acceptance_criteria>
  <done>The template code is gone, `src/platform.ts` records restored accessories and nothing else, three mirrored test modules pass, and `npm run check` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → npm registry | Every tracked and untracked file in the working tree is a publication candidate until the `files` allowlist constrains it. |
| npm registry → developer machine | Newly installed packages execute install lifecycle scripts on this machine and on every user's Homebridge host. |
| repository → git history | A development configuration file under `test/hbConfig/` will hold a real account password once a developer runs `npm run watch`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information disclosure | `npm pack` output | high | mitigate | Replace the `.npmignore` denylist with a `files` allowlist of `dist`, `config.schema.json`, and `CHANGELOG.md`, delete `.npmignore`, and gate the change with a `npm pack --dry-run --json` assertion that every packed path is either under `dist/` or one of the four npm-forced files (Task 1). This removes the 735 agent-runtime files and the research archive holding live vendor scripts and captured cloud responses. |
| T-01-02 | Information disclosure | `test/hbConfig/config.json` | medium | mitigate | Untrack the file, commit `test/hbConfig/config.example.json` in its place, and add the original path to `.gitignore` before any developer supplies real credentials to it (Task 2). |
| T-01-03 | Denial of service | template `setInterval` and example accessories | medium | mitigate | Delete `src/platformAccessory.ts` and the unmanaged interval; the platform registers no listener and starts no timer (Task 3). |
| T-01-SC | Tampering | npm installs of `mqtt`, `@cucumber/cucumber`, `aedes`, `ws`, `strong-mock` | high | mitigate | The `01-RESEARCH.md` "Package Legitimacy Audit" table records first-publish date, weekly downloads, and source repository for each, and verified that all five report empty `scripts.install` and `scripts.postinstall`. No `[ASSUMED]` or `[SUS]-on-substance` package remains, so no blocking human checkpoint is required; the two `too-new` verdicts were resolved by first-publish date. `aws-crt` was rejected in part because it runs an `install` lifecycle script. |
| T-01-04 | Repudiation | Homebridge accessory cache | low | accept | A developer's Homebridge instance may already hold example accessories created by a prior `npm run watch`. `D-03` forbids the plugin from removing them, so they persist until the developer clears that instance's cache manually. Accepted: the alternative is a removal path this phase is explicitly not permitted to build. |
</threat_model>

## Artifacts this phase produces

New files created by this plan:

- `tsconfig.test.json` — second TypeScript project, `rootDir` `.`, `outDir` `dist-test`
- `cucumber.json` — `default` and `real` profiles
- `CHANGELOG.md` — Keep a Changelog structure with `## [Unreleased]`
- `test/index.test.ts`, `test/settings.test.ts`, `test/platform.test.ts`
- `test/hbConfig/config.example.json`

New npm scripts: `build:test`, `test:unit`, `test:cucumber`, `test:coverage:direct`, `test:coverage:all` (and a redefined `test`).

New package.json keys: `files`.

New dependencies: `mqtt` (runtime); `@cucumber/cucumber`, `aedes`, `ws`, `strong-mock` (development).

Exported symbols after this plan (`src/platform.ts`): `BasementGuardianPlatform`, `BasementGuardianPlatformAccessory`, `BasementGuardianAccessoryContext`.

Deleted: `src/platformAccessory.ts`, `src/@types/homebridge-lib.d.ts`, `test/plugin.test.mjs`, `.npmignore`, and the `homebridge-lib` and `ts-node` dependencies.

<verification>
- `npm run check` exits zero, covering `typecheck`, `lint` at `--max-warnings=0`, all three `fallow` sub-commands with `--fail-on-issues`, `format:check`, and both test suites.
- `npm pack --dry-run --json` lists only `dist/**`, `package.json`, `README.md`, `LICENSE`, `CHANGELOG.md`, and `config.schema.json`.
- A probe `.ts` file under `test/` parses and lints without a project-service error and without a floating-promise error.
- `npx tsc -p tsconfig.test.json --noEmit` succeeds.
- `git check-ignore test/hbConfig/config.json` succeeds.

Follow `CLAUDE.md`: run `pre-commit run --files <changed files>` before every `git commit`, fix and restage until clean, and never pass `--no-verify`. Commit messages follow Conventional Commits with a title of 5 to 72 characters and body lines of 80 characters or fewer, and mention no planning artifacts.
</verification>

<success_criteria>
- The runtime dependency set is exactly `mqtt`.
- Six new npm scripts exist and `npm test` runs both suites.
- `npm pack --dry-run` output is confined to the allowlist.
- No template accessory code remains under `src/`.
- `npm run check` is green on the resulting tree.
</success_criteria>

<output>
Create `.planning/phases/01-secure-cloud-foundation/01-01-SUMMARY.md` when done.
</output>
