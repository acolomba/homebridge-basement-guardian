---
phase: 01-secure-cloud-foundation
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - features/support/steps/shadow.ts
  - features/support/steps/runtime.ts
  - features/support/world.ts
  - src/runtime/accountRuntime.ts
  - test/runtime/accountRuntime.test.ts
  - eslint.config.js
autonomous: true
gap_closure: true
requirements: [SYNC-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`npm run check` produces the same answer on every run: five consecutive runs of the credential-rotation feature all exit 0."
    - "No step definition asserts a positive count or content on a collection the plugin fills asynchronously without first waiting on that same collection."
    - "Rotation timing is injected through `AccountRuntimeOptions` like `pollIntervalMs`, so a scenario drives rotation in milliseconds rather than waiting thirty real seconds (SYNC-04)."
    - "The credential-rotation feature completes well inside twenty seconds, which it structurally could not do while the thirty-second floor was a module constant."
    - "A floating promise in the Cucumber harness is a lint error, so a scenario cannot silently abandon a server, a client, or a shutdown."
  artifacts:
    - path: "features/support/steps/shadow.ts"
      provides: "Step assertions that wait on the collection they read"
      contains: "untilTrue"
    - path: "src/runtime/accountRuntime.ts"
      provides: "Rotation lead and floor as injected options"
      exports: ["AccountRuntimeOptions", "MIN_ROTATION_DELAY_MS", "ROTATION_LEAD_MS"]
    - path: "features/support/world.ts"
      provides: "Millisecond rotation timing for the harness"
      contains: "createAccountRuntimeFromConfig"
    - path: "eslint.config.js"
      provides: "Floating-promise exemption narrowed to the unit-test tree"
  key_links:
    - from: "features/support/world.ts"
      to: "src/runtime/accountRuntime.ts"
      via: "the seam carries the injected rotation timing from the harness into the runtime"
      pattern: "createAccountRuntimeFromConfig"
    - from: "features/support/steps/shadow.ts"
      to: "features/support/fakeShadowBroker.ts"
      via: "every positive assertion on a broker-filled collection waits on that collection"
      pattern: "untilTrue"
---

<objective>
Make the phase seal gate mean something. `npm run check` exited 1 on the verifier's first run and 0 on the next three, and the flake sits on the single scenario that proves SYNC-04 credential rotation. A gate that certifies four times out of five certifies nothing, and a non-deterministic gate teaches every later executor to re-run instead of to read.

Purpose: every other gap-closure plan in this phase commits behind `pre-commit`, which runs `npm run check` — and D-12 makes that one command cover both the unit suite and the Cucumber suite precisely so nothing can be skipped by accident. Until the suite is deterministic, no later plan can tell its own regression apart from this one. That is why this plan is wave 1 on its own.

Output: a credential-rotation scenario that passes repeatedly, rotation timing injected the way `pollIntervalMs` already is, and a lint configuration that no longer hides abandoned promises in the harness.
</objective>

<execution_context>
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-secure-cloud-foundation/01-VERIFICATION.md
@.planning/phases/01-secure-cloud-foundation/01-REVIEW.md
@.planning/phases/01-secure-cloud-foundation/01-CONTEXT.md
@.planning/phases/01-secure-cloud-foundation/01-PATTERNS.md
@.planning/phases/01-secure-cloud-foundation/01-10-SUMMARY.md
@.planning/phases/01-secure-cloud-foundation/01-11-SUMMARY.md
@CLAUDE.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@features/support/steps/shadow.ts
@features/support/steps/runtime.ts
@features/support/world.ts
@features/support/fakeShadowBroker.ts
@src/runtime/accountRuntime.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Every positive step assertion waits on the collection it reads</name>
  <read_first>
    - `features/support/steps/shadow.ts` in full — compare `assertClientIdentifiers` against `assertHandshakeCount` and `assertCompleteShadowRequests`, which are the correct shape
    - `features/support/steps/runtime.ts` in full — `assertRecoveryAnnouncedOnce` and `assertVendorRequestCount`
    - `features/support/fakeShadowBroker.ts` lines 160-200 — `handshakes` is pushed on the WebSocket upgrade, `clientIds` on the broker's own client event, and those are two different moments
    - `features/support/world.ts` — the `until` / `untilTrue` contract and its named-failure argument
  </read_first>
  <files>features/support/steps/shadow.ts, features/support/steps/runtime.ts</files>
  <behavior>
    - Running `features/credentialRotation.feature` five times in a row exits 0 five times.
    - A step that asserts a collection has reached N entries fails with its own named message when it does not, rather than with a bare deep-equality diff.
    - A step that asserts an exact count still asserts the exact count after the wait, so an over-count is still a failure.
  </behavior>
  <action>
    Close the flake the verifier reproduced (gap 7). `assertClientIdentifiers` reads `broker.clientIds` with a bare equality assertion and no wait. The step before it waits on `handshakes`, which the broker pushes on the WebSocket upgrade, while `clientIds` is pushed later when the broker processes the MQTT CONNECT packet. Those are different moments, so the assertion can run one packet early and see a single identifier.

    Route `assertClientIdentifiers` through `this.untilTrue` on the length of `broker.clientIds`, with a named failure describing the identifiers that never arrived, and keep the exact deep-equality assertion after the wait — exactly the two-step shape `assertHandshakeCount` and `assertCompleteShadowRequests` already use. Give the step the same raised step timeout its siblings carry.

    Then sweep both step modules for the same shape and convert every match. The rule: a step that asserts a positive count or positive content on a collection the plugin fills asynchronously must first wait on that same collection, not on a neighbouring one. Convert at least `assertCanonicalChangeCount` in the shadow module and `assertRecoveryAnnouncedOnce` in the runtime module; both currently read a plugin-filled array with no wait of their own. Leave the absence assertions alone — `assertNoCompleteShadowRequest`, `assertNoUnhandledRejection`, `assertNoRequestReachesTheCloud` and `assertNoErrorLogged` assert that nothing happened, and a wait cannot strengthen them.

    Do not change any `.feature` file in this task. Do not weaken an exact count into a lower bound; the wait establishes the floor and the existing assertion keeps the ceiling.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; for i in 1 2 3 4 5; do npx cucumber-js features/credentialRotation.feature || exit 1; done</automated>
  </verify>
  <done>Five consecutive runs of the credential-rotation feature exit 0, and no step in either module asserts a positive result on a plugin-filled collection without waiting on that collection first.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rotation lead and floor become injected options</name>
  <read_first>
    - `src/runtime/accountRuntime.ts` lines 26-55 (`ROTATION_LEAD_MS`, `MIN_ROTATION_DELAY_MS`) and lines 145-155 (`rotationDelayMs`)
    - `src/runtime/accountRuntime.ts` `AccountRuntimeOptions` and `createAccountRuntimeFromConfig`, to see how `pollIntervalMs` is already injected and where the seam supplies it
    - `features/support/world.ts` `launch()` and `issueShadowCredentials`
    - `features/support/steps/runtime.ts` `ROTATION_DEADLINE_MS` / `ROTATION_STEP_TIMEOUT_MS`
    - `test/runtime/accountRuntime.test.ts` — how the existing cases build `AccountRuntimeOptions`; a new required member touches every construction site
  </read_first>
  <files>src/runtime/accountRuntime.ts, test/runtime/accountRuntime.test.ts, features/support/world.ts, features/support/steps/runtime.ts</files>
  <behavior>
    - `rotationDelayMs` returns the injected floor when the vendor expiry does not parse.
    - `rotationDelayMs` returns the injected floor when the expiry is already inside the injected lead window.
    - `rotationDelayMs` returns expiry minus now minus the injected lead when that value is above the floor.
    - A runtime built with a floor of a few milliseconds performs a second credential request without any real-time wait beyond that floor.
  </behavior>
  <action>
    Close WR-12's second item. `MIN_ROTATION_DELAY_MS` and `ROTATION_LEAD_MS` are module constants the runtime factory does not accept by injection, unlike `pollIntervalMs`, so the rotation scenario waits thirty real seconds under a forty-five-second deadline. That is most of the suite's wall time and it contradicts the project's own rule that scheduling be driven by injected values rather than a real wait.

    Add `minRotationDelayMs` and `rotationLeadMs` to `AccountRuntimeOptions` beside `pollIntervalMs`, and have `rotationDelayMs` read them instead of the module constants. Keep both constants exported — they are the production values and `createAccountRuntimeFromConfig` is what supplies them, so the seam stays the single place production numbers are chosen and the mirrored test still has a symbol to import.

    Update every construction site in `test/runtime/accountRuntime.test.ts` for the two new required members, and add cases covering the four behaviours above.

    In `features/support/world.ts`, pass a small millisecond floor and lead through the seam so a scenario's rotation is driven by the injected value. Choose values well under one second and name them as module constants in the world with a comment saying why the harness compresses them.

    Then bring the rotation step's deadline in `features/support/steps/runtime.ts` back in line with its siblings: the separate rotation deadline and rotation step timeout exist only because the wait was thirty real seconds, so drop them and use the module's ordinary deadline and step timeout.

    Do not add a production reset hook, a test-only branch, or an environment read. The seam already exists; this is one more ordinary injected dependency, and D-11 puts the Cucumber fakes at the transport level precisely so the harness drives production wiring rather than a test escape hatch.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test "dist-test/test/runtime/accountRuntime.test.js" &amp;&amp; timeout 20 npx cucumber-js features/credentialRotation.feature</automated>
  </verify>
  <done>The rotation floor and lead arrive through `AccountRuntimeOptions`, the unit pair covers the four behaviours, and the credential-rotation feature finishes inside twenty seconds — which it could not do while the floor was a module constant.</done>
</task>

<task type="auto">
  <name>Task 3: A floating promise in the harness is a lint error again</name>
  <read_first>
    - `eslint.config.js` lines 88-96 — the override block and the rationale comment on it
    - `.planning/phases/01-secure-cloud-foundation/01-REVIEW.md` WR-14 item 4
    - `features/support/world.ts` and every module under `features/support/steps/` — these are the files the change will report on
  </read_first>
  <files>eslint.config.js, features/support/world.ts, features/support/steps/shadow.ts, features/support/steps/runtime.ts, features/support/steps/configuration.ts, features/support/steps/authentication.ts, features/support/steps/harness.ts</files>
  <action>
    Close WR-14's fourth item. The floating-promise rule is disabled for the whole Cucumber tree, and the stated reason — the unit runner's `test()` returning a promise at every call site — does not apply to Cucumber files at all. The harness starts servers, brokers, and clients and then shuts them down, so this exemption hides exactly the class of defect the lifecycle scenarios exist to catch, and plan 01-17 adds a scenario that asserts a shutdown left no live connection.

    Narrow the override so it covers the unit-test tree only, and correct its rationale comment to describe the runner rather than the harness.

    Then run the linter and resolve every floating promise the change reports under the Cucumber tree. Resolve each one on its merits: `await` it when the step genuinely needs the work to finish, and mark it explicitly discarded only where the code deliberately does not wait and a comment says why. Do not silence a report by re-widening the override, by adding a file-level disable, or by adding an inline disable comment.

    If the report turns out to be larger than roughly a dozen sites, stop and record the count in the summary rather than pushing through — that would mean the harness has a real ownership problem that wants its own plan, not a lint pass.
  </action>
  <verify>
    <automated>npx eslint . --max-warnings=0 &amp;&amp; npm run test:cucumber</automated>
  </verify>
  <done>The floating-promise exemption names only the unit-test tree, the linter is clean at zero warnings, and the Cucumber suite still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → phase seal | A non-deterministic assertion decides whether the phase is certified. |
| vendor credential response → rotation schedule | An untrusted expiry string drives a timer the connection depends on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-64 | Repudiation | `npm run check` as the phase seal | high | mitigate | Every positive step assertion waits on the collection it reads, so a pass means the condition held rather than that the poll happened to land late. The rotation feature is run five times in a row as the task's own gate. |
| T-01-65 | Denial of service | injected rotation floor | medium | mitigate | The floor stays a required option with the production value supplied by the seam, so a compressed harness value cannot leak into production; a missing member is a compile error rather than a silent zero. |
| T-01-66 | Tampering | unparseable vendor expiry | medium | mitigate | `rotationDelayMs` already refuses a non-finite parse and falls back to the floor; the unit cases now assert that against the injected floor rather than a module constant. |
| T-01-67 | Denial of service | abandoned harness promise | medium | mitigate | The floating-promise rule covers the Cucumber tree again, so a scenario that abandons a server, a client, or a shutdown fails the lint gate instead of leaking a handle into the next scenario. |
| T-01-68 | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package. No dependency is added, removed, or upgraded; if a task appears to need one, stop and re-plan rather than installing. |
</threat_model>

<verification>
- `npx cucumber-js features/credentialRotation.feature` exits 0 on five consecutive runs.
- `timeout 20 npx cucumber-js features/credentialRotation.feature` exits 0, so the scenario no longer waits thirty real seconds.
- `npm run test:coverage:direct -- "dist-test/src/runtime/accountRuntime.js"` reports 100 percent lines, branches, and functions for the runtime pair run alone.
- `npx eslint . --max-warnings=0` is clean with the exemption narrowed.
- `npm run check` exits 0, and is run twice to confirm the same answer both times.

Follow `CLAUDE.md`: run `pre-commit run --files <changed files>` before every `git commit`, fix and restage until clean, and never pass `--no-verify`.
</verification>

<success_criteria>
- The credential-rotation scenario passes repeatedly rather than four times in five.
- No step definition asserts a positive result on a plugin-filled collection without waiting on that collection.
- Rotation timing is an injected option, and the scenario finishes in seconds.
- The Cucumber harness is covered by the floating-promise rule.
</success_criteria>

<output>
Create `.planning/phases/01-secure-cloud-foundation/01-12-SUMMARY.md` when done.
</output>
