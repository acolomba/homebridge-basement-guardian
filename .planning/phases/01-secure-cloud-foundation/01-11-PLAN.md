---
phase: 01-secure-cloud-foundation
plan: 11
type: execute
wave: 7
depends_on: ["01-08", "01-10"]
files_modified:
  - features/support/world.ts
  - features/support/steps/configuration.ts
  - features/support/steps/authentication.ts
  - features/support/steps/shadow.ts
  - features/support/steps/runtime.ts
  - features/configuration.feature
  - features/authentication.feature
  - features/shadowMerge.feature
  - features/shadowLifecycle.feature
  - features/credentialRotation.feature
  - features/degradedOperation.feature
  - features/lifecycle.feature
  - CHANGELOG.md
autonomous: true
requirements: [CONF-03, AUTH-01, AUTH-02, SYNC-02, SYNC-03, SYNC-04, SYNC-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The whole runtime is exercised against a local fake vendor cloud at the transport level, with no client library named in any scenario, so the scenarios survive a change of transport implementation (D-10, D-11)."
    - "A real partial heartbeat delivered over the fake broker preserves previously reported pump and power fields in the canonical snapshot (SYNC-02)."
    - "A requested value delivered by the fake device never becomes reported state (SYNC-02)."
    - "A complete-shadow request is published on the first connection and again after every reconnect, and a REST poll reconciles state the shadow missed while disconnected (SYNC-03)."
    - "Rotating credentials does not disconnect the live socket, and the next handshake after a forced reconnect carries the rotated credential material and the new vendor client identifier (SYNC-04)."
    - "Shutdown during an in-flight retry wait, an in-flight request, and an open shadow connection produces no unhandled rejection (SYNC-05)."
    - "A credential rejection stops authentication, deletes the cached token, and produces no further token request against the fake tenant (D-13)."
    - "A shadow connection failure leaves the runtime up on the polling-only path and logs the degraded path once (D-15)."
    - "With credentials absent the plugin starts nothing: no token request, no REST request, and no connection reaches the fake cloud (CONF-03)."
    - "`npm run check` passes both suites and the packed artifact still lists only allowlisted files now that the feature directory exists (D-12, D-21, D-23)."
    - "At the phase seal the `.fallowrc.json` `ignoreFindings` list holds exactly the eight `D-17` type-only scaffold entries: every transitional entry the transport plans parked has been removed, so the dead-code gate passes on genuine reachability rather than on a suppression (D-17)."
  prohibitions:
    - statement: "MUST NOT place a real account email, a real password, a real token, or a real vendor device identifier into a feature file, a step definition, or a fixture; scenarios use stable placeholders (D-027)."
      status: resolved
      verification: judgment
    - statement: "MUST NOT let a scenario reach a live vendor service or the public network; every dependency is a loopback stand-in owned by the scenario (D-10)."
      status: resolved
      verification: judgment
  artifacts:
    - path: "features/support/world.ts"
      provides: "Per-scenario world wiring the runtime through the composition seam to the fake cloud"
      exports: ["BasementGuardianWorld"]
    - path: "features/shadowMerge.feature"
      provides: "Partial-heartbeat merge and requested-state exclusion scenarios"
    - path: "features/credentialRotation.feature"
      provides: "Rotation-without-disconnect and reconnect-with-new-identifier scenarios"
    - path: "features/lifecycle.feature"
      provides: "Shutdown-during-in-flight-work scenarios"
    - path: "CHANGELOG.md"
      provides: "The phase's user-visible changes under the Unreleased section"
      contains: "## [Unreleased]"
  key_links:
    - from: "features/support/world.ts"
      to: "src/runtime/accountRuntime.ts"
      via: "the world builds the runtime through the composition seam with harness constants"
      pattern: "createAccountRuntimeFromConfig"
    - from: "features/support/world.ts"
      to: "features/support/fakeShadowBroker.ts"
      via: "the harness constants point the runtime's shadow scheme and endpoint at the local broker"
      pattern: "createFakeShadowBroker"
---

<objective>
Prove this phase's four success criteria against a local fake vendor cloud, then record what changed for the reader and re-check the packed artifact now that the feature directory exists.

Purpose: `D-10` puts the fake-pump harness in this phase because the phase's third and fourth success criteria are close to untestable without it — partial-shadow merge, requested-state exclusion, credential rotation, reconnect, and shutdown are all transport-level behaviors. `D-11` fixes the seam at the transport, so no scenario names a client library. `D-12` makes both suites part of one test command so nothing is skipped by accident. `D-23` records the phase's user-visible changes so later phases append rather than bootstrap.

Output: seven behavior feature files driving the real runtime against loopback fakes, plus a changelog entry and a re-verified packed artifact.
</objective>

<execution_context>
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/homebridge-basement-guardian/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-secure-cloud-foundation/01-CONTEXT.md
@.planning/phases/01-secure-cloud-foundation/01-VALIDATION.md
@.planning/phases/01-secure-cloud-foundation/01-08-SUMMARY.md
@.planning/phases/01-secure-cloud-foundation/01-10-SUMMARY.md
@.planning/intel/context.md
@features/CLAUDE.md
@features/support/world.ts
@src/runtime/accountRuntime.ts
@.claude/rules/changelog.md
@.claude/rules/typescript-style-guide.md
</context>

<interfaces>
<!-- What the world composes. Both were published by earlier plans in this phase. -->

```typescript
// src/runtime/accountRuntime.ts
export interface AccountRuntimeDeps {
  config: BgConfig;
  constants: ProtocolConstants;   // the harness supplies its own, pointing at the loopback fakes
  storagePath: string;
  clock: Clock;
  log: RedactingLogger;
  connect: MqttConnect;
  createSalt: () => string;
}
export function createAccountRuntimeFromConfig(deps: AccountRuntimeDeps): AccountRuntime;

// features/support/fakeShadowBroker.ts
export interface FakeShadowBroker {
  readonly host: string;
  readonly handshakes: readonly string[];
  readonly clientIds: readonly string[];
  publishReported(deviceId: string, reported: Record<string, unknown>, version: number): void;
  publishGetAccepted(deviceId: string, shadow: Record<string, unknown>, version: number): void;
  publishGetRejected(deviceId: string, code: number): void;
  disconnectAll(): void;
  close(): Promise<void>;
}
```

Binding phrasing rules for every feature file and step module (`features/CLAUDE.md`):

- Concise, active verbs; present tense; no use of the word "should".
- Sentence casing for scenario names; all lower case for step text.
- No conjunction step keyword; repeat the keyword instead.
- "these" when a data table follows; the definite article for something specific.
- Step definitions organized by function, not by feature file, ordered given, then when, then then.
- Given names the thing given; when names the action; then names `assert` followed by the condition.
- In test code let exceptions bubble; do not catch and log.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Point the real runtime at the fake cloud and write the step vocabulary</name>
  <read_first>
    - `features/support/world.ts`, `features/support/steps/harness.ts`, and the four fake modules as the harness plan left them
    - `src/runtime/accountRuntime.ts` — the composition seam and its dependency object
    - `src/protocol.ts` and `src/config.ts` — the constant and configuration shapes the harness must supply
    - `features/CLAUDE.md` in full
    - `.planning/PROJECT.md` section "Test Strategy" subsection "Cucumber Support Code"
  </read_first>
  <files>features/support/world.ts, features/support/steps/configuration.ts, features/support/steps/authentication.ts, features/support/steps/shadow.ts, features/support/steps/runtime.ts</files>
  <action>
Extend the world so a scenario can build the real runtime against the fake cloud. Assemble the harness constants from the running fakes: the REST base URL from the fake REST service, the Auth0 origin from the fake tenant, the region and realm as placeholders, and the WebSocket protocol as the unsecured scheme so the signer produces a URL the local broker accepts. The endpoint and the vendor client identifier need no override at all, because they arrive as data in the fake credentials response.

Build the runtime through the composition seam with those constants, a per-scenario temporary storage path, the world's controllable clock, a redacting logger whose delegate records every line into the world's observation log, the client library's connect function, and a fixed salt source. Nothing here is a production escape hatch: the constants are an ordinary injected dependency of the seam.

Expose on the world what the steps assert against: the observation log, the store, the runtime's monitoring path, and each fake's recorded requests, handshakes, and client identifiers.

Write four step modules organized by function rather than by feature file:

- `configuration.ts` — given a configuration, given a configuration missing a field, when the plugin starts, then assertions about what did and did not reach the fake cloud.
- `authentication.ts` — given the tenant answers with a token, given the tenant rejects the credentials, given the tenant throttles, then assertions about the recorded token requests, the cached file, and the logged lines.
- `shadow.ts` — given these devices, given the device reports these fields, when the device publishes a partial heartbeat, when the connection drops, then assertions about the canonical snapshot, the subscribed topics, the published complete-shadow requests, and the recorded handshakes.
- `runtime.ts` — when Homebridge shuts down, when the credentials rotate, then assertions about timers, the monitoring path, unhandled rejections, and the observation log.

Follow the required naming exactly: a given step's method is named for the thing given, a when step's for the action, and a then step's is `assert` followed by the condition. Order the definitions given, then when, then then, within each module. Let exceptions bubble.

Register a process-level listener in the world's setup that records any unhandled rejection into the observation log and removes itself in cleanup, so the shutdown scenarios can assert on the absence of one rather than hoping the process would have crashed.

Use short, self-describing placeholders for every value: a placeholder account email, a placeholder password, and a placeholder device identifier shaped like the vendor's but carrying no real account identifier and no real serial number.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; test -f dist-test/features/support/steps/runtime.js</automated>
    <automated>npx eslint features --max-warnings=0</automated>
  </verify>
  <acceptance_criteria>
    - `features/support/world.ts` builds the runtime through `createAccountRuntimeFromConfig` with constants derived from the running fakes.
    - The world exposes the observation log, the store, the monitoring path, and each fake's recorded traffic.
    - Four step modules exist, each grouped by function, each ordering its definitions given, then when, then then.
    - Every then-step method name begins with `assert`.
    - The world records unhandled rejections and removes its listener during cleanup.
    - No step definition or world member names a client library type.
    - No placeholder in any file is a real account email, password, token, or vendor device identifier.
    - `npx eslint features --max-warnings=0` is clean.
  </acceptance_criteria>
  <done>A scenario can start the real runtime against the fake cloud and assert on everything the phase's criteria need.</done>
</task>

<task type="auto">
  <name>Task 2: Write the seven behavior feature files</name>
  <read_first>
    - `.planning/phases/01-secure-cloud-foundation/01-VALIDATION.md` — the per-requirement verification map naming each scenario this phase owes
    - `features/CLAUDE.md` in full
    - `features/harness.feature` — the phrasing already established
    - `.planning/intel/context.md` section 1 (the seven fields a partial heartbeat carries) and section 2 (credential rotation)
    - `.planning/phases/01-secure-cloud-foundation/01-CONTEXT.md` decisions `D-10`, `D-13`, `D-15`, and `D-22`
  </read_first>
  <files>features/configuration.feature, features/authentication.feature, features/shadowMerge.feature, features/shadowLifecycle.feature, features/credentialRotation.feature, features/degradedOperation.feature, features/lifecycle.feature</files>
  <action>
Write one feature file per concern, each holding the scenarios the phase's validation map names.

`configuration.feature` — with credentials absent the plugin starts nothing: no token request reaches the fake tenant, no request reaches the fake REST service, no connection reaches the fake broker, and one error line names what to fix. A second scenario covers an out-of-range poll interval producing the same refusal, because a hand-edited file bypasses the settings form entirely.

`authentication.feature` — a cache miss performs the grant and writes the cache; a restart with a valid cache performs no token request; a credential rejection stops authentication, deletes the cache, logs one actionable error, and produces exactly one recorded token request across the whole scenario; a throttling response logs the warning naming the response code, the thirty-day window, and how to stop the plugin, and does not halt.

`shadowMerge.feature` — a device reporting a full set of fields, then a partial heartbeat carrying only the seven heartbeat fields, leaves the pump, power, charging, test, and fault values from the earlier report intact in the canonical snapshot. A second scenario has the fake device deliver a document carrying a requested value and asserts the canonical snapshot is unchanged. A third repeats an identical heartbeat and asserts no change is reported.

`shadowLifecycle.feature` — a complete-shadow request is published on the first connection; the connection drops and reconnects and a second complete-shadow request is published; state that changed while the connection was down is reconciled by the next REST poll rather than replayed by the subscription.

`credentialRotation.feature` — rotating the credentials in the cache does not disconnect the live socket and shadow messages keep arriving; after a forced reconnect the newly recorded handshake carries the rotated credential material and the new vendor client identifier. A third scenario has the credential request fail once and asserts a later rotation still happens.

`degradedOperation.feature` — REST discovery succeeds while the broker refuses connections: the runtime stays up, the monitoring path reads as polling-only, the degraded path is logged once across repeated attempts, and REST polling keeps the snapshot current. A second scenario brings the broker back and asserts the combined path returns and recovery is logged once.

`lifecycle.feature` — shutdown while a retry wait is pending, shutdown while a request is in flight, and shutdown with an open shadow connection each complete with no unhandled rejection recorded; a second shutdown after the first also completes; starting, shutting down, and starting again performs no work on the second start.

Follow the binding phrasing rules throughout: sentence casing for scenario names, all lower case for step text, active verbs, present tense, no use of the word "should", no conjunction step keyword — repeat the keyword instead — the definite article for something specific, and "these" introducing any data table.

Give any scenario that waits on a message an explicit short deadline, so a transport regression fails as a timeout on a clearly named step rather than as a silent hang.
  </action>
  <verify>
    <automated>npm run test:cucumber</automated>
    <automated>node -e "const fs=require('fs');const files=fs.readdirSync('features').filter(f=&gt;f.endsWith('.feature'));const missing=['configuration','authentication','shadowMerge','shadowLifecycle','credentialRotation','degradedOperation','lifecycle'].filter(n=&gt;!files.includes(n+'.feature'));if(missing.length)throw new Error('missing feature files: '+missing.join(', '));for(const f of files){const t=fs.readFileSync('features/'+f,'utf8');if(/^\s*And\s/m.test(t))throw new Error(f+' uses a conjunction step keyword');if(/should/i.test(t))throw new Error(f+' uses a discouraged modal verb');}"</automated>
  </verify>
  <acceptance_criteria>
    - All seven feature files exist and every scenario passes under `npm run test:cucumber`.
    - No feature file uses a conjunction step keyword and none contains the discouraged modal verb, proven by the structural check above.
    - The merge scenario asserts the pump, power, charging, test, and fault values survive a partial heartbeat.
    - The requested-state scenario asserts the canonical snapshot is unchanged after a document carrying a requested value.
    - The rotation scenario asserts the socket is not disconnected and that the following handshake carries new credential material and a new client identifier.
    - The degraded scenario asserts the monitoring path value and exactly one degraded log line across repeated attempts.
    - Every lifecycle scenario asserts zero recorded unhandled rejections.
    - The credential-rejection scenario asserts exactly one recorded token request for the whole scenario.
    - The Cucumber process exits on its own.
  </acceptance_criteria>
  <done>Seven feature files drive the real runtime against the fake cloud and cover every behavior the phase's validation map names.</done>
</task>

<task type="auto">
  <name>Task 3: Record the phase's changes and re-check the packed artifact</name>
  <read_first>
    - `CHANGELOG.md` as the toolchain plan left it
    - `.claude/rules/changelog.md` in full
    - `package.json` — the publish allowlist
    - `.agents/skills/simple-english/SKILL.md` and `.agents/skills/humanizer/SKILL.md`
    - Every `01-*-SUMMARY.md` in this phase directory, for what actually changed
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
Add this phase's user-visible changes to the `Unreleased` section of `CHANGELOG.md`, appending to the entries the toolchain plan already wrote rather than replacing them. Apply the `simple-english` skill and the `humanizer` skill to every entry before committing.

Keep each entry to one user-visible change, one sentence, active voice, aiming for twenty-five words and never exceeding the forty-word ceiling the verify enforces, leading with what changed for the reader rather than with the mechanism. Put rationale, internals, and rejected alternatives in the commit body, never here. Do not list what remains unimplemented and do not spread one change across several bullets. Do not mention any planning artifact.

Cover, under `Added`: signing in with a Basement Guardian account through the Homebridge settings form; keeping device state current from the vendor cloud. Under `Changed`: the settings form now refuses to save an incomplete account, and the plugin refuses to start on an out-of-range setting rather than substituting a value. Under `Security`: the cached sign-in token is stored owner-only inside the Homebridge storage directory, and passwords, tokens, and temporary cloud credentials no longer appear in the log. Keep the total short — this is a release note, not a phase report.

Then re-run the packaging check. The feature directory now exists and is exactly the kind of directory the previous denylist would have shipped; the allowlist must still hold. Confirm the packed file list contains only compiled output, the settings schema, the changelog, and the files the registry force-includes, and that the bundled protocol data file is present inside the compiled output.

Finally run the full phase gate and fix anything it reports: the type check, the lint at zero warnings, all three code-health sub-commands with their failure flags, the format check, and both test suites.

This is the phase gate. Plans 07, 08, and 09 each ran the check leg by leg instead of as one repository-wide command, and each states its own accounting for why. What they left for here is the Cucumber leg, which plan 08 owned in its own wave, and the confirmation that nothing is still parked. Every module without a production consumer was parked in the `ignoreFindings` list under a named plan that removes the entry, and plan 10 wired the shadow client into the account runtime and removed all five. Confirm that holds: the list must be exactly the eight type-only scaffold entries the source-tree plan established, and the dead-code check must pass with the transport modules reached from `src/index.ts` rather than only from their tests. A leftover transitional entry means a module is still unwired and the gate is blind to it. Treat that as a failure of this plan, and fix the wiring rather than the list.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('CHANGELOG.md','utf8');if(!t.includes('## [Unreleased]'))throw new Error('missing Unreleased section');const body=t.split('## [Unreleased]')[1].split(/\n## \[/)[0];const bullets=body.split('\n').filter(l=>l.trim().startsWith('- '));if(bullets.length===0)throw new Error('Unreleased section has no entries');const tooLong=bullets.filter(b=>b.trim().slice(2).split(/\s+/).length>40);if(tooLong.length)throw new Error('entries over 40 words: '+tooLong.join(' | '));"</automated>
    <automated>npm run build &amp;&amp; npm pack --dry-run --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const allowed=new Set(['package.json','README.md','LICENSE','CHANGELOG.md','config.schema.json']);const files=JSON.parse(s)[0].files.map(f=>f.path);const bad=files.filter(f=>!f.startsWith('dist/')&amp;&amp;!allowed.has(f));if(bad.length)throw new Error('unexpected packed files: '+bad.join(', '));if(!files.includes('dist/protocol.json'))throw new Error('the bundled protocol data file is not in the packed output');process.stdout.write('packed '+files.length+' files\n')})"</automated>
    <automated>node -e "const c=require('./.fallowrc.json'); const scaffolds=['src/device/events.ts','src/device/health.ts','src/device/family.ts','src/device/gemini.ts','src/device/halo.ts','src/accessories/basementGuardian.ts','src/accessories/services.ts','src/persistence/accessoryContext.ts']; const got=c.ignoreFindings||[]; if(JSON.stringify(got)!==JSON.stringify(scaffolds)) throw new Error('at the phase seal ignoreFindings must be exactly the eight D-17 scaffolds; got '+JSON.stringify(got));"</automated>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` has a non-empty `Unreleased` section whose every bullet is 40 words or fewer.
    - The `Unreleased` section carries entries under `Added`, `Changed`, and `Security`, and no entry names a planning artifact.
    - `npm pack --dry-run --json` lists only paths under `dist/` plus the five force-included or allowlisted root files, and the feature directory is absent.
    - `dist/protocol.json` is present in the packed output.
    - `.fallowrc.json` `ignoreFindings` deep-equals exactly the eight `D-17` scaffold paths, so no transport module is still hidden from the dead-code check.
    - `npm run check` exits zero across the type check, the lint at zero warnings, all three code-health sub-commands, the format check, and both test suites. This is the phase gate the scoped plans deferred to. Reachability closed one plan earlier, when plan 10 wired the composition seam; this run confirms it holds with the Cucumber suite and the packaging check included.
  </acceptance_criteria>
  <done>The phase's changes are recorded in one short release note, and the packed artifact is confirmed clean now that the feature directory exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scenario fixtures → public repository | Feature files and step definitions are committed and published. |
| test suite → network | A scenario able to reach the public network can reach the real vendor. |
| repository → npm registry | The feature directory is new and is exactly the kind of directory the previous denylist would have shipped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-59 | Information disclosure | committed scenario fixtures | high | mitigate | Every value is a short placeholder. The device identifier is placeholder-shaped and carries no real account identifier and no real serial number; no email, password, or token in any file is real. The pre-commit secret scan runs on every commit. |
| T-01-60 | Information disclosure | packed artifact after the feature directory exists | high | mitigate | The publish allowlist is re-verified in this plan by asserting the complete packed file list, and the assertion is positive: every path must be under the compiled output or one of five named root files. |
| T-01-61 | Information disclosure | live vendor access from the deterministic suite | high | mitigate | Every dependency is a loopback stand-in the scenario owns and tears down. The opt-in real-hardware profile is a separate Cucumber profile selected by tag and is not part of the default run. |
| T-01-62 | Repudiation | a silent transport regression | medium | mitigate | Every scenario that waits on a message carries an explicit short deadline, so the silent-hang failure mode surfaces as a timeout on a named step rather than as an indefinite stall. |
| T-01-63 | Denial of service | leaked sockets across scenarios | medium | mitigate | The world's after hook runs cleanup in reverse order and the acceptance criteria require the Cucumber process to exit on its own. |
</threat_model>

## Artifacts this phase produces

New files: `features/support/steps/configuration.ts`, `features/support/steps/authentication.ts`, `features/support/steps/shadow.ts`, `features/support/steps/runtime.ts`, `features/configuration.feature`, `features/authentication.feature`, `features/shadowMerge.feature`, `features/shadowLifecycle.feature`, `features/credentialRotation.feature`, `features/degradedOperation.feature`, `features/lifecycle.feature`.

Extended: `features/support/world.ts` gains the runtime composition, the observation log, and the unhandled-rejection recorder. `CHANGELOG.md` gains the phase's `Added`, `Changed`, and `Security` entries.

<verification>
- `npm run test:cucumber` passes every scenario and the process exits on its own.
- The structural check finds no conjunction step keyword and no discouraged modal verb in any feature file.
- `npm pack --dry-run --json` lists only allowlisted files and includes the bundled protocol data file.
- `.fallowrc.json` `ignoreFindings` holds exactly the eight `D-17` scaffold entries.
- `npm run check` exits zero. This plan owns the phase gate: plans 07, 08, and 09 each ran a scoped gate because neither a parallel sibling's work nor a later plan's wiring was theirs to hold green, and this is where the repository-wide run is asserted over the finished phase.

Follow `CLAUDE.md`: run `pre-commit run --files <changed files>` before every `git commit`, fix and restage until clean, and never pass `--no-verify`. Apply the `simple-english` and `humanizer` skills to the changelog entries before committing them.
</verification>

<success_criteria>
- The real runtime is driven end to end against a loopback fake vendor cloud.
- Every behavior the phase's validation map names has a passing scenario.
- No scenario names a client library, reaches the network, or carries a real credential.
- The phase's changes are recorded and the packed artifact is confirmed clean.
- The repository-wide gate is green with every source module reachable and no transitional dead-code suppression left behind.
</success_criteria>

<output>
Create `.planning/phases/01-secure-cloud-foundation/01-11-SUMMARY.md` when done.
</output>
