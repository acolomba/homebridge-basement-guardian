---
milestone: "1.0 (v1 requirements scope; package.json still reads 0.1.0 pending release gates — no explicit milestone-version field was ever recorded in .planning/, see Notes)"
audited: 2026-09-05T14:40:00Z
status: tech_debt
scores:
  requirements: 47/47
  phases: 7/7
  integration: 9/9
  flows: 4/4
gaps: []
tech_debt:
  - phase: 02-safe-gemini-discovery-and-identity
    items:
      - "RESOLVED 2026-09-05: 2 backstop-tagged truths (Reconciliation.observe() per-device-flag isolation; registerDiscoveredDevices order-independence) had no STATE.md Deferred Verification row and no addressed_in routing to a later phase. Added to STATE.md's Deferred Verification table. Neither is known to block 1.0.0."
  - phase: 03-safety-monitoring-in-homekit
    items:
      - "1 of 3 human_verification checks still open: flood automation survives a degraded water scope in a real Apple Home. Tracked in STATE.md, rides with the G-003/G-004 session. Blocks 1.0.0 only."
  - phase: 04-pump-records-and-official-controls
    items:
      - "6 human_verification items; item 1 (cached-before-release PumpService adopts the 4 new record characteristics on upgrade) is load-bearing and unclosed — no test has met a real Homebridge accessory cache. Items 2-4 join Phase 3's flood check + G-003/G-004. Item 5 is a desk task. Item 6 is G-001 (1.0.0-only)."
  - phase: 05-degraded-operation-and-recovery
    items:
      - "5 deferred items: 3 require the G-003/G-004 real-paired-home session (1.0.0-only). 2 are WINDOWS ledger entries (entry 6: Pump Controller Link Lost message naming; entry 14: IN-03 wall-clock jump) with no stated release gate — should be triaged, not release-blocking by default."
  - phase: 06-validated-release-candidate
    items:
      - "5 deferred items, all pure execution/infrastructure gaps this phase's code cannot close: GitHub Actions CI matrix and real packed-tarball scan never run (branch never pushed), GitHub private-vulnerability-reporting unavailable at this account tier, G-001..G-004 hardware gates pending, live real-pump suite run pending. Tracked in 06-UAT.md and STATE.md."
  - phase: requirements-traceability
    items:
      - "REQUIREMENTS.md attributes RES-01's 'time-based half' delivery to Phase 5, but no 05-*-SUMMARY.md claims RES-01 completion (only RES-03/RES-04 do) — it was actually delivered by Phase 5.1 (05.1-01 through 05.1-05, 05.1-07). Cosmetic: the requirement is genuinely satisfied, just mis-attributed in the traceability table. Recommend correcting Phase 5 -> Phase 5.1 in REQUIREMENTS.md."
  - phase: nyquist-coverage
    items:
      - "4 of 7 phases (02, 04, 05, 06) have a VALIDATION.md still at status: draft / nyquist_compliant: false — /gsd-validate-phase was never run to reconcile them. Per ADR/#2117 this is a coverage TODO, not a compliance failure (04 and 05's VALIDATION.md content is substantively filled, likely just a missed status flip; 02 and 06's are closer to genuinely unfilled templates). Only 01, 03, and 05.1 show status: validated / nyquist_compliant: true."
---

# Milestone 1.0 Audit — Basement Guardian

**Audited:** 2026-09-05T14:40:00Z
**Status:** tech_debt — all requirements satisfied, no integration gaps, no blocking defect; accumulated deferred/tracking debt needs maintainer review before `/gsd-complete-milestone`.

## Notes on Milestone Version

`gsd_run query init.milestone-op` returns `milestone_version: null` — this project's `.planning/` setup never recorded an explicit milestone-version field (no prior `/gsd-complete-milestone` has run; `archived_milestones: []`, `archive_count: 0`). `package.json` still reads `"version": "0.1.0"`. REQUIREMENTS.md groups all 47 requirements audited here under a `## v1 Requirements` heading, and ROADMAP.md frames the whole phase sequence (1 → 6) as ending in "an npm-ready release candidate" gated by `G-001`–`G-004` before `1.0.0`. This audit therefore treats the milestone as **v1** (the full requirements set gating a `1.0.0` release), while noting the actual version bump has not happened and several of the gates that would justify it are still open (see Tech Debt below). The maintainer should confirm the intended version label before running `/gsd-complete-milestone`.

## Requirements Coverage

**47/47 v1 requirements satisfied** across a 3-source cross-reference (REQUIREMENTS.md traceability table, each phase's `*-SUMMARY.md` `requirements-completed` frontmatter, and each phase's `*-VERIFICATION.md` body). No orphaned requirement — every ID assigned to a phase is claimed by that phase's summaries and discussed in that phase's verification. No unsatisfied requirement — the `gaps_found` FAIL gate does not trigger.

One cosmetic attribution issue, not a coverage gap: **RES-01** is delivered by Phase 5.1, not Phase 5 as REQUIREMENTS.md's traceability table states (see `nyquist-coverage`/tech-debt entry `requirements-traceability` above for detail). The requirement itself is genuinely satisfied.

| Category | Requirements | Status |
|----------|--------------|--------|
| Platform and Configuration | CONF-01..06 | ✓ Satisfied |
| Authentication and Secret Handling | AUTH-01..02 | ✓ Satisfied |
| Cloud Synchronization | SYNC-01..05 | ✓ Satisfied |
| Device Families and Accessory Lifecycle | DEV-01..08 | ✓ Satisfied |
| Safety Monitoring and HomeKit Representation | SAFE-01..08 | ✓ Satisfied |
| Pump Records and Official Controls | CTRL-01..05 | ✓ Satisfied |
| Degraded Operation and Recovery | RES-01..04 | ✓ Satisfied (RES-01 mis-attributed to Phase 5 vs. actual Phase 5.1 — cosmetic) |
| Release Quality, Privacy, and Distribution | REL-01..09 | ✓ Satisfied |

## Phase Verification Summary

| Phase | VERIFICATION.md Status | Score | Open Items |
|-------|------------------------|-------|------------|
| 1. Secure Cloud Foundation | passed (4th re-verification pass) | 22/22 | None open — 3 deferred items all routed to and closed by later phases |
| 2. Safe Gemini Discovery and Identity | passed | 25/27 | **2 backstop items untracked at milestone level (see Tech Debt)** |
| 3. Safety Monitoring in HomeKit | human_needed | 6/6 | 1 of 3 human-verification checks open (flood automation, 1.0.0-only) |
| 4. Pump Records and Official Controls | human_needed | 17/17 | 6 human items, 1 load-bearing (cache-upgrade adoption) |
| 5. Degraded Operation and Recovery | passed (re-verified) | 4/4 | 5 deferred items (3 are G-003/G-004, 2 are untriaged WINDOWS ledger entries) |
| 5.1. Per-Pump Trust and Monotonic Silence | passed | 8/8 | None |
| 6. Validated Release Candidate | passed (deferred by maintainer 2026-09-05) | 6/6 | 5 deferred items, all infra/hardware execution gaps |

## Cross-Phase Integration

**Integration checker verdict: all checked seams wired, no orphaned or missing links.**

- **End-to-end pipeline** (auth → discovery → HomeKit representation → controls → degraded-state handling → release headers): confirmed wired through actual call sites, not just declared. Auth (`src/cloud/auth.ts`) feeds REST/shadow (`src/cloud/api.ts`, `src/cloud/shadow.ts`) into `accountRuntime.ts`'s canonical snapshot, pushed to `platform.ts` via injected callbacks; controls route through `commandPort.ts` with the `CTRL-05` 2.5s deadline; Homebridge's `shutdown` event actually invokes `runtime.stop()` (`SYNC-05`).
- **Phase 5.1's per-device trust amendment** (RES-01/RES-02) is fully applied end to end: `monitoringHealth.ts`'s per-device `silentDevices()` map → `accountRuntime.ts`'s `monitoringTrustByDevice` → `platform.ts`'s `applyMonitoringHealth` (joins by `deviceId`, defaults unmapped accessories to the safe `shadowSilent: true`) → `basementGuardian.ts`'s `markMonitoring`. No leftover single-boolean account-wide shadow-silence path found; the genuinely account-wide fact (`credentialsRejected`) is deliberately left account-wide and documented as such.
- **Phase 6's `PLUGIN_USER_AGENT` header** does not interfere with Phase 1's auth contracts: additive on REST/Auth0 calls, and on the MQTT/IoT path it rides `wsOptions.headers`, outside the SigV4-signed query string.
- **No TODO/FIXME/stub/placeholder** found crossing a phase boundary in `src/`. The one deliberately open cross-cutting item (Phase 3's flood-automation check) is a tracked, documented release gate, not a silent break.

## Nyquist Validation Coverage

Nyquist capability is active (`workflow.nyquist_validation`).

| Phase | VALIDATION.md | Classification |
|-------|---------------|-----------------|
| 01 | `status: validated`, `nyquist_compliant: true` | ✓ COMPLIANT |
| 02 | `status: draft`, `nyquist_compliant: false` (10 pending rows — genuinely unfilled) | NOT-VALIDATED |
| 03 | `status: validated`, `nyquist_compliant: true` | ✓ COMPLIANT |
| 04 | `status: draft`, `nyquist_compliant: false` (1 pending row of 164 lines — likely just a missed status flip) | NOT-VALIDATED |
| 05 | `status: draft`, `nyquist_compliant: false` (4 pending rows of 1103 lines — likely just a missed status flip) | NOT-VALIDATED |
| 05.1 | `status: validated`, `nyquist_compliant: true` | ✓ COMPLIANT |
| 06 | `status: draft`, `nyquist_compliant: false` (literal unfilled template — never reconciled) | NOT-VALIDATED |

Per the #2117 rule, `NOT-VALIDATED` is a coverage TODO (`/gsd-validate-phase {N}` was never run), not a compliance failure — `nyquist_compliant` is not authoritative for these 4 phases either way. Phases 04 and 05 look substantively filled and likely just need the status reconciled; phases 02 and 06 are closer to genuinely unfilled.

## Tech Debt Summary

**7 items across 6 phases/cross-cutting concerns** (see YAML frontmatter `tech_debt` for full detail). None blocks milestone completion by the project's own established convention (hardware/real-home items explicitly gate `1.0.0` only, not phase or milestone completion — see `PROJECT.md` Constraints and `CLAUDE.md`'s Release section). **Resolved during this audit:** Phase 2's two backstop human-verification items had no tracking anywhere at the milestone level; they are now rowed in STATE.md's Deferred Verification table.

---

## ▶ Next Up — Basement Guardian

**Options, not auto-routed** (this audit stops here for a maintainer decision; the workflow's `tech_debt` status offers both):

**A. Complete milestone** — accept the tech debt above, track it in the backlog, and proceed:

```
/gsd-complete-milestone 1.0
```

**B. Address debt first** — at minimum, add Phase 2's two orphaned human-verification items to STATE.md's Deferred Verification table (a documentation fix, not new code), and/or run `/gsd-validate-phase {N}` for phases 02/04/05/06 to reconcile Nyquist status. Then re-audit.

**Also available:**
- `cat .planning/v1.0-MILESTONE-AUDIT.md` — this report
